# 安全策略

SkySheep 的定位是「普通用户也能安全用的桌面 Agent」，权限确认与网络安全是产品底线。感谢你负责任地披露漏洞。

## 支持的版本

目前只为**最新发布版本**提供安全修复。发现漏洞时请先升级到最新版，确认问题仍存在后再报告。

| 版本 | 支持情况 |
| --- | --- |
| 最新一版 | 支持安全修复 |
| 更早版本 | 不支持 |

## 如何报告漏洞

请使用 GitHub 私密漏洞报告：

1. 打开仓库 <https://github.com/Sky-scrape/SkySheep>；
2. 进入顶部 **Security** 标签页；
3. 点击 **Report a vulnerability**，按表单填写并提交。

**不要**通过公开 issue、PR、Discussions 或任何其他公开渠道报告安全问题，以免在修复发布前被恶意利用。报告中请尽量包含：受影响的版本号、复现步骤或概念验证、影响评估，以及（如已有思路）修复建议。

## 响应承诺

- 我们会在收到报告后**约 90 天内响应**，期间可能就复现与影响与你沟通；
- 确认为有效漏洞后，我们会尽快修复、发布新版本，并在发布说明与安全通告中向你致谢；
- 修复版本发布前，请勿公开披露细节；披露时间由双方协商确定。

## 重点关注面

以下属于 SkySheep 安全模型的核心，任何绕过或破坏都按高危处理：

- **权限门绕过**：写文件、执行命令类操作必须经 PermissionGate 确认（`engine/src/skysheep/security/gate.py`），任何绕过确认流程直接生效的路径都是漏洞；
- **工作区信任绕过**：项目目录下的 `.skysheep/mcp.json` 与 `.skysheep/skills/` 必须先经用户确认（`engine/src/skysheep/security/trust.py`）才可执行或注入；任何「打开目录即生效」、或使信任检查被跳过的路径（包括仅凭内容变更绕过指纹复核、跨项目泄漏信任、篡改信任记录）都是漏洞；
- **SSRF**：`web_fetch` 的 SSRF 防护（仅允许公网地址、逐跳校验、解析结果固定为连接目标）不得被绕过或诱导放行内网地址（含 DNS rebinding 形式的两阶段解析）；
- **提示注入导致的数据外泄**：若网页、MCP、技能等外部内容能驱动 Agent 读取并发送本地敏感数据（含 `~/.skysheep/` 下的配置与密钥），请报告；
- **检查点/备份完整性**：会话检查点与滚动备份机制若可被篡改、伪造或污染历史数据，请报告。

## 已知取舍（不是漏洞，但你需要知道）

- **渠道凭据以明文保存在用户目录**：飞书渠道的 App ID / App Secret 落
  `~/.skysheep/`（`feishu-cli/` 与其配置），微信渠道的 `bot_token` 落
  `~/.skysheep/config.toml`。与 API Key 同一处理方式：只在本机磁盘上、不进
  版本库、界面上只回显「已保存」不回显明文；但任何能读到你用户目录的本地程序
  都能读到它们。请把 `~/.skysheep/` 当敏感目录对待。
- **局域网 / Tailscale 访问走明文 HTTP**：远程访问靠访问令牌（`?token=` 会进入
  浏览器历史与日志），cookie 未加 `Secure` 标记。在可信网络里用没问题；跨不可信
  网络时请自行套一层 TLS 隧道（如 Tailscale 本身的加密、或反向代理 + 证书）。
- **电脑控制与浏览器控制默认关闭**：需要在 设置 · 远程控制 里显式打开；
  `run_command` 的子进程会剥离密钥类环境变量（`*_API_KEY` 等），依赖这些变量的
  命令需要自己在命令里传参或改用配置文件。
